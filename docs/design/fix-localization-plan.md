# Localization Fix Plan

> Living document. The "why" is the contract; the phases are the route. Update as we iterate.

## ▶ Orientation — PAST / PRESENT / FUTURE (updated 2026-07-24)
> This doc runs long. Start here: this block is the "you are here" map. Detailed history lives in the dated session entries below; the phase route + the current engine are in "★ PRIMARY registration approach"; forward strategy vs. `main` is in "Cross-stream design directions" immediately below. The "▶ NEXT SESSION" block further down keeps the step-by-step immediate actions.

**⏮ PAST — done & device-validated** (detail in the dated entries below)
- **Phase 0 (2026-06-12/15):** the drift-*compounding* hypothesis was DISPROVEN. The real problem: ε (relocalization seating error) is **approach-dependent and decimeter-scale** — users won't re-approach to the inch, so meshes land ~25 cm apart across visits unless we refine.
- **The pivot (2026-07-09):** dense mesh-ICP (Phase 2 — built + device-validated but brittle on lumpy LiDAR, CPU-heavy) **demoted to fallback**; **plane-to-plane registration is PRIMARY** (robust to noise, cheap, scales with surface count not face count).
- **Built + validated:** `SaveRegistration`, ghost proxy, `PlaneRegistration` solver, live ghost auto-align; the **DECISION 3 postprocess stage** (raw-only save · deferred post-save RoomBuilder · hard gates · proxy · colorize · bad-scan detection); the RoomCaptureSession **retain fix** (killed the whole "RoomPlan produced nothing" failure class), the **double-load fix**, the **wireframe cross-section fix**.
- **Registration validated end-to-end (2026-07-20):** a real rescan through record→save→gate→Process→**APPLIED**; the **window-wall bistability** found, and solver **v2 trim rescue** built + validated (APPLIED trans=7.0 cm, inside the live-measured 5.6–7.3 cm band).
- **Merge-back to main (2026-07-21):** the two merge landmines (privacy×colorize leak, legacy-scan blast radius) defused *before* merging; `origin/main` merged in (`47bebf0`), the 2 real conflicts resolved, build green.
- **Merge smoke PASSED (2026-07-22):** on-device new-scan save→Process — capture path intact (`meshClassifier=on`, mesh→413k faces, video format pinned 1920×1440@30fps + hiRes excluded = the M2 iPad fix live), colorize privacy guard **armed + fired** (`[VertexColor] privacy mask mode ON`), proxy built (130k faces / 5.0 MB), upload/postprocess gate confirmed. The log's apparent "double roomplan write" → investigated, **not a bug** (scan-root copy + `raw_data/` mirror, both consumed, kept in sync); fixed a latent `MeshPreviewView` raw_data-first read-order footgun (`997ad1c`).
- **Corners closed + min-area guard shipped + validated (2026-07-22):** all on-device corners pass — colorize toggle-OFF (structural-only Process, colorize never attempted), bulk Process (Latest/All across locations; per-scan pending-steps → deliberately heterogeneous batches, all cleared their gates), legacy retroactive registration (dev toggle → a legacy scan **APPLIED trans=11.4 cm**, 4 walls, RMS 13.6 mm, weakFrac 0.984), colorize-after-registration (un-apply trans matches the APPLIED value, colored ratio ~97%). Shipped the **min-area (1 m²) guard on room-decoded fit planes** (`1104d4b`): drops RoomPlan slivers from the fit (device-validated on a real sliver — finalRMS 26→8 mm, weakFrac 0.53→0.95, no trim needed, trans ~7 cm), while the proxy keeps them (`minArea:0`) so the ghost still draws every plane; + a colorize un-apply diagnostic (`0e5e5af`). **Track-loss resilience:** the plane path survived a mid-session VIO reinit + mesh reset and still APPLIED cleanly.
- **PR #28 opened + review→fix hardening (2026-07-22):** ran a multi-agent self-review of the whole branch (6 concern-area reviewers → adversarial verify → Copilot cross-check → completeness critic), then folded in Copilot's PR comments across **5 review rounds (all build-verified)**. Convergence signal: substantive findings fell 3→4→2→1→1 and the tail was pre-existing/low-confidence — Copilot re-scans the whole ~5k-line diff each push, so it samples pre-existing items indefinitely rather than converging to zero; stopped when in-scope findings were exhausted (deferred items logged under FUTURE → "Deferred from PR #28 review"). The one substantive defect: `ScanPostprocessor` read main-actor SwiftData `@Model` state (incl. faulted relationships) off the utility queue — fixed by snapshotting everything on main into a `ScanWork` struct *before* dispatch, plus a per-scan in-flight lock extended to the bulk/single colorize paths (so a Process bake and a colorize can't race the same in-place `mesh.obj`). Also hardened: privacy **fail-closed** on indeterminate status in *both* colorize + export, era-detected via the `privacy_filter` metadata key (a lost-`masks/` deferred scan can't silently colorize raw depth, while genuine legacy — readable metadata, no key — still colorizes and legacy exports skip the redundant Vision pass); the dev-mode-off reset now restores `meshClassifier` + the four diag toggles (a bench-OFF value no longer leaks to production); **one deterministic `CapturedScan.canonicalOrder`** for every canonical-owner site (gen-0 / run order / DECISION-3 gate can't flip on a `capturedAt` tie); `canonicalRoomFrame` framed on walls/floor per its doc; `ghostIsProxy` reset hygiene; and the stale "a snap splits the saved mesh" comments corrected to the snap-STORM semantics ([[saved-mesh-repins-immune-to-snaps]]). Two review findings were adversarially verified as **false positives and dismissed** (ICP mid-refit transform rollback — the kept transform *is* the last validated solve; whitening scale-guard — the `1e-9` floor is arithmetic, the raw-eigenvalue gate is the real degeneracy backstop). All 5 Copilot comments (3 first pass + 2 later) resolved. Full off-main fix is verified *statically* (no `@Model` touched off-main); simulator build green throughout.

**⏸ PRESENT — in flight**
- **PR #28 MERGED 2026-07-23** (`4f74a97`; `plane-registration` → `main`); the new stream is **adjacent-stitch plane alignment** (see FUTURE + the "★ ADJACENT-STITCH plane alignment" design section). **Post-merge follow-ups still open, none a blocker:** (a) one device **Process smoke** to confirm the postprocess concurrency refactor didn't regress gating — a single Process on a rescan + one bulk Process (this is a *functional-regression* check; the off-main fix itself is verified statically, and the colorize-vs-Process race it also closed is low-stakes/recoverable); (b) the *visual* person-exclusion eyeball — a scan with a person in frame, confirm no person pixels baked (code path confirmed live; only the eyeball is outstanding).
- **Notable open thread (NOT ours, NOT a blocker):** the capture-path **ARFrame-retention starvation** escalated across the 07-22 runs — from a brief blip to *causing* a mid-session VIO reinit + mesh reset (delegate retaining 11–13 frames, fps→7–18, jpeg_encode 200–425 ms, capture I/O backlog). This is main's `30fps + deferred-blur + personSegmentation` capture path, not the localization core (our code runs only at postprocess) — the graduation-gate "sustained CPU contention → VIO collapse" mechanism, now observed live. The PR review also flagged `bulkExport`/`bulkUpload` doing the **same off-main SwiftData reads** the postprocess fix addressed — **verified byte-identical on `main`** (inherited, not introduced here) → belongs with this same capture-perf/threading ticket, not this PR. Worth a capture-perf ticket. **(2026-07-24 correction — it is NOT an app retain leak):** the save path has capped in-flight frame encodes at **2** since `d6eb3fd` (2026-06-02, `maxFramesInFlight`), and nothing stores an `ARFrame` (the delegate extracts scalars/buffers and drops the wrapper), so the "retaining 11–13 frames" cannot be ours — they're framework-internal consumers (ARKit meshing + RoomPlan-continuous), i.e. the same CPU-contention mechanism, not a retention bug. Decisive check: `inFlightSaves` sits ≤2 while ARKit reports 11–13 → save path innocent. Reframes the ticket from "chase a retain leak" to "shed capture-path CPU load" → see the **live-light capture** pivot under "Cross-stream design directions".
- On merge: delete `plane-registration` + `sync-coordinate-space` (+ the pre-rebase backup); continue on fresh short branches.

**⏭ FUTURE — queued (todo)**
- **Untested corners — ALL ✓ on device 2026-07-22:** upload/postprocess gate · colorize toggle OFF · bulk Process (Latest/All) · legacy retroactive registration (dev toggle) · colorize-after-registration. (Corner list closed.)
- **Deferred builds:** door/opening quads (design settled) · v3 solver: trim by TARGET (a wall's fragments drop with it). *(min-area floor on room-decoded planes — shipped 2026-07-22, `1104d4b`.)*
- **Deferred from PR #28 review (threading/perf — none a blocker; verified, chose NOT to patch in-PR):**
  - **`loadGhostMeshData` main-thread O(n) OBJ rewrite** (Copilot round 5): the ghost de-registration undo (`SaveRegistration.transformOBJ`) runs synchronously on the **main thread** at CaptureView appearance → possible UI stall on very large scans. The expensive full-mesh path is effectively **dev-only** — rescans load the small proxy (cheap `transformOBJ`); the full mesh only carries an applied registration under the dev-gated `registerLegacyScans`, otherwise `inverseForGhost` is nil and the rewrite is skipped. The function already reads the mesh `Data` on main, and a correct fix is an **async ghost-load refactor** around the world-map gate (`ghostLoadAttempted` `defer` + `cachedGhostMeshData` — decoupling them risks ghost-less relocalization firing while a ghost is still inbound = real regression surface). Do it with the [[ar-session-bringup-main-thread-freeze]] AR-bring-up main-thread work, not a review-cycle patch.
  - **`currentFramePlanes` scan-root-only read** (Copilot round 5, low-confidence): reads `roomplan.json` from the scan root only while `pendingSteps` tolerates the `raw_data/` mirror — a roomplan promoted only to `raw_data/` would make the proxy step silently see no planes and skip. Narrow (roomplan is at scan-root by proxy time); a one-line dir-then-raw fallback fixes it if it ever bites.
  - **`bulkExport`/`bulkUpload` off-main SwiftData reads** (inherited from `main`, verified byte-identical) **+ the ARFrame-retention starvation** → the capture-perf/threading ticket (see the "⏸ PRESENT" open-thread bullet).
- **Candidate capture pivot (PARKED behind stitching-alignment):** "live-light capture" — cheap `ARPlaneAnchor` structure + object-only dense mesh, RoomPlan + rich semantics offloaded to postprocess. Measurement-gated (ablations A/C/B; RoomPlan per-frame CPU is the blind spot). Full write-up under "Cross-stream design directions → Candidate capture pivot".
- **Plan-level:** Phase 2.2 (surface-the-refusal UX — console-only today) · 2.3 (thresholds from real data — first datapoint: ~26 mm RMS shape-repro floor vs. the 50 mm gate) · Phase 3 (layers ride the live frame) · Phase 4 (stitch pose graph / versioned re-baseline) — **4.2 / 4.1 designed 2026-07-23: see "★ ADJACENT-STITCH plane alignment"** · degradation-ladder rungs · SpaceAnalyzer×alignment conflict.
- **Cross-stream (main):** see the next section — the seams between our localization (geometric) layer and main's capture-source pluralization (photometric) direction.

## Cross-stream design directions — localization × main (added 2026-07-21)

While this branch went deep on localization (mesh-ICP → plane registration → the 4D canonical frame → DECISION 3 postprocess), `main` moved in a different design area entirely — **capture quality + capture-source pluralization**. The two merged near-cleanly textually (2026-07-21, `47bebf0`), but they meet at real design seams worth recording so they aren't re-derived at the crossroads.

**Where main went** (from `origin/main` + its design docs):
- Capture-path performance: **30 fps default** + **privacy blur deferred capture→export** (off the hot ioQueue) — to stop frame-pool starvation / VIO tracking loss on low-end devices.
- Privacy correctness centralized at export (per-frame `masks/` sidecar; all formats + a Vision fallback for unmasked frames).
- Device compat: hiRes-format exclusion (restores mesh on M2 iPad), MWDATMockDevice removal.
- **Forward direction (design docs):** REQ-031 hi-res stillness keyframes + a planned **voxel coverage grid**; **pluggable still sources** — onboard camera (default) vs. an external **360° equirect camera on a rod**; a **Meta wearable camera** stream (`MWDATCamera`). North star: **splat-quality photometric coverage** from many sources.

**The unifying frame.** The two streams are the **geometric** (ours: put every observation in one canonical frame, over time) and **photometric** (main's: get the best pixels from the most sources) halves of one system. They meet at exactly one place: **every photometric sample needs a pose in the canonical frame to be useful — and pose is our layer.** Main's source pluralization is, downstream, a demand on our registration.

**Decisions NOT in conflict — already aligned / convergent (bank these):**
1. **Both streams pivoted dense → sparse-structured.** Ours: dense mesh-ICP → 5–20 RoomPlan planes. Main's 360 doc: many narrow pinhole stills → one omnidirectional equirect per voxel. Same lesson (structure resolves the ambiguity that dense sampling burns compute fighting) — it should govern future primitives too.
2. **Both independently built "defer heavy work off the hot capture path."** Main: blur → export. Us: registration/proxy/colorize → postprocess. Same antagonist (the capture path is sacred; VIO quality is the hard constraint), same answer. ⇒ **DECISION 3's postprocess stage is the natural home for main's future heavy work too** (splat gen, coverage grid, 360-still transfer/stitch) — they're the same architectural slot, not parallel pipelines.
3. **The plane pivot future-proofed localization for main's multi-source direction.** Dense LiDAR-mesh ICP is LiDAR-only; had we kept it as primary, localization would be a dead end the moment non-LiDAR sources (360, wearable) arrive. Plane/structural registration is **source-agnostic** — RoomPlan today, photogrammetric planes tomorrow, same solver. (Fortunate as much as foresight.)

**Decisions we WILL face at crossroads (not yet forced):**
1. **Source-agnostic registration.** To seat a 360/wearable still (no ARKit primitives) in the canonical frame, `PlaneRegistration` must accept planes from photogrammetry, not just RoomPlan/`ARPlaneAnchor`. Cheap discipline NOW: keep `PlaneRegistration.Plane` free of ARKit assumptions (it already is) so this stays a *plane-source* question, never a solver rewrite. Extends the degradation ladder (a new "rung 0.5: non-LiDAR photometric source").
2. **Unified compute / thermal budget.** Live VIO + our postprocess + a 360 rig streaming 70 MP JPEGs over WiFi draw on one budget with no unified accounting. We model it from the compute side (ICP CPU attribution); main from the capture side (30 fps). Nobody models it whole — this binds when sources stack.
3. **Unified coverage model.** Main's voxel coverage grid (photometric: which voxels seen, from which directions) and our observability (`weakAxisFrac` — enough structure to align?) + ghost-proxy coverage + the wall-extension-honesty problem are **one concept in two metrics**. A shared coverage representation would serve splat quality AND registration observability AND the good-enough gates at once — probably the highest-leverage abstraction both streams are groping toward independently.
4. **The 360 rig IS a registration problem.** A rigid phone→camera extrinsic + rod-sway noise + the stillness gate = our machinery (fixed transform + seating ε). When the rig lands, seating its stills into the canonical frame is ours to own — not a parallel pipeline.

### ▶ Candidate capture pivot — "live-light capture" (added 2026-07-24 · PARKED behind stitching-alignment refinement)

> A design direction, **not active work**. Recorded so it isn't re-derived; **do NOT interweave with the current stitching-alignment effort.** Everything here is **measurement-gated** — size the prize (ablations below) before building any of it.

**Thesis.** Session degradation scales with polygon face count (2026-07-13: the `unnamed` CPU curve ~165% @85k → ~326% @400k → **~469% @700k** faces, mode-independent — it's ARKit meshing + RoomPlan-continuous CoreML, both framework-internal). Export/postprocess run *after* teardown and can be serialized against memory; the only **contended** budget is LIVE, where VIO + ARKit meshing + RoomPlan + frame I/O + render all run at once. So the target is the live *work*, not the stored data (keeping the full raw mesh costs memory, which is comfortable — ~1.9 GB @790k faces). Restructure capture around the plane pivot: **cheap structure live, dense mesh only for objects, RoomPlan + rich semantics offloaded to postprocess.**

**What runs where.** Live *keeps* ARKit `sceneReconstruction` (object mesh + per-face class) + cheap `ARPlaneAnchor` detection (structure + coverage) + VIO + frame capture; live *drops* RoomPlan-continuous (`RoomCaptureSession`) and the dense wall/floor wireframe render. Postprocess *gains* self-derived structure (RANSAC on `wall`/`floor`-classified faces from `mesh.obj`+`face_classes.bin`) + a downstream semantic model for the rich labels.

**Render routing — by per-face ARKit class + planarity.** Planar-structural (`wall/floor/ceiling/window/door`) → quad/plane, not dense; `table/seat/none` → keep dense mesh. `none` is the happy accident: ARKit only names `table`/`seat` among objects (8 coarse classes total, raw values at [ARCoverageView.swift:3272](../wisescan-ios/ARCoverageView.swift#L3272)), so lumpy objects (fridge/bed/appliance) land in `none` = exactly the 4D targets we want dense. Caveat: `none` also catches not-yet-confident flat wall faces → use the class as *hint* and a cheap **local planarity test** (normal coherence over the neighborhood — ARKit provides normals) as *arbiter*. This filters *our* render/retain of wall faces; it does NOT reduce ARKit's meshing (see hard limit).

**Why the semantics offload is safe (your call, well-founded).** The rich labels (bed/storage/refrigerator/toilet/…) are **RoomPlan-only already** — live ARKit gives just the 8 coarse `ARMeshClassification` values; the fine `CapturedRoom.Object.Category` set collapses into the display `SemanticClass` and is preserved only in `roomplan.json` ([AppConstants.swift:276](../wisescan-ios/AppConstants.swift#L276)). So offloading isn't losing a live capability — it's moving RoomPlan's *exclusive* contribution off the hot path. Saved RGB-D + poses + mesh is textbook input for indoor 3D semantic seg (the category list ≈ ScanNet200 / SUN RGB-D classes). Pragmatic path: per-keyframe 2D open-vocab detector → back-project to mesh via poses → instance-cluster → oriented boxes. Heavier alt: 3D mesh/point seg. New infra, but postprocess cost (accepted).

**The dollhouse is a postprocess artifact — and worth it on its own merits.** A parametric dollhouse (planes + labeled boxes) reads far better than a dense colored mesh from a top-down / "Google Maps" stitch view (colored floor = visual noise; boxes-and-planes is legible). "Halfway-RoomPlan" decomposes into a room **shell** from RANSAC planes (easy-ish) + labeled object **boxes** from the downstream model (the real work). Both offline. Independent argument to invest in the parametric representation for the stitch/overview UI regardless of the perf motive.

**Coherence (why this shape).** Objects = the **4D payload** (dense, the change-tracking targets); walls = the **stable registration scaffold** (cheap planes suffice). The canonical plane-registration already aligns sessions → detailed objects land in a common frame → object-change tracking over time. Registration is coupled to RoomPlan's type only at *ingestion* ([PlaneRegistration.swift:93](../wisescan-ios/PlaneRegistration.swift#L93)); the internal `Plane` is source-agnostic (and this is already banked as the source-agnostic-registration crossroads above) → swapping the plane source is a contained refactor + gate re-tune, **de-riskable offline**: RANSAC-fit planes from existing scans' classified meshes and diff against their `roomplan.json` planes, no new device runs.

**The hard limit — `sceneReconstruction` is global / all-or-nothing.** ARKit meshes the *whole* scene (walls included) whenever it's on; you cannot scope meshing to objects, and classification is a *label on already-meshed faces*, not a meshing filter. So the pivot removes RoomPlan-continuous (big) + our wall render (medium) but **not** ARKit's wall-meshing (bundled with object meshing) — that's the irreducible floor (ablation B). If B is the killer, the only non-SDK-fighting lever is **capture guidance**: the coverage grid doubles as a meshing governor — once a wall's plane fit is stable, stop cueing dwell there, redirecting session budget (and re-meshing) to the objects. Can't stop ARKit meshing a wall it sees; can stop the user dwelling on it.

**Coverage viz — decouple render resolution from mesh resolution.** Live wall/floor coverage feedback is *needed* (a single RoomPlan quad can't tell you where you still need to sweep), but the current per-anchor wireframe couples viz cost to mesh density — the thing that grows unbounded with dwell. Replace with a **fixed-resolution occupancy grid** projected onto the (cheap `ARPlaneAnchor`) plane extents: ~10 cm cells that fill as depth samples land. A 4 m×2.5 m wall = ~1000 cells forever, vs tens of thousands of accumulating triangles — same coverage signal, bounded cost. Doubles as the meshing governor above.

**Measurement plan (do FIRST — sizes the prize).** Dev-flag ablations; watch fps + cpu-by-thread + frame-delivery gap *as the session drags* (degradation is dwell/area-dependent — a 3-min run, not a snapshot):
- **A — kill our wireframe build** (early-return `buildWireframeForAnchor` [ARCoverageView.swift:1295](../wisescan-ios/ARCoverageView.swift#L1295)): isolates *our* app-side polygon render cost. One line.
- **C — kill RoomPlan-continuous** (don't start `RoomCaptureSession` during recording): isolates RoomPlan CoreML/frame. **This is the current blind spot** — we've benched RoomPlan *memory* (~200 MB model + up to ~200 MB end-of-session footprint) but have essentially **no per-frame/per-second CPU number** for it. Ablation C is exactly that measurement.
- **B — kill `sceneReconstruction`** (`config.sceneReconstruction = []`): isolates ARKit meshing = the irreducible floor. Diagnostic-only (loses mesh + classification).
- **Partition:** A + C are *removable* (→ this pivot's win); B is *irreducible if you want any dense mesh*. Run A → C → B. If C is a big slice of the 07-13 `unnamed` load, the pivot clearly pays; if the load is mostly B, the pivot needs the capture-guidance lever to matter.

**Topology / feasibility notes (ground the cost model).** RoomPlan shares the app's **single** ARSession (`RoomCaptureSession(arSession:)` [ARCoverageView.swift:1676](../wisescan-ios/ARCoverageView.swift#L1676), `arSession = arView.session`) — it is *not* a second VIO/camera pipeline, so its cost is the continuous CoreML analysis layered on top, not a duplicate. RoomPlan perception is **live-only**: no API accepts recorded frames, and `CapturedRoomData` is Codable-in-signature-only (throws at encode — the 2026-07-16 sidecar saga), so RoomPlan genuinely *cannot* be re-run from raw data in postprocess. That's *why* the plan is "derive planes + semantics ourselves," not "defer RoomPlan." The save-path ARFrame retention is already bounded (see the ⏸ PRESENT correction) and is orthogonal to this.

## Phase 0 findings & course correction (2026-06-12)

Phase 0 shipped (0.1/0.2/0.3) and the data **redirected the diagnosis**. The headline hypothesis below — that rescans compound drift and the saved map *degrades into a multi-session blob* until gen 3 can't localize — is **disproven**. What actually happened:

- **The gen-3 "never localizes" failure was a save bug, not drift.** The save path re-ran `makeConfiguration()` with `initialWorldMap=nil` before `getCurrentWorldMap`, detaching the session from the map it had relocalized into — so a relocalized generation persisted only the ~hundred feature points it observed itself. The map **collapsed** (gen-1 saved 5549 features → gen-2 saved **159**) — the *opposite* of blob growth. gen-3 then had 159 features to relocalize against and couldn't lock.
- **Fixed** ([CaptureView+Recording.swift](../wisescan-ios/CaptureView+Recording.swift), commit `e976c11`): mutate the *live* config (`sceneReconstruction=[]`, no `resetTracking`) instead of re-running a nil-map config, preserving the merged map. **Verified on device:** gen-2 now saves **10357** features (5549 inherited + new), and **gen-3 localizes in 3.9 s** with a clean settle.
- **ε is small and not (yet) compounding.** Settle landed ~12 cm from origin; ICP correction was ≈8 cm at gen-2, ≈1.5 cm at gen-3 — it went *down*, not up. The map **accumulates by area** (deduped), not unboundedly — re-scanning the same space converges because ARKit refines existing features rather than duplicating them.
- **No 25°/3 ft false-lock observed.** Every lock so far has been clean. The Phase-2 ICP gate still matters in principle, but we have **zero** gross-residual data to set its threshold.

**Effect on the plan:**
- **Phase 1's stated justification (stop catastrophic compounding) is gone.** A canonical frame is still wanted for **Phase 4 (3D-stitch durability)** and the **4D same-frame-every-visit** goal, but it is no longer a localization-*failure* fix. Priority drops; it is now **gated on 0.4 below**.
- **0.4 (the real Phase-0 exit) is CLOSED (2026-06-15) with a TWO-PART result:**
  - **(a) For an *identical* re-approach, ε is bounded** — settle pose held to ~1.5 cm across gen 3/4/5, ICP correction decreased. No compounding off the relocalization chain. → **Phase 1 stays demoted** (it fixes *which* map you relocalize against, which was never the problem).
  - **(b) ε is APPROACH-DEPENDENT and decimeter-scale.** When the start standpoint differs (gen 6/7), the relocalization registration error jumps to **~25 cm** (gen-7, the trustworthy measurement: 99% inliers, well-constrained, `trans=25.4cm`; gen-6's `68cm` was the same effect *overstated* by 46% partial-overlap letting ICP wander). The careful "same start to the inch" protocol on gen 3/4/5 was *masking* this — few-cm reproducibility came from re-approaching identically, not from relocalization being intrinsically tight. **This is the real product problem:** 4D users won't re-approach to the inch, so meshes land ~25 cm apart across visits. **The fix is Phase 2.1 (ICP-refine the relocalized pose against the canonical mesh before save), NOT Phase 1.**
  - Residual watch (map longevity, NOT ε): `features` oscillates with coverage (10357→11585→15677→15218→19718) — went *down* at gen-6, so it tracks coverage rather than accumulating monotonically; not a runaway. Keep an eye across equal-coverage passes.
- Failure mode #2 (frozen-layer desync) is unchanged and **deferred by decision** — the saved/colorized preview renders from the saved mesh, not the live `world:.zero` layers, so it is correct today; live-viz fixes (Phase 3) wait.

Also shipped this pass (scanning robustness, not localization-plan items, but they keep relocalization runs clean): a **VIO-guard fix** (commit `03acb18` — a bare frame-delivery gap from a compute hiccup no longer halts the scan; it must be corroborated by hard-degraded tracking, and `.relocalizing` is treated as benign recovery) and a **double-save guard**. Relevant here only because a session cut mid-relocalization can't localize.

## Background — what's broken and why

**Symptoms (observed on device):**
- 1st scan of a space is fine. 2nd scan localizes but with a **consistent offset**. 3rd scan starts the AR session but **never localizes** (or lands ~25°/3 ft off). *(2026-06-12: the gen-3 "never localizes" turned out to be a save bug — now fixed; see "Phase 0 findings" up top. We have **not** reproduced the ~25°/3 ft false-lock; treat that parenthetical as unverified.)*
- Within a single render, layers **diverge from each other** — the live mesh sits on reality while voxels / connectors / ghost mesh drift off.

**Root cause (two failure modes, one false premise).**

Every visual layer was built on the assumption that an `ARWorldMap` re-establishes a **fixed, reproducible coordinate frame** each time you load it — so geometry baked to that frame's numbers will align in passthrough/VR. That premise is false in the part that matters:

- An `ARWorldMap`'s **internal** frame (feature/anchor coords relative to its own origin) *is* immutable across loads. ✅
- Its **registration to physical reality** is **re-estimated on every relocalization with fresh error each time**. The origin (`0,0,0`) is an arbitrary `t=0` datum with no physical pin; it rides that re-estimate. ❌
- The only thing fixed to reality is an **`ARAnchor`** — ARKit re-pins anchors to their physical feature every frame as the floating frame corrects. The coordinate frame floats *by design*; anchors are the remedy.

From that, two distinct problems:

1. **Compounding (the chain).** ⚠️ **(2026-06-12: this is the failure mode Phase 0 DISPROVED — see "Phase 0 findings" up top. The gen-3 failure was a save bug that *collapsed* the map to 159 features, not drift that bloated it into a blob; the map actually accumulates healthily and gen 3 localizes after the fix. The "relocalize against the latest scan" chaining is still architecturally worth changing for Phase 4 / frame stability, but it is NOT what caused catastrophic failure. Kept for history.)** Rescans relocalize against `sortedScans.first` — the **latest** scan ([LocationDetailView.swift:117](../wisescan-ios/LocationDetailView.swift#L117) / [:140](../wisescan-ios/LocationDetailView.swift#L140)) — and re-save a *new* merged world map. So each generation bakes a fresh relocalization error on top of the last, and the saved map degrades into a multi-session blob. By gen 3 the map is offset *and* an ambiguous relocalization target → catastrophic failure. (The [CaptureView.swift:133-138](../wisescan-ios/CaptureView.swift#L133-L138) comment treats this chain as a lossless "continuous frame" — it isn't.)

2. **Frozen-layer desync.** Nearly every overlay is placed with RealityKit `AnchorEntity(world:)` — a one-time static placement that does **not** follow ARKit's ongoing corrections (loop closure, post-relocalization snaps). The live wireframe even bakes `ARMeshAnchor` geometry into world coordinates under `world:.zero` ([ARCoverageView.swift:735](../wisescan-ios/ARCoverageView.swift#L735)). So after any frame correction, baked layers lag while the camera and live anchors move — hence the layer-vs-layer divergence.

**Reframe for our target hardware:** we target **LiDAR** devices. Sub-degree / few-cm relocalization noise is acceptable. **25°/3 ft is not drift — it's relocalization *failure*** (wrong-feature lock on a degraded map). The fix must therefore (a) **never compound**, and (b) **never trust a relocalization it hasn't geometrically verified** with the LiDAR mesh.

## Target architecture

- **One fixed canonical frame per location.** A designated canonical map defines the location's coordinate space permanently. All scans — 4D repeats included — relocalize **into** it and never redefine it. Re-baselining is a deliberate, versioned op, never a side effect of a normal scan.
- **LiDAR ICP as the trust layer — registered to *structure*, not content.** `ARWorldMap` relocalization is a *coarse bootstrap*; point-to-plane ICP of the live LiDAR mesh against the canonical mesh **refines** the pose and yields a **residual** that **gates** the save (rejects the 25°/3 ft locks). ARKit gives a tracking *state*, not an alignment *error* — the residual is the missing signal. Crucially the ICP registers against the **stable scaffold** (walls/floor/ceiling/built-ins), not all geometry, so moved furniture can't bias or fail the lock — see "Scene change vs. registration" below.
- **Pose graph over location frames (3D stitch).** Inter-location transforms are edges between *canonical frames*, not scan instances. 4D updates register into a frame and never move it, so they **can't disturb** the 3D layout. The graph changes only on deliberate re-baseline.
- **Overlays ride the live frame.** Every layer derives its pose the way the camera feed does — current-session geometry bound to live `ARMeshAnchor`s; cross-map geometry bound to `ARAnchor`s stored in the world map — never baked to frozen numbers.

### The two product modes this must serve
- **4D (scan in time):** one space, repeated scans over time, each colored mesh landing in ~the same coordinate frame each visit → served by the fixed canonical frame + ICP-refined registration.
- **3D (stitch space):** many known locations stitched together, transforms holding even as individual locations are updated in 4D → served by the pose graph over fixed canonical frames.

### Scene change vs. registration (the 4D contract)
4D *is* change capture — furniture moves, objects come and go, and over a long horizon **structure itself changes** (a wall comes down, a room is renovated). The system must register reliably *through* that change and treat the change as the **product**, never as registration error. The mesh-to-mesh difference between a good relocalization in a redecorated room and a blown relocalization looks identical to a naive global RMS — so the design must separate the two.

**One principle: separate the *frame* from the *content*. Register to what doesn't move; the part that moved is the 4D signal.** This yields three regimes, handled distinctly (not two):

1. **Content change (furniture rearranged) — normal 4D.** Register to the stable scaffold; moved objects have no canonical surface nearby, so they fall out of the ICP inlier set automatically and can't bias the pose. The **outlier fraction is the change metric** — reported, not rejected. Inlier residual stays small (few cm); inlier fraction stays high.
2. **Partial structural change (a wall moved, an extension added) — still registerable, and high-value 4D data.** Enough original scaffold usually survives to register into the canonical frame on the *surviving* structure. The changed structure shows up as outliers **classified as wall/floor/ceiling** (vs. seat/table) — that semantic split is what distinguishes "someone moved the couch" from "someone moved the wall," and the latter is a higher-priority change alert.
3. **Wholesale change (renovation; too little scaffold survives) — graceful re-baseline, not silent failure.** When the inlier fraction drops **below the trust floor**, you genuinely can't register into the old frame. Don't reject silently and don't save garbage: detect it from the data and **surface a versioned re-baseline** (Phase 4.4). **The original baseline is never moved** — it stays the permanent coordinate authority because connector/stitch transforms are pinned to it. Re-baseline pins a *new generation* to gen 0 via a measured old→new transform (the surviving connectors double as the fiducials that measure it), so the 3D graph and the timeline stay intact.

**Implications that propagate into the phases:**
- The save gate is **not** a global RMS. It is **(inlier RMS < threshold) AND (inlier fraction > floor)**, with outlier fraction (classified) logged as the change metric (Phase 2.2/2.3).
- "Stable scaffold" comes from semantics already in the app — RoomPlan surfaces and/or `.meshWithClassification` (wall/floor/ceiling vs. furniture). Restrict/up-weight ICP correspondences to structural classes; geometric fallback = the large planes. 
- The canonical scan is chosen for **structural completeness**, not recency or being-first — it's the registration reference forever. Canonical = frame + scaffold; the **latest** scan always defines current content (Phase 1.1 / Open decisions).
- Re-baseline is **data-triggerable** (inlier fraction below floor, or sustained structural-class change), versioned, and timeline-preserving — not only a manual renovation op (Phase 4.4).

---

## Plan

Sequenced by leverage and dependency. ~~**Recommended ship order: 0 → (1 + 3a parallel) → 2 → 3b → 4.**~~ **Revised 2026-06-15 (0.4 closed, ε bounded):** the save-bug fix already restored multi-generation localization, and 0.4 confirmed ε does not compound — so Phase 1 folds into Phase 4. **Updated again 2026-06-15 (gen 6/7):** 0.4 part (b) found relocalization registration is **approach-dependent (~25 cm)** — so **Phase 2.1 (ICP-refine) is now the NEXT active phase** (fixes per-visit mesh offset; doesn't need a gross false-lock to justify it). Net active order: **probe upgrade (wider gate/more iters) → Phase 2.1 ICP-refine → 2.2/2.3 gate+thresholds → Phase 3a (un-defer) → Phase 4 (Phase 1 folds in).** Phase 1 alone never fixes per-approach ε; Phase 2.1 does. Open longevity check unchanged: equal-coverage passes to confirm feature count plateaus.
All work is **device-only** (ARKit does not run in the simulator).

### Phase 0 — Instrument & prove (cheap, reversible; do first)
Validates the diagnosis and produces the ICP residual metric Phase 2 reuses.
- [x] **0.1** Relocalization ε log: after relocalization settles, log live-camera pose vs. loaded map's origin anchor + the map's feature-point count. Confirms compounding + map growth per generation.
- [x] **0.2** LiDAR ICP residual (**log-only**): point-to-plane ICP of live mesh vs. canonical/prior mesh; log residual across good scans and the gross-failure cases to set a threshold.
- [x] **0.3** Layer-divergence probe: log a baked `world:.zero` reference vs. live camera before/after the relocalization snap.
- [x] **0.4** *(added 2026-06-12; measured 2026-06-15)* **Deeper-chain ε measurement.** Ran the tape-standpoint chain gen 3/4/5. **Result: ε is BOUNDED — Phase 1 not needed for localization.** Settle pose reproduced to ~1.5 cm across all three gens (gen3 `(-0.004,0.029,-0.109)`, gen4 `(0.010,0.020,-0.105)`, gen5 `(-0.000,0.020,-0.108)`; yaw -2.4/-2.5/-5.3°), and the ICP correction *decreased* (gen4 `trans=6.4cm initRMS=80mm` → gen5 `trans=3.7cm initRMS=43mm`). No walk-off, no compounding. **Open sub-question (map longevity, NOT ε):** `features` grew 10357→11585→15677 — partly confounded by coverage (gen-3 was a short pass), but the gen4→gen5 +35% is unexplained. Needs 2–3 *equal-full-coverage* passes to confirm same-area dedup plateaus vs. accumulates; if it keeps climbing on identical coverage, that's a map-size cap to schedule (not a failure). Minor: gen-5 ICP barely refined (43→41mm, inliers 100%→97%) — possible small scene change or rougher lock; watch.

  **ADDENDUM (2026-06-15) — gen 6/7 broke the same-start protocol on purpose, and found approach-dependent ε:**
  - gen-6 (different start, ~80% coverage): `initRMS=83mm finalRMS=49mm trans=67.8cm rot=0.80° inliers=46%`. The 68 cm looked alarming but was **overstated** — 46% inliers (partial overlap) + near-0° rotation + a generous 30 cm gate let ICP slide along a planar surface (floor invariance: point-to-plane to the floor is unchanged by horizontal translation, so `initRMS` stays low even at a big horizontal offset — my earlier "initRMS rules out a real offset" argument was wrong).
  - gen-7 (different start, **high coverage**): `initRMS=98mm finalRMS=68mm trans=25.4cm rot=2.37° inliers=99%`. **This is the trustworthy off-nominal datum** — 99% inliers fully constrains ICP, so no wrong-minimum escape. It says a different approach gives a **real ~25 cm** relocalization registration error. (Settle pose `(1.174,-0.268,-0.264) yaw=-98.3° (0.0s)` is just the different physical standpoint + pre-window relocalization; the 99% inliers confirm the frame IS the map frame, ~25 cm off — not a failed lock.)
  - **Conclusion:** ε ranges few-cm (identical approach) → ~25 cm (different approach). → Phase 2.1.
  - **Probe limitation exposed:** at a 25 cm offset the probe is at its measurement edge — `maxCorrDist=0.30m` is barely wider than the offset, and ICP **hit the 14-iter cap without converging** (finalRMS stayed 68 mm). To measure these offsets cleanly (and to reuse the ICP as the Phase-2.1 refine engine), **widen the correspondence gate (≥0.5–1.0 m) and raise the iteration cap**, ideally coarse-to-fine. Do this small probe upgrade first and re-measure a few different-approach scans to get a real ε distribution before/while building 2.1.
  - `corrections maxΔpos` of 1.2 m and `maxΔrot` ~90–98° at gen 6/7 are degraded-frame junk poses from rougher, more-movement sessions — not clean snaps; the corrections probe is noisy when tracking is rough.
- **Exit (original):** ~~numbers showing ε compounds, failures = large residual, baked layers lag the snap.~~ **Superseded 2026-06-12:** 0.1–0.3 proved the gen-3 failure was a *save bug* (map collapse), not compounding; that's fixed. The remaining open measurement is **0.4** (does ε compound now?). Snaps (0.3) confirmed non-zero → baked layers do lag (feeds Phase 3, deferred). No false-lock residual captured yet → Phase-2.3 threshold still un-set.

#### Implementation notes (code landed)
All three probes live in [LocalizationDiag.swift](../wisescan-ios/LocalizationDiag.swift), **log-only** and gated behind the existing `perfDiagnostics` Developer-Mode flag (same channel as the VIO instrumentation — `PerfDiag.log`, OSLog subsystem `org.arenaxr.scan4d`/category `perf`). Nothing changes capture behaviour; safe to leave compiled in. Log lines are tagged for grepping: `[LocDiag ε]`, `[LocDiag ICP]`, `[LocDiag snap]`.

Hook sites:
- **0.1 map stats** — [ARCoverageView.swift `makeConfiguration`](../wisescan-ios/ARCoverageView.swift) logs feature-point count / anchor count / extent / center every time an `ARWorldMap` is deserialized (the single chokepoint for all load paths).
- **0.1 settle pose** — `driveAlignmentPhase` logs the live-camera pose (position + yaw/pitch/roll, relative to the map origin = `0,0,0` after reloc) the moment relocalization first reaches `.normal`, plus seconds-to-settle. One-shot per alignment; reset on record/nominal transitions.
- **0.2 ICP** — ghost OBJ is stashed in `loadGhostMesh`; live world-space verts are sampled (capped per anchor) in `buildWireframeForAnchor` and, once ≥4000 accumulate during a relocalized recording, run **once** through a compact point-to-plane ICP (voxel-grid NN, subsampled, ≤14 iters, 6×6 Gauss-Newton) off the delegate queue. Logs `initialRMS` (residual at the relocalized/identity pose — the gross-failure signal), `finalRMS` (achievable floor), and the recovered correction (trans cm / rot °).
- **0.3 snap** — `SnapTracker` in `session(_:didUpdate:)` logs any single-frame camera-pose jump > 2 cm / 0.5° (the frame correction a baked `world:.zero` overlay does **not** follow).
- **Per-run summary** — a single `[LocDiag SUMMARY]` line is emitted at stop-recording for every relocalized run, consolidating map features/anchors + settle pose + ICP result + snap max/count, all **computed on-device** (no off-device math, no transcription — copy one line into the table). Summary state is main-thread-only; the delegate/ICP-queue probes hop to main to populate it. (If ICP is still in flight at stop, the summary shows `ICP: pending` and the standalone `[LocDiag ICP]` line carries the numbers — rare, since ICP triggers early in the pass and runs in ~tens of ms.)

**Important architectural finding (shapes Phase 2.1).** `sceneReconstruction` is `[]` during the pre-record relocalization/alignment phase — live `ARMeshAnchor`s only exist *after recording starts* ([ARCoverageView.swift:271-273](../wisescan-ios/ARCoverageView.swift#L271-L273)). So the ICP residual (0.2) is measured **during recording**, not at the settle moment. Phase 2.1 wants to "apply the ICP correction before `isRecording` flips on" — that will require enabling mesh reconstruction *during* the alignment phase (or a brief mesh pre-roll) so there's a live cloud to ICP against before the gate. Flagging now so 2.1 budgets for it.

#### HOWTO — run the profiling by hand (device-only; ARKit doesn't run in the simulator)
1. **Enable the flag.** Build to a LiDAR device, open **Settings → Developer Mode → Performance Diagnostics** (the `perfDiagnostics` toggle). Without it, every probe is a no-op.
2. **Open a log stream** (the values are logged `privacy: .public`, so they show literally — not `<private>`). Either:
   - Xcode console with the device attached, **or**
   - `Console.app` → select the device → filter on subsystem `org.arenaxr.scan4d`, **or**
   - terminal: `log stream --device --predicate 'subsystem == "org.arenaxr.scan4d" && category == "perf"' --style compact | grep '\[LocDiag'`

   Each relocalized run ends with one paste-ready consolidated line — everything computed on-device:
   ```
   [LocDiag SUMMARY] map=scan_3.arworldmap features=12043 anchors=2 | settle: pos=(0.83,1.42,-0.19) yaw=-23.4° (3.2s) | ICP: initRMS=84.2mm finalRMS=11.0mm corr(trans=7.3cm rot=2.10°) inliers=1840/2500 (74%) iters=9 | corrections: count=4 maxΔpos=4.1cm maxΔrot=1.30°
   ```
   The individual `[LocDiag ε]` / `[LocDiag ICP]` / `[LocDiag snap]` lines are still emitted for detail/debugging, but the SUMMARY line is all you need to fill a table row. (For the **never-localizes** case there's no recording → no SUMMARY; grab the single `[LocDiag ε] map load …` line instead.)
3. **Mark a physical standpoint.** Put a piece of tape on the floor. Every generation below, start the scan standing *on that tape, facing the same wall* — this is what makes the `[LocDiag ε] settle` camera pose comparable across generations.
> **⚠️ Updated 2026-06-12 — the save-bug fix shipped, so gen 3 now LOCALIZES.** The steps below were written for the pre-fix world where gen 3 was expected to fail; the *failure-capture* framing (steps 6–7) is now mostly historical. The save path no longer collapses the map (it preserves the inherited features), so the new goal of this procedure is **0.4: extend the chain to gen 4/5/6 and confirm ε stays bounded** — watch `[LocDiag ε] map load … features=N` *flatten* (it should grow only with new area, then plateau) and the `settle` pose stay within a few cm of the tape across generations. A *growing* `features` on the same area, or a settle pose that walks off the tape generation-over-generation, would be the compounding we originally feared (and would re-justify Phase 1). The original gen-2/gen-3 failure-contrast goal is moot for localization, but the **false-lock residual** (step 6, if you can ever provoke one) and the **4D change trials** (step 8) are still un-collected and still feed Phase-2.3.

4. **Generation 1 (baseline).** Fresh scan of the space, save it. (No map loaded → no `ε`/`ICP` lines yet; expected — gen 1 defines the frame, and it becomes the relocalization target for gen 2.)
5. **Generation 2 (tolerable reference — this one records).** **Rescan Space** → stand on the tape → let it relocalize (it will, *with the consistent offset*) → confirm alignment → record a short pass that re-sees the same surfaces as the ghost → save. Capture:
   - `[LocDiag ε] map load … features=N` — note N (the gen-1 map's size).
   - `[LocDiag ε] relocalized settle: cam pos=… yaw=…` — the offset pose (compare to where you *know* you're standing on the tape).
   - `[LocDiag ICP] initialRMS=… finalRMS=… correction trans=… rot=…` — the **tolerable** residual (the consistent offset's magnitude). This is the accept end of the threshold.
6. **Generation 3 (the unfixed failure).** **Rescan Space** again → stand on the tape. Now the target is the gen-2 *merged* map. Two outcomes, logged differently — record whichever you get:
   - **Never localizes** (session starts, tracking never reaches `.normal`): `[LocDiag ε] settle` **never fires**, recording never starts, so `[LocDiag ICP]` **never fires** either. The proof is that *absence* **plus** the `[LocDiag ε] map load … features=N` line showing N **inflated vs. gen 2** (the multi-session blob). Note "did not localize" in the table.
   - **False-locks ~25°/3 ft off** (reaches `.normal` but visibly wrong — ghost mesh floats off the room): `settle` fires with a pose way off the tape. **Force-record anyway** (confirm alignment, short pass over surfaces the ghost covers) to capture `[LocDiag ICP] initialRMS=…` — this should be a **gross** residual / large `correction rot`, cleanly separated from gen 2's. This is the reject end of the threshold.
7. **Retry the failure as many times as you want (manual reset).** Each rescan that *does* save bumps the relocalization target forward, so to re-test gen-3-against-gen-2: in the location detail, **delete the most-recent scan** to roll the target back to the gen-2 map, then rescan again. (Recreating gen 2 from scratch works too — same effect.) Repeat to see whether the failure is consistent and to get a few residual samples for the threshold.
8. **4D change trials (proves the gate must use *inlier* RMS, not global RMS).** From a known-good gen-2 baseline:
   - **Move furniture, then rescan.** Shift a couch/table/boxes, relocalize, record. Expect: ICP `initRMS` stays **small** (walls/floor still align) while the **inlier % drops** (the moved geometry no longer matches). This is the signature of *content change* — and it must **pass** the gate, with the missing inlier % being the 4D change signal.
   - **Move/remove structure, then rescan.** If feasible, change something structural (open/close a partition, move a large shelving wall). Expect inlier % to drop **further**; this is regime 2/3 territory. If inlier % falls below where you'd still trust the lock, that's your candidate **inlier-fraction floor** (→ Phase 2.3 / re-baseline trigger).
   - Contrast both against the gen-3 false-lock from step 6: there the **`initRMS` itself blows up** (or inliers collapse toward zero with no coherent alignment). The discriminator to confirm: *small initRMS + reduced inliers = real change (accept); large initRMS or collapsed inliers = registration failure (reject).*
9. **Observe a snap (any generation).** During relocalization, and during recording on a loop closure, watch for `[LocDiag snap] frame correction: Δpos=… Δrot=…`. Each line is how far a frozen `world:.zero` layer fell behind reality at that instant. Easiest to provoke: walk a loop back to a previously-seen area mid-record.
10. **Record the numbers** (the SUMMARY line carries `inliers=N/M (P%)` — `P` is the key 4D discriminator):

   | gen / trial | features | settle pos / yaw | initRMS | finalRMS | correction trans/rot | inliers % | snap Δpos/Δrot |
   |-------------|----------|------------------|--------:|---------:|----------------------|----------:|----------------|
   | 2 (tolerable) |          |                  |         |          |                      |           |                |
   | 2 + furniture moved |    |                  |         |          |                      |           |                |
   | 2 + structure moved |    |                  |         |          |                      |           |                |
   | 3 (never localized) |    | — (no settle) | — | — | — | — |                |
   | 3 (false-lock, forced) |  |                  |         |          |                      |           |                |

   **Exit when:** (a) gen-3 `map load features` is clearly inflated vs. gen 2 → blob growth; (b) gen 2 settles with the offset while gen 3 either never settles or settles grossly off the tape → compounding ε / relocalization failure; (c) the false-lock ICP `initialRMS`/`correction` is cleanly separable from gen 2's → sets the Phase-2.3 inlier-RMS threshold; (d) the change trials show **small initRMS + reduced inlier %** (real change, must accept) cleanly separable from the false-lock's **large initRMS / collapsed inliers** (must reject) → confirms the inlier-RMS-not-global gate and sets the **inlier-fraction floor**; (e) snaps are non-zero → baked layers lag. Drop the thresholds into Open Decisions. Note: a deeper chain (gen 4, 5…) only becomes testable *after* Phase 1 stops the compounding — at that point re-run this and confirm `features` and settle pose **stop** growing.

#### What the numbers feed (measurement → downstream decision)
The Phase-0 values aren't just proof — they're inputs the later phases consume. Record them (the table above) so they outlive this run.

- **0.1 map stats + settle pose → Phase 1 motivation *and* its exit gate.** The per-gen growth justifies the canonical frame; then it doubles as the regression check — keep gen-2/gen-3 as the *before*, ship Phase 1, re-run the same tape-standpoint procedure, confirm `features`/settle pose **flatten** (this is the "verify via 0.1" in Phase 1's exit).
- **0.2 `initialRMS` contrast (gen-2 tolerable vs. false-lock gross) → the Phase-2.3 threshold.** Accept/reject line sits between them.
- **0.2 `finalRMS` (post-ICP) → the 2.1-vs-2.2 split.** Gross `initialRMS` that refines to a small `finalRMS` = recoverable → Phase 2.1 *refines* it. `finalRMS` that stays large = too far gone → Phase 2.2 must *reject* outright. This is what decides which gate catches a given failure.
- **0.2 `correction trans/rot` magnitude → ICP tuning for 2.1.** If false-locks land ~3 ft off, the probe's `maxCorrDist = 0.30 m` ([LocalizationDiag.swift](../wisescan-ios/LocalizationDiag.swift)) is too small to even find correspondences at that offset — meaning ICP can *gate* a gross failure but can't *refine* it. The recorded magnitudes set the real Phase-2 search radius / iteration budget.
- **0.2 iters/wall-clock → the "on-device vs. offloaded ICP" open decision.** Fast on-device → keep it on-device (tentative call); slow on full mesh → argues for offload.
- **0.3 snap magnitude + frequency → Phase 3 justification, exit gate ("verify via 0.3"), and 3a-vs-3b prioritization** (big/frequent snaps → do it early).

**Two failure modes route to two different gates** (de-risks Phase 2): *"never localizes"* (never reaches `.normal`) is already caught by ARKit's tracking state + the existing relocalization-timeout UX — the ICP gate doesn't handle it. The ICP residual gate exists specifically for the *false-lock* (ARKit says `.normal` but it's wrong). If a device only ever produces "never localizes" and never a clean false-lock, you won't get a gross `initialRMS` sample — that's fine: set the threshold a margin above gen-2's tolerable residual and let the never-localize path stay on the tracking-state gate.

**Threshold caveat:** don't freeze it from one space / one device — capture across 2–3 spaces and both shipping device classes first, or it over-fits.

### Phase 1 — Canonical frame — ~~*highest leverage*~~ *demoted to a Phase-4 prerequisite (0.4 closed 2026-06-15)*
> **Reframed 2026-06-12; confirmed 2026-06-15.** The "stop the compounding" justification is **disproven** — the gen-3 catastrophic failure was a save bug (now fixed), not drift. **0.4 measured ε across gen 3/4/5 and found it bounded** (settle pose stable to ~1.5 cm, ICP correction decreasing). So this phase is **not a localization fix** — it is wanted only for **Phase 4 (3D-stitch durability)** and the **4D same-frame-every-visit** goal, and should be scheduled *with Phase 4*, not urgently. The one residual to watch is map-size growth (0.4 sub-question), which is a separate longevity concern, not a frame-stability one.
- [ ] **1.1** Add `canonicalScanId` to `ScanLocation` (default: the scan with the best **structural** coverage — full walls/floor/ceiling — not merely first/largest, since it's the registration reference forever). Backfill existing locations.
- [ ] **1.2** Relocalize against the **canonical** scan's map, not `sortedScans.first` — [LocationDetailView.swift:117](../wisescan-ios/LocationDetailView.swift#L117) & [:140](../wisescan-ios/LocationDetailView.swift#L140).
- [ ] **1.3** Stop re-saving a fresh merged map as the *future relocalization target* on rescan. Store each scan's transform **into the canonical frame** + its colored mesh in canonical coords.
- [ ] **1.4** Rewrite the invariant comment at [CaptureView.swift:133-138](../wisescan-ios/CaptureView.swift#L133-L138) (it depends on "relocalize to latest," which this removes).
- **Exit:** scans 3,4,5… relocalize against the same fixed map; ε stops growing (verify via 0.1). *(2026-06-12: scans 3+ already relocalize after the save fix; this exit's "ε stops growing" is exactly what 0.4 measures pre-emptively — if 0.4 shows ε already bounded without a canonical frame, the localization benefit here is marginal and the real payoff is Phase 4.)*

### Phase 2 — Robust registration (ICP refine + gate) — ⚠️ **DEMOTED TO BACKGROUND REFERENCE (2026-07-09)**
> **Superseded by the plane-based primary approach** (see "★ PRIMARY registration approach" below). Dense mesh-ICP is brittle to lumpy LiDAR mesh and scales terribly with room size; plane-to-plane registration replaces it. **Everything in this Phase-2 section is kept for its findings and as a fallback engine — but it is no longer the path.** The device-validated bake history (sign correct, gates work, `horizMin` eigen-split, best-pick) all still informs the plane fit (same observability math), which is why it's retained in full.
> **[Historical] Promoted to next 2026-06-15.** 0.4 showed relocalization registration error is **approach-dependent (~25 cm for a different standpoint)** — not a gross false-lock, but a systematic per-visit offset that makes 4D meshes land ~25 cm apart. **2.1 (ICP-refine the relocalized pose against the canonical/ghost mesh before save) is the fix**, and it's the highest-value localization work now. **Two precursors before/alongside 2.1:** (i) upgrade the 0.2 ICP probe — widen `maxCorrDist` to ≥0.5–1.0 m, raise the iter cap, coarse-to-fine — so it converges at decimeter offsets and can double as the refine engine; (ii) handle the architectural finding below — live mesh only exists *after* recording starts, so 2.1 needs mesh enabled during the alignment phase (or a brief pre-roll) to have a cloud to ICP against before the gate.

Depends on 0.2. Registers to the **stable scaffold**, not all geometry (see "Scene change vs. registration").

**Precursors LANDED (2026-06-15, still log-only, behind `perfDiagnostics`) — re-measure pending on device:**
- **(i) ICP probe upgraded to a refine engine.** `LocalizationDiag.runICPResidualLog` is now a thin logging wrapper over a pure, reusable `refine(source:targetPts:targetNrm:)` that **returns the recovered rigid transform** (the eventual 2.1 bake input) plus the report. It runs **coarse-to-fine** (`icpCorrSchedule = [1.0, 0.5, 0.25, 0.12] m`, `icpItersPerLevel = 12`), rebuilding the NN grid with `cellSize = maxCorrDist` per level — the old single 0.20 m grid could only ever match within ~0.34 m regardless of `maxCorrDist`, which (with the 14-iter cap) is why it stalled unconverged at the 25 cm offset. `ICPReport` gained `converged` (finest level hit the step early-out vs. ran out of iters) and `transform`; both the standalone `[LocDiag ICP]` line and the `[LocDiag SUMMARY]` line now print `converged=yes/NO` and the inlier %.
- **(ii) Live mesh during alignment.** When `perfDiagnostics` is on AND a world map is loaded, `.mesh` reconstruction is enabled *during the pre-record alignment phase* (normally off until record-start) at both config sites (`makeUIView` + the ghost-data reload path in `updateUIView`). The mesh-anchor handlers (`didAdd`/`didUpdate`) now feed the ICP **pre-record** (gated `hasWorldMap && hasSeenRelocalizing && !isRecording`), and the one-shot probe is **armed at map load** (`locDiagBeginRun`) instead of record-start so the alignment-time measurement survives into recording instead of being clobbered by a record-time re-run (it still falls back to firing during recording if alignment never accumulated enough mesh). Net: the `[LocDiag ICP]`/SUMMARY numbers now reflect the offset **at the moment 2.1 will bake**, and should now read `converged=yes` at decimeter offsets.
- **⚠️ Perf regression found & fixed (2026-06-15, gen-8).** The first coarse-to-fine cut tied the NN-grid `cellSize` to `maxCorrDist` — at the 1.0 m coarse level that made the ±1-cell search a **27 m³** volume (vs. the original 0.20 m grid's ~0.6 m³), and the target cloud was uncapped. On gen-8's high-coverage / large-extent map (`extent=(44.9,54.7,142.8)`) the coarse level stalled for seconds-to-minutes: the SUMMARY printed `ICP: pending` with **no `[LocDiag ICP]` line** (the probe never returned). Worse, that pegged a core *during the session* — gen-8 showed `corrections maxΔrot=94.6°`, the saved preview landed ~2 m / >15° off the other generations, and the **early-coverage region was split off** (a large mid-scan frame correction: geometry before the snap stayed in the old frame). So the "log-only" probe was **not behaviorally inert** — its compute destabilized the very scan it measured. Fix: **cap the target surfels (`icpMaxTarget=4000`), decouple cell size from the gate** (fixed `icpGridCell=0.5 m`; `nearest` widens its cell ring to cover `maxCorrDist`, the `bestD=maxDist²` filter still enforces the exact gate). Back to ~tens of ms. **Lesson for 2.1:** keep the refine bounded and fast — it runs adjacent to live tracking.
- **Re-measure RESULTS (2026-06-15, post-fixes) — ε is ~5–7 cm for a typical different approach, well-constrained.** Three converged different-approach ("off-fixed start/end") samples after the perf + localized-target fixes: `trans=7.2cm` (finalRMS 33mm), `5.4cm` (23mm), and the decisive one `6.3cm rot=0.73° wall=72% horizObs=2.93 finalRMS=24mm converged=yes` on a clean settle (3 corrections). The probe now reports **`wall%`** (inliers on vertical surfaces) and **`horizObs`** = `(Σnx²+Σnz²)/Σny²` — the conditioning of the horizontal-translation DOF (ARKit world Y up). This was added because point-to-plane against **floor/ceiling is degenerate for X/Z+yaw** (the offset that matters), so a level-light/floor-heavy sweep could *under-report* ε. The `horizObs=2.93 / wall=72%` run was deliberately swept with little up/down and **still gave 6.3 cm** → the small offsets are **real, not a floor-domination artifact**. **gen-7's ~25 cm now looks like an outlier** (farther standpoint, measured pre-fix, no obs metric, never converged), not the norm. **Takeaway:** typical per-approach ε ≈ 5–7 cm, well-constrained and cleanly recoverable → straightforward for 2.1 to bake out. Only trust `trans` from runs with healthy `horizObs`/`wall%`; (limitation: `horizObs` lumps X+Z, so a corridor can read OK while under-constraining the along-corridor axis — split into the 2×2 horizontal eigen-analysis if that case arises).
- **Re-measure protocol:** re-run the gen-6/7 different-approach scans (HOWTO step 6, different standpoints) and confirm (a) `converged=yes` at the ~25 cm offsets, (b) a real ε distribution with a stable `finalRMS` floor, (c) the recovered `transform` sign matches the manual-ghost-nudge convention (the slider transform that moves the ghost onto reality == `inverse(refine live→ghost)`) before wiring the actual `setWorldOrigin` bake. Open: DOF constraint (full 6-DOF vs. gravity-locked yaw+translation); scaffold-restriction source (geometric large-plane now, since the ghost OBJ carries no class labels, vs. persisting classification on canonical save).

**2.1 bake WIRED (2026-06-15, behind `perfDiagnostics`, pending device validation).** The alignment-phase refine, if it passes a trust gate (`LocalizationDiag.BakeGate`: converged, **≥400 correspondences**, `horizObs` ≥0.5, **`finalRMS` ≤60 mm**, `trans` ≤0.5 m, rot ≤15°, plus a 15% inlier-fraction *sanity* floor), is converted to a **gravity-locked correction** (`bakeTransform` = `gravityLocked(refine.transform.inverse)` — yaw + full translation, pitch/roll dropped) and stashed as `Coordinator.pendingICPBake`. At record-start `updateUIView` calls `session.setWorldOrigin(pendingICPBake)` **before** the manual ghost nudge (reusing the proven manual-bake path), logs `[LocDiag BAKE]`, records `bakedTransM/bakedYawDeg` in the summary, and **re-arms the ICP** so it re-measures POST-bake during recording. Decisions locked: **DOF = gravity-locked yaw+translation**; **rollout = `perfDiagnostics`-gated dev-only first**; **untrusted refine = silent fallback to the raw relocalized frame** (no bake, no UX — the reject/reprompt gate is 2.2). Stale corrections cleared at map reload. **Validation criterion:** the `[LocDiag SUMMARY]` `bake:` segment shows what was baked and its `ICP:` segment shows the POST-bake residual — a correct bake collapses the residual `trans` toward ~0. A `[LocDiag BAKE OK]`/`[LocDiag BAKE WARN]` line auto-flags this (WARN if residual > 60% of the baked magnitude → inverted sign or bad lock). **Pre-record firing fix (2026-06-17):** the alignment window rarely accumulates the 4000-vert one-shot budget before the user records, so early bakes never had a correction ready (every ICP we saw was the *recording*-phase fire). The pre-record refine now fires at a **lower 2000-vert threshold** and **re-runs throttled (~2 s)** during alignment so a fresh correction is ready whenever the user records; recording-phase fires stay one-shot at 4000. (User should also sweep some wall during the "aligning" step.) **On-device cue (2026-06-17):** a perfDiagnostics-only green chip ("Auto-aligned · X cm / Y° — ready to record") appears in CaptureView the moment a trusted pre-record correction is locked in (`ScanStore.icpAlignReady`) — its appearance is the "alignment sweep is done, safe to record" signal, so validation no longer needs a tethered console to know a correction is queued. The production/user-facing affordance + reject-reprompt UX stays Phase 2.2.

**✅ BAKE VALIDATED ON DEVICE (2026-06-17) — sign correct, the offset nulls.** First end-to-end run: two pre-record refines (3.4 cm `horizObs=0.85`, then the fresher 5.0 cm `horizObs=2.83`) → chip shown → user waited for green → recorded → `[LocDiag BAKE] trans=5.0cm yaw=0.19°` → genuine post-bake re-measure `[LocDiag BAKE OK] residual trans=2.0cm vs baked 5.0cm`. Residual collapsed toward the noise floor (an inverted sign would have ~doubled it to ~10 cm), so **`bakeTransform = gravityLocked(refine.transform.inverse)` is the correct direction**. The re-run mechanism correctly baked the *latest/best-constrained* of the queued corrections. **Race fixed same session:** a pre-record refine still in flight at record-start completed during recording and was misclassified as post-bake → spurious `BAKE WARN` (its 5.6 cm was a *pre*-bake offset). Now classified by **dispatch-time** state, not completion-time, so only refines dispatched during recording count as post-bake. **Remaining to graduate to default:** confirm the residual collapse holds across ≥2–3 spaces / both device classes (incl. the wider ~25 cm offsets), then split the bake off `perfDiagnostics`. **Trust gate keyed on absolute corr count + tight finalRMS + horizObs + convergence, NOT inlier fraction** — a farther-standpoint scan (34% inliers but 842 corr, horizObs 2.85, converged, 30.6 mm, 17.7 cm) was a good fit the old 60% fraction floor wrongly rejected; low fraction = low overlap/scene change (still registerable), which is the 4D *change metric* (2.2/2.3), not bake trust. **Still TODO before graduating to default:** confirm the residual collapse on device across ≥2–3 spaces/both device classes; then split the bake out from `perfDiagnostics`; scaffold up-weighting (vs. the current `horizObs` gate) still optional.

**Small-space behaviour + verdict hardening (2026-06-17).** A small-office gen-2 run exposed two things: (1) the post-bake verdict could fire a false `BAKE OK` when the baked offset was below the residual-noise floor (baked 2.7 cm → "residual 4.9 cm" squeaked under the old 5 cm absolute floor while being *larger* than baked). Fixed: three verdicts now — **INCONCLUSIVE** (baked < ~6 cm noise floor, can't confirm a collapse), **OK** (residual < 0.6×baked), **WARN** (didn't shrink). (2) ε is inherently *tiny* in cramped spaces (you can't approach far in a closet), so the bake there is low-value **and** low-risk. Added a **min-offset skip** (`BakeGate.minTransM = 3 cm`, applied at the call site so it logs `[LocDiag BAKE SKIP] … already aligned` distinctly from untrusted): a trusted-but-sub-noise correction leaves the frame untouched and shows no chip → "no chip = already aligned". Insight: **the bake's value scales with space size** (big rooms → big approach-dependent ε → where it earns its keep); small spaces are the *graceful-degradation* suite, not the efficacy suite. The office ICP was actually well-conditioned (`horizObs` 1.95–4.22) — its only real failure was rough tracking (19.8 s settle, `maxΔpos=98cm`), an ARKit/VIO limit, not a bake bug. **2×2 horizontal eigen-split BUILT (2026-06-17, as a gate).** A hallway scan confirmed the corridor failure but *upstream* — ARKit relocalization never reached `.normal` (`map=none`, 7.7 m loop-closure snaps from corridor self-similarity), so no ICP fit ran and tracking-state caught it, not `horizObs`. The eigen-split was built anyway on first principles: lumped `horizObs=(Σnx²+Σnz²)/Σny²` is *provably* foolable by a corridor (abundant cross-corridor side-wall `Σnx²` masks the ~0 along-corridor `Σnz²` → reads healthy), so a corridor that *does* relocalize would bake a confident-but-wrong along-axis correction (worse than no bake). Now `refine` also accumulates `Σnx·nz` and reports `horizMinObservability` = smaller eigenvalue of the 2×2 horizontal block `[[Σnx²,Σnxnz],[Σnxnz,Σnz²]]` / `Σny²` (the weakest-constrained horizontal *direction*, axis-agnostic). `bakeTransform` now also gates on `horizMinObservability ≥ 0.5` (**tuned 0.2→0.5 on 2026-06-22** from an elongated repeating-desk room — see below). Logged as `horizMin=` on the `[LocDiag ICP]`/SUMMARY lines. It's a **gate, not a fix**: refuses to bake an along-axis-ambiguous corridor fit; can't recover the along-axis offset (nothing geometric can in a corridor). **The hallway also exposed a separate UX gap (deferred):** the rescan relocalization prompt ([CaptureView.swift:237](../wisescan-ios/CaptureView.swift#L237)) hangs forever on "move camera to relocalize" with no timeout/escape — a relocalization-timeout failure UX (+ optional mid-scan big-snap "scan unreliable" guard) is wanted, plus the understanding that the 7 m jumps are false *loop-closure*, not VIO drift (self-similar geometry is a fundamental ARKit limit; the real fix for feature-poor spaces is physical fiducials). **Next efficacy gap:** a deliberately wide offset (~25 cm) in a well-tracked space to confirm a big offset nulls.

**✅ WIDE-OFFSET VALIDATED + record-timing gate (2026-06-22).** Wide-offset run: baked **14.5 cm** (well-constrained — `horizMin=0.96`, 99% inliers, converged) → post-bake **6.0 cm** `BAKE OK`. With the earlier 5→2 cm clean run, the bake is validated across the offset range (sign + collapse). The 6 cm residual (vs ~2 cm) was session roughness (`maxΔrot=93.86°`) + room floor, not a bake deficiency — 14.5→6 cm is the 4D win. **Timing race found & gated:** an earlier ideal run (`trans=16.5cm`, 98%, `horizMin=0.81`, converged) did NOT bake — the refine completed just *after* record-start (settle was slow, 16.9 s; user recorded at "ghost visible," before the green chip), so the straggler guard correctly dropped it (`bake: none`, `ICP: pending`). The fix: the **ghost mesh appearing ≠ correction ready**, so (1) a perfDiag **"📐 Aligning — wait for green"** cue shows whenever relocalized but the correction isn't locked (absence of green now reads as "still working"), and (2) **`toggleRecording` auto-waits** up to 8 s for `icpAlignReady` before starting (shows "Finalizing alignment…"), then bakes — times out → record raw so feature-poor spaces aren't blocked. Both perfDiag-only; removes the "recorded a hair early → no bake" miss. **Remaining before graduating off `perfDiagnostics`:** a couple more clean confirmations / both device classes, then split the bake (and this record gate + the chip) out of `perfDiagnostics` into the production path with the user-facing affordance + the relocalization-failure/timeout UX (#3).

**✅ EIGEN-SPLIT VALIDATED + `minHorizMin` tuned 0.2→0.5 (2026-06-22, elongated repeating-desk room — Scan B).** This room relocalized fine (3.7 s settle, clean) but has along-length ambiguity (repeating desks) — the exact middle regime the eigen-split targets and that the hallway (failed upstream) and square rooms (always high) couldn't expose. `horizMin` swung **0.02 / 0.31 / 0.17 / 0.78** across refines while lumped `horizObs` stayed deceptively healthy (0.94–1.98) — confirming (a) the lumped metric IS fooled by elongated geometry and (b) `horizMin` discriminates it. The 0.02 refine was correctly gated out; but a **`horizMin=0.31` fit (lumped 1.98) baked 7.5 cm → post-bake 5.6 cm `BAKE WARN`** (didn't null) — a true-positive catch proving 0.2 was too low. All clean OKs sat ≥0.61 with a gap at 0.41–0.61, so the floor moved to **0.5**: this room now mostly *refuses* to bake (correct — its along-axis registration is unreliable; the user confirmed its scans are visibly offset in the preview list, unlike other rooms), baking only when a well-constrained cross-structure view (`horizMin` 0.78) is in hand. The bake can't *fix* along-axis ambiguity (nothing geometric can); refusing + falling back to raw is the right behavior. **Still open (Scan A):** the residual-floor question — is the recurring ~6 cm null the 8 cm target voxel or session roughness? A clean simple room with an offset sweep decides whether to tighten `icpTargetVoxel` (→ tighter nulls + a lower INCONCLUSIVE floor).

**✅ Residual-floor RESOLVED + the real limiter is tracking roughness (2026-06-23, several rooms).** Across rooms the post-bake residual tracks **session cleanliness, not offset magnitude or a fixed voxel floor**: a clean empty room nulled 4.3→**1.6 cm** with **finalRMS 8–20 mm** (far below any voxel-imposed floor); rough rooms WARNed/loosened (14.5→6, 17.3→11.5 cm, with 50+ corrections / 90° snaps). A fixed 8 cm voxel would floor *every* run identically — instead the floor moves with roughness. **Conclusion: leave `icpTargetVoxel=0.08`; the ~6 cm residuals were roughness, not the voxel.** 
- **Key product insight — feature *distribution* > feature *amount*, and rough tracking is the COMMON case.** A "normal" room with blank wall-centers (whiteboard / large painted walls) and features only near corners tracks *worse* than a starkly empty but uniformly-textured room: VIO needs continuous frame-to-frame features, and panning across a blank wall-center is a **VIO desert** → drift + corrections (one such room: 27.6 s settle, 51 corrections, 90° snap). Feature deserts are everywhere in real offices/homes, so **rough tracking is normal, not an edge case.** 
- **Expectation reset:** the ~2 cm clean null is the *feature-dense best case*. In typical rooms VIO roughness bounds the achievable residual, so the realistic bake win is **~15–25 cm per-visit offset → ~6–11 cm** (a 2–3× improvement), not pinpoint. That's a tracking floor, not a bake deficiency.
- **Also: wide offset ⟺ rough tracking are coupled** — a wide offset means a very different standpoint → less baseline overlap → harder/slower/jumpier relocalization. So the cases where the bake matters most are the roughest. Don't keep room-hunting for a clean wide-offset null; the bake is validated (sign correct, reduces offset every run, gates reject garbage, WARN flags marginal cases) and the limiter is upstream VIO.

### ⚠️ DEVICE-TEST COURSE CORRECTION (2026-06-25) — read this before the build list below
Items 1 + 2 were built (2026-06-23) and validated on device. The data **redirected the priorities** — read this first; the numbered list below is annotated but partly superseded.

- **The saved mesh is IMMUNE to mid-scan camera snaps — item 2's *single-snap* premise is invalid.** `exportMeshOBJ` (the save path, [CaptureView+Recording.swift:168](../wisescan-ios/CaptureView+Recording.swift#L168)) rebuilds the OBJ **at save time** from `currentFrame.anchors`, each vertex = `meshAnchor.transform * local` using the anchor's **current** transform ([ARCoverageView.swift:2076-2090](../wisescan-ios/ARCoverageView.swift#L2076-L2090)). ARKit re-pins *every* anchor consistently on a loop-closure/relocalization correction, so the export captures them all coherently in the corrected frame → **no split**. Confirmed on device: a fresh scan with a **10.9 cm normal→normal snap AND a 2.4 m degraded bounce** produced a **clean mesh** (no visible defect at ≥11 cm scale). So "single mid-scan snap → saved mesh split" is **false for this pipeline** ([[saved-mesh-repins-immune-to-snaps]]; the gen-8 split was the pathological perf-pegging-probe bug — [[log-only-probe-not-behaviorally-inert]] — not a normal snap). → **A single snap no longer warns. Prong-2 (recovery-episode detector) REJECTED** — the 2.4 m bounce was benign, so it would false-alarm.
- **BUT item 2 was RE-PURPOSED as a snap-STORM detector (un-shelved 2026-06-25) — it catches a collapse the VIO guard misses.** Second session, the self-similar repeating-desk room: the camera pose **oscillated `71→22→71→22 cm`, 42 times, under `.normal` tracking** (ARKit flip-flopping between two identical-looking desks — false loop-closure, the same fundamental limit as the hallway). The VIO guard never tripped (it keys on degraded states; tracking stayed nominal). So `TrackingStabilityMonitor` now flags only on a **storm** (`≥stormThreshold=4` non-physical jumps within `stormWindow=3 s`), not any single jump — logs `[TrackStab] SNAP STORM …`, sets `ScanStore.trackingUnreliable`, banner "⚠️ Tracking unstable — N sudden jumps; relocalization may be failing" (perfDiag-gated). This is a genuine "broken-but-reports-normal" signal nothing else had.
- **What snaps / rough tracking actually hurt** (NOT mesh integrity): (a) **registration / the bake** — item 1's domain; (b) **live viz** (the `world:.zero` baked overlays — Phase 3); (c) **colorization coverage**. 
- **Item 1 (the bake) UNDER-CORRECTS in rough/elongated rooms.** A rescan baked **3.5 cm** but the post-bake ICP showed **~21 cm remaining** (`horizMin 1.01`, converged — trustworthy). Walk-through: only **one** pre-record refine passed the `horizMin≥0.5` gate (3.5 cm @ 0.88); the big-offset fits (25.7 cm @ 0.19, 11.2 cm @ 0.46) were correctly gated as along-axis-ambiguous, so they never entered the buffer. The accurate well-conditioned big-offset fit (21 cm @ 1.01) only materialized **post-record** — too late to bake. **No selection policy among pre-record candidates fixes this** (the best-pick from item 1 never even engaged — only 1 candidate), because the accurate fit didn't exist at record-time. The bake/green-chip fires on the **first qualifying well-conditioned fit, which can be an early UNDER-measurement before coverage is complete.** → **Fix (design): a STABILITY/AGREEMENT gate** — withhold the green chip + bake until the offset estimate **converges** across ≥2 well-conditioned refines that agree (forcing the user to keep sweeping until the accurate fit lands pre-record). Needs the **full refine history** (the disagreement was between *gated* and *accepted* fits, invisible to the trusted-only buffer) and a stability threshold to be **tuned from 2–3 more rescans before building** (one data point = over-fit risk).
- **Colorization gap on featureless walls (separate subsystem; new issue).** A rough rescan left ~half a featureless wall **unpainted** (voxels didn't project), bad enough to **block visual alignment eval**. It's a VIO/coverage desert (the wall under-samples), **not** caused by the 21 cm registration offset (colorization happens within the rescan's *own* live frame). Candidate for its own track. Note: the **ICP residual already quantifies alignment** (the 21 cm), so colorization isn't required to measure under-correction — only for a visual gut-check (use geometry/wireframe overlay instead).
- **Second session (2026-06-25) — thermal/perf confound found + fixed; item-1 best-pick validated on a small offset:**
  - **Item-1 best-pick WORKS (first real engagement).** A rescan with wildly noisy refines (`3.4/5.3/11/16/28/181 cm`) correctly locked onto the well-conditioned `3.4 cm @ horizMin 2.71 @ finalRMS 20.5 mm` fit and **held it across "best of 2/3/4"** vs. all the garbage → baked 3.4 cm → **post-bake residual 1.9 cm** (a clean null on a small short-axis offset). The quality score does its job.
  - **"Got worse and worse / never locked" breakdown = self-similar room (primary) + map growth + thermal/perf load (compounders).** The clean `71/22` oscillation is the geometry (false loop-closure). The map grew `433k → 551k faces` per rescan (relocalization target getting bigger/more ambiguous each generation — the 0.4 longevity concern). And the perfDiag ICP was **re-parsing the entire ghost mesh every ~2 s** — a real sustained-compute/heat load that throttles VIO into the breakdown it measures (same class as [[log-only-probe-not-behaviorally-inert]]).
  - **FIX LANDED — cache the ghost surfels once.** `LocalizationDiag.buildGhostSurfels(from:)` parses + builds the surfel cloud **once** at ghost-load (background queue → delegate-queue store as `Coordinator.locDiagGhostSurfels`); `runICPResidualLog` now takes the pre-built `targetPts/targetNrm` instead of re-parsing the OBJ each call. Removes the per-2 s re-parse (the thermal confound) and is a prerequisite for graduating the bake to production (can't re-parse 100k–700k faces every 2 s in prod).
  - **Even well-conditioned fits give garbage during the breakdown** (`33.6 cm @ horizMin 1.66`, `59 cm` post-bake) → re-validates the **stability/agreement gate** direction (refuse to bake when fits *disagree*), but convergence characterization is contaminated by the breakdown → need a **clean session in a non-self-similar room on a cooled device** before tuning it.
- **Session 2026-06-29/30 — drift confirmed, double-load fixed, work committed:**
  - **Map drift is real and now measurable.** Added a robust-extent metric to `logMapStats` (`dist-from-median p50/p95/p99/max`). The "feature-rich room" map read `p99=8.1 m` but `max=78.8 m` on a 112 m bbox → ~1% (~200) of its feature points are a **drift trail** (a baked-in tracking excursion), not room size. ARKit relocalizes against the full set incl. those outliers (the ICP is shielded — `localizedTarget` crops to the live neighbourhood).
  - **The bake is bounded by relocalization quality + map state, not by the bake logic.** Four rooms, four non-clean results, four *different* causes: elongated → non-rigid map distortion (16→21 cm WARN); big room → drift/accumulation (17.6→12.4 WARN); clean/empty room → rough 43 s settle + 412 cm snap (9→27 WARN, partly double-load-induced). **Common thread: a single rigid bake can only be as good as the relocalization + map it refines, and real rooms relocalize roughly / carry drift.** This re-elevates **Phase 1 (canonical frame — stop re-merging a growing map)** + **map hygiene** as the real levers, possibly above further bake tuning. (Bake still validated *in principle* — session-1 clean OKs.)
  - **Committed** (branch `sync-coordinate-space`): `932fb32` fix double-load · `ec2070b` feat 2.1 diagnostics+bake (all perfDiag) · `3961553` docs. Working tree clean.

- **Session 2026-07-20 — rescan validated end-to-end; WINDOW-WALL BISTABILITY found; solver v2 (trim rescue) built:**
  - **The rescan ran and the whole mechanical chain held:** single `[LocDiag ε] map load` (double-load fix confirmed on a real record run) · map inherited 9,243 features → saved 17,782/39 anchors (grew, no collapse) · deferred RoomBuilder post-save (8 surfaces/5 objects) · unprocessed-scan hard gate fired · `postprocess Scan 2: steps=registration+proxy+colorize` · refusal path wrote the sidecar + saved raw · previews align.
  - **The fit itself: `GATE REFUSED` — correctly.** `trans=14.2cm yaw=0.50° finalRMS=69.3mm walls=5 floors=1 weakFrac=0.961 converged` — failed only `maxFinalRMS` (50 mm). Root cause (user field knowledge + proxy dims): **the all-glass wall is BISTABLE** — LiDAR/vision sometimes catches the *concrete wall beyond the glass*, so RoomPlan seats that wall ~20 cm out between sessions (scan 1 floor 4.5×4.2 vs scan 2 4.5×4.4, heights 3.6→3.7, + a 0.2 m sliver wall). A rigid fit can't reconcile two different room *shapes*: the conflict smears across all walls (RMS 69 mm) and drags trans to 14.2 cm, while the true seat ε (measured live at prescan) was 5.6–7.3 cm. Refusing protected a raw frame that was already good — which is why the previews still line up. **New noise source named: RoomPlan per-session SHAPE reproducibility, distinct from seating ε.**
  - **Design insight — trimming is ambiguous without physics:** dropping the bistable wall OR its honest opposite both refit self-consistently (two rigid hypotheses ~20 cm apart), so gate-passing alone can't pick a side. Tie-break = **smallest translation** (seat ε is cm-scale, surface bistability dm-scale — the small-trans hypothesis is the consensus compatible with a plausible seat, whichever wall lied).
  - **Solver v2 built (BUILD GREEN, not yet device-run):** per-pair residual table logged on every save-path fit (`[PlaneReg]   fit wall[i] W×H → tgt[j]: centerOff=±cm rms=mm` + unmatched leftovers both sides) · `PlaneRegistration.trimRescue` leave-one-out over matched wall pairs, smallest-trans passing candidate, **save/postprocess path only** (live auto-align unchanged — trimming half-grown live planes could rescue the wrong hypothesis) · sidecar `version` 1→2 + `trimmedSourceWall` field; ScanPostprocessor re-queues **refused-only** stale sidecars (applied = final, mesh already transformed) · proxy force-rebuilds when a late-applied registration moves mesh.obj out from under a version-current header.
  - Committed same day (pre-v2): `5531b18` cross-section wireframe · `21f2b08` DECISION 3 postprocess stage · `d90e3f2` this doc.
  - **Retest same day: v2 PASSED on device.** Version gate re-queued scan 2 (`steps=registration` alone — proxy v4-current, colorize done). Pair table confirmed the bistability **as a pair-split, not a lone outlier**: the two 4.5 m walls (the pair separated by the grown axis) read −10.9/−10.6 cm with the conflict smeared between them, the 4.4 m pair sat at −2.7 cm, floor exact; the 0.2 m sliver matched the SAME target as wall[4] (many-to-one) at −10.5 cm — it's a fragment of the same physical (window) wall. TRIM RESCUE dropped wall[4]; in the refit, wall[2] snapped −10.9 → −0.7 cm (proving wall[4] was the mover), and **APPLIED trans=7.0 cm (inside the 5.6–7.3 live band) yaw=0.44° finalRMS=26.4 mm weakFrac=0.529 trimmed=wall[4]**. Forced proxy rebuild fired (identical dims table, new frame). This room's true shape-reproducibility floor ≈ 26 mm RMS → the 50 mm gate sits ~2× above it (sane, first real 2.3 datapoint).
  - **Residual nit + v3 candidates:** the sliver (0.74 m², coplanar with the dropped wall) stayed matched at −20.5 cm → ~1 cm axis bias (weight-fraction × 20 cm). v3: **trim by TARGET, not source** (many-to-one ⇒ one physical wall = one target; dropping tgt[3] would have removed wall[4]+sliver together) · min-area floor on room-decoded planes (the live-anchor path already has 1 m²; room path has none).

#### ★ MERGE-BACK to main (opened 2026-07-21) — the branch has run long; land what's solid
This branch-off-a-branch (`plane-registration` ← `sync-coordinate-space`) is +15 ahead / 8 behind `origin/main` (sync-coordinate-space fully contained — no separate merge needed). **Merge `origin/main` INTO the branch** (do NOT rebase again — one rebase already survived; keep the validated history). Everything on the branch is either production-validated (DECISION 3 stage, RoomPlan retain fix, double-load fix, wireframe fix, registration + trim rescue) or perfDiag-gated inert diagnostics — merge all of it; don't cherry-pick (untangling 15 interleaved commits costs more than it saves). Priority order:
1. **★ Privacy × colorize collision — DONE 2026-07-21 (pre-installed, dormant until merge).** Main moved privacy blur capture-time → **export-time** (`6055af5`/`4a3188a`): images + depth on disk are now RAW, person regions saved separately as `masks/frame_XXXXX.png`, blur applied to the *staged* copy at export. **Colorize (postprocess, pre-export) had no privacy code — it relied on capture-time depth-zeroing** (its `depth==0` skip) to exclude people for free; deferred blur silently removed that, so colorize would bake unblurred face/body pixels into `colors.bin` → exported as **PLY vertex colors** (`ScanExportManager` OBJ+colors→PLY), a path export blur never revisits → violates PRIVACY.md's absolute guarantee. Fix restores the invariant in `VertexColorAccumulator.colorizeFromSavedFrames`: in the deferred-blur era (masks/ dir exists) with privacy on (masks non-empty OR `privacy_filter` metadata flag), sample each frame's mask and skip person pixels (±1 mask-px ≈ export's 12 px dilation); **frames with no mask (stencil warm-up) → skip the whole frame** (user decision — top-K redundancy absorbs it). Era discriminator = **masks/ dir existence** (only the deferred-blur capture session creates it), so legacy scans stay on the depth==0 path and privacy-off scans no-op. **Dormant on this branch (no masks/ yet) → zero behavior change pre-merge; activates automatically post-merge.** BUILD GREEN.
2. **Legacy-scan retroactive registration — DONE 2026-07-21 (scoped out by default).** The blast radius (every non-oldest legacy scan on every existing install lighting up "needs postprocess", hard-gating old locations, Process moving already-accepted meshes) is defused: `wantsRegistration` now returns true only for explicit `rescanSpace` scans; legacy (`scanCaseRaw == ""`) scans are gated behind a dev-only "Register Legacy Scans" toggle (`AppConstants.Key.registerLegacyScans`, default OFF) AND excluded when the location is link-adjacent (a legacy adjacent-link is indistinguishable from a rescan → a similar room could false-lock). The toggle is the validation path for retroactive registration when we choose to exercise it. BUILD GREEN.
3. **Textual conflicts — DONE 2026-07-21 (merge commit `47bebf0`).** Only 2 real: `AppConstants.swift` (unioned both sides' new keys — trivial) and `ARCoverageView.swift` record-start config (took main's `makeConfiguration()` — it loads `initialWorldMapURL` to preserve the relocalized frame AND honours our `meshClassifier` toggle via `meshReconstructionMode()` internally AND adds main's video-format pinning [the 30→60fps switch that broke Recon3D SLAM on M2 iPad] → net gain, no loss). One benign delta: record-start now emits an extra `[LocDiag ε] map load` line (logMapStats inside makeConfiguration); the rescan-ENTRY double-load fix is unaffected. Auto-merged `FrameCaptureSession`/`ScanExportManager`/`SettingsView` semantically verified (both sides' additions coexist). `masks/` preservation confirmed: `saveScan` moves the whole raw session dir, so main's masks land in `raw_data/masks/` for the colorize guard to read. **Build green; on-device save→Process smoke still pending.**
4. **Release-please hygiene — DONE.** pbxproj markers intact (2× `MARKETING_VERSION = 0.4.0 /* x-release-please-version */`; origin/main is also 0.4.0 — the "0.8.0" in main's history was the SDK, not the app version). pbxproj changed only on main's side (MWDATMockDevice removal) → merged cleanly, no double-add.
5. **After merge (pending device smoke):** delete `plane-registration` + `sync-coordinate-space` (+ the pre-rebase backup once verified); continue the queue on fresh short branches. Local `main` fast-forwarded to `origin/main` for hygiene 2026-07-21.

### ▶ NEXT SESSION — START HERE (updated 2026-07-23)
> **★ CURRENT HEAD OF QUEUE (2026-07-23): PR #28 MERGED (`4f74a97`) → now on `main`. New stream: ADJACENT-STITCH plane alignment (Phase 4.2 / 4.1).** Design specced below ("★ ADJACENT-STITCH plane alignment"). **First move = prerequisite P1: settle the raw↔canonical seam** — a standalone correctness fix (the merge left stitch edges in the raw frame while geometry went canonical → decimeter seam on any stitch touching a rescanned / legacy location) *and* the foundation the plane refinement sits on. Sequence: **verify** (`placeScans` uses raw `mSrc·inv(mTgt)`; confirm `sourceScan` / `targetScan` are the anchor's *frame-scan*, esp. the Flow-B relocalization target) → **fix** (derived, `T`-composed edge `T_A·(mSrc·inv(mTgt))·T_B⁻¹`, `T` from `registration.json`) → **measure** the residual on real stitched+rescanned data (sizes the compass window; decides if the translation-snap is worth building). P2 (per-scan north is pose-anchored) + P3 (`registration.json` `T` loadable at build time) verify in parallel. Then the **Tier-B covariance blend, yaw-first**. **✅ UPDATE 2026-07-23: P1 verify + derived-`T` fix DONE (sim build green) — see the "✅ P1 VERIFIED + FIXED" status in the design section; next concrete move = MEASURE the residual on a stitched+rescanned capture, then Tier-B.** Prior head (PR #28 pre-merge) retained below for history.
> **★ PRIOR HEAD (2026-07-22): PR #28 OPEN, 5 review rounds folded in (all build-verified).** Merge done, smoke passed, all corners closed, min-area guard shipped (`1104d4b`), and the PR self-review + Copilot rounds are resolved (postprocess off-main SwiftData → snapshot+lock; privacy fail-closed; dev-toggle resets; one canonical-owner comparator; canonicalRoomFrame walls/floor; the colorize `isColored`-only-when-produced fix; the `frameCenters` memo footgun; see the "⏮ PAST" review→fix bullet). **The head is now: one device Process smoke → merge → branch cleanup.** Process smoke = single Process on a rescan + one bulk Process (functional-regression check on the concurrency refactor; the off-main fix is verified statically). Then delete `plane-registration` / `sync-coordinate-space` / pre-rebase backup. Follow-ups, none a blocker: the deferred review items in **FUTURE → "Deferred from PR #28 review"** (loadGhostMeshData main-thread rewrite · currentFramePlanes raw_data fallback · bulkExport/bulkUpload off-main + ARFrame starvation → capture-perf/threading ticket); the visual person-exclusion eyeball; the review's dismissed nits (v3 trim-by-target, etc.). The corner checklist below is retained for history:
> **★ PRE-MERGE VALIDATION CHECKLIST (2026-07-20c) — the DECISION 1→3 chain is device-validated end-to-end (registration APPLIED trans=7.0 cm via trim rescue; see session entry).** Remaining checks, in order:
> 1. ~~Eyeball scans 1+2 in the canonical-frame preview~~ **done** — scan 1/2 dead-on at a fixated corner; solver v2 committed. ~~+ on-device smoke~~ **done** (2026-07-22; see Orientation).
> 2. The untested corners: colorize toggle OFF (structural-only Process) · bulk Process across locations (Latest/All) · legacy-scan retroactive registration · colorize-after-registration fix on a registered scan. *(upload/postprocess gate ✓ 2026-07-22.)*
> 3. Then the deferred build queue: door/opening quads (design settled — aperture = structural quad w/ wall-cell punch-out; leaf = content via mesh) · v3 solver candidates (trim by TARGET; min-area floor on room planes — session entry) · longer-term seeds: persist the prescan live-fit as an independent prior/cross-check; window-coverage / RoomPlan-confidence down-weighting; Phase 2.2 surface-the-refusal UX (console-only today); degradation-ladder rungs (see "Degradation ladder" in the PRIMARY section) when a space binds the wall constraint.
> *(Prior heads, for history:)* **2026-07-20b** — v2 retry of scan 2 (done: TRIM RESCUE → APPLIED 7.0 cm). **2026-07-20a** — rescan validation run (done: mechanical chain ✓, GATE REFUSED correctly). **2026-07-15** — DECISION 3 postprocess refactor (built + validated). **2026-07-09** — plane-based save-time registration is PRIMARY; mesh-ICP bake demoted to background reference (Phase 2). This supersedes the mesh-bake-centric next-steps in items 1/3 below (kept for history/data); the old "decisive bake experiment" is optional — it only *quantifies* the CPU win.

Everything below the older lists is still valid detail/history; the PRIMARY-approach section is the current head of the queue.
1. **THE decisive experiment — bake on a fresh small CLEAN location.** Create a **brand-new** location (never scanned/linked → small, drift-free gen-1 map), cooled device, rescan from a clearly different standpoint (~15–20 cm offset). The double-load fix is now in, so the settle is no longer sabotaged by a double `resetTracking`. Confirm on the (now single) `[LocDiag ε] map load` line that `p99 ≈ max` (no drift trail). **The outcome picks the pivot:**
   - **Clean `BAKE OK` + tight fit cluster** → map drift/accumulation *was* the limiter → pivot to **Phase 1 / map hygiene** (the bake is fine; give it a clean target).
   - **Still WARN on a clean small map** → the single-rigid-offset premise is the issue → build the **item-1 stability/agreement gate** (withhold bake when buffered fits disagree; needs the *full* refine history, not just the trusted buffer; tune the threshold from the convergence sequences).
2. Item-3 **relocalization-timeout UX** (the hallway hang) — ✅ **BUILT + DEVICE-VALIDATED 2026-07-02**; see item 3 in the "Next" list below for details.
3. **Graduation** (see roadmap below) — gated on #1's outcome.
- Parallel track to schedule: **colorization coverage** on featureless walls (separate subsystem; blocks visual alignment eval but not the ICP-measured eval).

#### Rebase reconciliation (2026-07-07) — branch now on main tip, notes for the experiment
`sync-coordinate-space` rebased onto `origin/main` (~45 upstream commits absorbed). Branch delta verified byte-identical for the localization core (`LocalizationDiag` / `TrackingStability` / `ScanStore`); build green. Extra commit `fc2c60f` extracts CaptureView body subviews (compiler type-check budget — no behavior change); `51cdd6f` restores release-please pbxproj markers dropped in a conflict. Backup of pre-rebase state: `backup/sync-coordinate-space-pre-rebase`.

**Upstream interactions to know about when running experiment #1:**
- **⚠️ SpaceAnalyzer's "Analyze" button is tappable mid-rescan-alignment** (gated only on `!isRecording`, [CaptureView.swift:633](../wisescan-ios/CaptureView.swift#L633)). `startAnalysisRoomPlanSession` re-runs the *shared* ARSession with RoomPlan's own configuration ([ARCoverageView.swift:1249](../wisescan-ios/ARCoverageView.swift#L1249)) — mid-settle this would perturb relocalization + the ICP feed. **Protocol: don't tap Analyze during the experiment.** Resolution decided + queued as item 5 in the "Next" list below (disable Analyze during the alignment window; do it with the item-4 graduation surgery, not before).
- Bake ordering preserved: at record-start the ICP `setWorldOrigin` (ARCoverageView ~378) fires **before** `startRoomPlanSession` (~409), so RoomPlan geometry lands in the baked frame. Main's `bf529fe` (snapshot CapturedRoom at Stop) is consistent with this.
- Main's `a6e9432` cold-init record gate + the frame-semantics re-asserts (`8792f6a`/`da06e45`) act at/during recording, not the alignment phase — no impact on the settle or the ε line.
- `toggleRecording` now runs main's gates (stop-menu, link-adjacent block, cold-init) **before** our 8 s alignment-await; coordinator flags the ICP feed reads are now `Atomic<Bool>` (5 read sites converted to `.load(ordering:)`).
- Main `4557ec0` moved `exportMeshOBJ` off the main thread — if it still enumerates live anchors, export now reads them slightly *after* Stop; worth a re-look if the snap-storm detector (memory: saved-mesh-repins) is revisited.
- **Post-rebase device re-check recommended (pre-experiment smoke):** one rescan through the merged record path (cold-init gate + await + bake) and item-3 panel still rendering sanely alongside the new ScanCoach bar / bottom stop menu.

#### Graduation roadmap (2026-06-30) — perfDiag seam refactor DEFERRED by decision
"perfDiag stuff" is **two populations with opposite fates:**
- **(A) Pure diagnostics** — ε / ICP-residual / snap probes, per-run summary, robust-extent, frame-gap logs, **ICP CPU/mem attribution** (`[MemDiag] EVENT ICP-REFINE`/`ICP-SURFELS` + the `icp=N%` cpu-by-thread line, added 2026-07-09). These **stay** perfDiag-gated (cheap, inert) or get **deleted** when the localization investigation closes. They never graduate.
- **(B) Feature code parked behind perfDiag** — the ICP **bake pipeline** (mesh-during-alignment → ICP feed → `setWorldOrigin`), the **record-await**, the **align chips**, the **storm warning**. These graduate (item 4).

**Status:** double-load already production · **storm warning is nearly graduated** (its *detector* already runs in prod; only the *banner* is gated — un-gate one line when ready, useful standalone) · **bake graduation is gated on validation we don't have yet** (a real win on typical rough rooms) + the stability gate + **the CPU-load gate below**.

**⚠️ New graduation gate — the bake pipeline must not add VIO-degrading CPU load (2026-07-09).** The upstream detour that merged CPU/mem diagnostics + deferred RoomPlan to main pinned down that the "rough tracking bounds the bake" limiter (2026-06-23) and the earlier per-2 s re-parse confound ([[log-only-probe-not-behaviorally-inert]]) are the same mechanism: **sustained CPU contention → thread starvation → VIO quality collapse** (not memory pressure, as first suspected). Since the bake pipeline (population B) runs *mesh-during-alignment* + a *re-armed ICP refine* right next to live VIO, it is itself a candidate CPU load. **Before graduating the bake off perfDiag, confirm its compute is cheap enough not to degrade tracking:** read the new `icp=N%` on the cpu-by-thread timeline and the `[MemDiag] EVENT ICP-REFINE cpu=…s (…% of 1 core)` bracket during a rescan alignment — if `icp` is a small slice next to `arkit`/`voxel` and refines are tens of ms, it's safe; if it's a large slice or refines run long, the refine must be throttled/bounded further before shipping. This is a **measurement gate, not new work** — the profiling to answer it is now in (population A). Pure diagnostics, so it does **not** reorder the phases; it just adds one check to item-4 graduation.

**Delimiters today: partial.** `LocalizationDiag` is a self-contained module and its pure engine (`refine` / `bakeTransform` / `BakeGate` / `refineQuality` / `buildGhostSurfels`) is already un-gated (graduates by being called from prod); log lines are tagged. **BUT there is no single feature seam** — the ~9 bake-feature gates (ARCoverageView `65, 236, 434, 962, 1731, 1767` + CaptureView `258, 760` + CaptureView+Recording `21`) key *directly* off `PerfDiag.enabled`, and some are double-duty (`PerfDiag.enabled && worldMap` = "enable mesh for the bake" AND "we're in dev"). So graduating today is **surgery across 9 interleaved sites, not a flag flip.**

**Cheap prep (DEFERRED 2026-06-30 by decision):** introduce one seam — `Coordinator.icpBakeEnabled`, initialized `= PerfDiag.enabled` — and route the 9 feature gates through it, leaving diagnostics on `PerfDiag.enabled`; then graduation = flip the flag (or wire to a real setting). **Deferred because the larger phase plan needs more work first** (Phase 1 / stability gate / validation); do the seam when the bake is actually ready to ship, not before. Logged here so it isn't re-derived next time.

#### ★ PRIMARY registration approach (pivoted 2026-07-09) — plane-based, save-time room-to-room registration
> **Pivot:** the dense mesh-ICP bake (Phase 2 / 2.1 — all the validated-on-device work above) is **demoted to background reference**: keep its findings and code as a fallback idea, but it is no longer the path. **Plane-based registration is now primary.**

**Why pivot.** Dense point-to-plane ICP is (a) **brittle to lumpy LiDAR mesh** — shallow, ambiguous minima on noisy geometry, so it wanders and can fabricate offsets; and (b) **scales terribly with room size** — NN over 10⁵–10⁶ surfels, and all the `localizedTarget`/cap machinery exists only to fight that. Plane-matching is cheap, **robust to lumps** (planes average the noise out), scales with **surface count not face count**, and is **apples-to-apples** with the RoomPlan surfaces we already persist. Cost context that forces the issue: even the **initial** scan (no ghost — just ARWorld + mesh reconstruction + RoomPlan) already strains CPU as rooms grow, and pre-scan hit ~400% core util *with the rest of the system idle*. So the rescan must add as little as possible on top — dense ICP + a full second mesh is the opposite of that.

**The core fit:** a **plane-to-plane** rigid registration of live **walls (vertical planes / horizontal normals)** against the ghost's walls. ~5–20 planes ≪ NN over thousands of surfels → the hot loop disappears. The observability rule + inter-axis weighting below govern the solve; the two big structural decisions are the **mechanism** (save-time vs live) and the **ghost rendering** — both new (2026-07-09), below.

**Observability rule (the core geometry — carries over exactly from `horizMin`):**
- Horizontal translation + yaw are observable iff you have **≥2 walls with linearly-independent normals.** Two *parallel* walls (a bare corridor) = one independent normal → the along-axis floats (unrecoverable by *any* geometric method).
- **It's the normal *direction* + area, not the wall count.** A 3rd parallel wall adds nothing; a wall 5° off-parallel barely helps; a wall at 90° helps fully. So gate on the **eigenvalue** (`horizMinObservability` = smaller eigenvalue of the 2×2 horizontal normal block), never a wall count — a count misfires (3 near-parallel walls read "enough" but aren't).
- **Cubicle row / two sets of parallel walls = recoverable**, because the dividers/cross-walls are the structurally-guaranteed "≥1 non-parallel wall." The offset seen there is a **conditioning** problem (weak axis out-voted by the high-area long walls), not an ambiguity → fixable by weighting.

**Inter-axis (whitening) weighting — the upgrade over 2.1a:** 2.1a is only wall-vs-floor weighting. Go further: **up-weight the under-represented horizontal normal direction** (the sparse cross-walls) so the abundant long side-walls don't dominate the solve and leave the weak axis sloppy. Principled form: whiten by the same 2×2 horizontal block already computed for `horizMin` → drive the block toward isotropic. Existence (≥2 independent normals) gets you *past the gate*; whitening gets you a *tight* fit on the weak axis instead of a merely-present one. **Cap the up-weight** (ceiling, mirroring 2.1a's floor) — leaning on a few divider faces trades long-axis slop for variance; there's a bias/variance knee to tune. Composes with the gate: weighting **expands** the recoverable regime; `horizMin` still **backstops** the truly-degenerate.

**Scope boundary (explicitly NOT engineered for):** periodic repeating structure on the weak axis (repeating pillars / identical cubicles) is beyond saving — it defeats **relocalization itself** (the 71/22 flip-flop, the 7 m hallway loop-closure jumps: ARKit lands in the wrong *period*), and no downstream registration refinement lives below a wrong-period relocalization. That's the physical-fiducial case, full stop.

**Degradation ladder (2026-07-20) — how strong is the wall constraint, and what falls back where.** The engine's true requirement is NOT "4 walls": it's **2 walls with independent normals + a floor** (one good corner) — the 4-wall rectangle is 2× redundant (that redundancy is what trim rescue spends). Below the corner the MATH already degrades gracefully (Tikhonov ⇒ an unobserved DOF resolves to ~0 correction, i.e. inherits relocalization); what's binary today is the GATE (`weakAxisFrac` refuses everything, even the observable DOFs). Design-only for now — nothing on this ladder is scheduled; pull rungs forward when a real space binds the constraint. The floor of the ladder is never "unaligned": it's the raw relocalized frame (ARKit's own ~5–15 cm alignment, visually decent on a good seat — validated 2026-07-20); every rung only *refines* that. All rungs emit the same artifact (rigid raw→canonical + `registration.json`), so fallback engines slot in behind a sidecar `method` field without touching gates/viewer/ghost/2.2-UX.
1. **≥3 matched walls + floor** — full fit + trim-rescue consensus vs one bistable wall. ✅ today (v2).
2. **A corner (2 independent walls) + floor** — fully observable, zero redundancy: a bistable wall is undetectable (consensus needs a 3rd opinion). Missing piece = an *independent estimate*, not more walls: **persist the prescan live auto-align fit** (11 mm RMS, discarded today) as a prior/cross-check — two independent estimates agreeing within cm is consensus that costs no wall. (Small build.)
3. **Parallel walls / one wall + floor (corridor)** — **partial-apply**: correct the observable DOFs (perpendicular offset, yaw — corners give it a lever arm — and height), leave the weak axis at relocalization. Per-eigenvector gating instead of all-or-nothing; the eig2x2 machinery already computes the needed decomposition. Never worse than refusing. (Small build.)
4. **No usable walls, distinct static structure** (machine in open space): (a) content planes — RoomPlan object faces / unclassified ARKit planes, down-weighted + consensus-gated (content is exactly what MOVES between visits — trust design needed); (b) **postprocess mesh ICP — DECISION 3 removed dense ICP's main disqualifier** (live CPU/VIO contention is gone at postprocess), and distinct structure is dense ICP's *well-conditioned* case (its wandering was on lumpy near-planar rooms). Engine exists + device-validated (`LocalizationDiag.refine`); needs mesh-vs-mesh-from-disk plumbing, same trust gates, init at relocalized pose. (Medium build.) ⚠️ Today's BAD-scan state fights this rung: no-roomplan ⇒ "redo scan", conflating "RoomPlan failed on a room" with "space isn't room-shaped" — must soften before rung 4 exists.
5. **Nothing distinct / repeating structure** — fiducials (physical tags, or opportunistic ARKit image anchors on existing signage/posters) or accept raw relocalization. Out of scope by standing decision (above).

**DECISION 1 — Registration mechanism: SAVE-TIME correct-at-export (primary); live bake (fallback).** Prefer computing the correction at **save**, not live:
- Record the rescan in the **raw relocalized frame** (no live `setWorldOrigin`).
- At save, register the **final `RoomBuilder` room** (this rescan, fully built) against the ghost's **persisted final room** (`roomplan_raw.json`) → a plane-to-plane rigid transform → apply it to the exported mesh + world map (or store it as the scan's transform-into-canonical).
- **Wins:** max-quality **apples-to-apples** (both sides complete RoomBuilder output, same fitter); **zero live/VIO-competing cost** (runs in the existing 15–25 s save pipeline where RoomBuilder already executes); **deletes the entire live-bake hot path** — no mesh-during-alignment, no pre-record refine, no green chip, no record-await, no timing races; and it **unifies Phase 2.1 with Phase 1.3** ("store each scan's transform into the canonical frame") into one save-time step.
- **Tradeoffs (accepted as worthwhile):** (i) **no live alignment feedback** — the ghost overlay stays offset by ε during the scan; only the saved result is corrected (fine for rescan-to-recapture; the real thing given up). (ii) **co-framing by hand** — the live bake co-framed mesh+map for free via one origin shift; save-time must apply the transform to both (or store T + apply on load) — this *is* the Phase-1.3/4 data-model work. (iii) **depends on RoomBuilder succeeding at save** → on timeout/failure, fall back to raw/uncorrected. (iv) loses the dense furniture-level content-change metric → structural (wall-level) change instead.
- **Live bake stays the fallback** if live alignment feedback proves necessary: live source = RoomPlan-continuous in prescan (apples-to-apples, gated on post-settle, stop at record-start) or ARKit vertical-plane anchors — and the "drop mesh-during-alignment" prize applies (`sceneReconstruction=.mesh` is on pre-record *only* to feed the ICP, [ARCoverageView.swift:236](../wisescan-ios/ARCoverageView.swift#L236); plane anchors don't need it → turning it off cuts both the ICP loop and the pre-record mesh cost).
- *Note:* the post-bake residual can also be computed at save (final-room↔ghost-room) — but it's **metric-only / non-actionable during recording** (can't re-bake mid-scan → split mesh) and **does not graduate**; save-time it either serves dev validation or feeds the **save-time accept/reject gate (2.2)**, which is the one actionable home.

**DECISION 2 — Ghost RENDERING during rescan: decimate; RoomPlan for alignment only, NOT coverage.** The ghost overlay has two purposes that want different things:
- **(a) alignment validation** (ballpark — is the ghost roughly on reality? if not the user retries from a different standpoint / re-prescans): coarse shape suffices → RoomPlan walls are *great* here (clean, cheap, and it's what we register against).
- **(b) coverage / "painting" guide** (how far to scan to re-cover the original extent in the new time slice): needs the **honest captured extent** — and this is where **RoomPlan misleads**: it **closes spaces and extends walls**, so on a partial capture of a large space or a hallway it fabricates boundaries and would tell the user to paint area the original never covered. RoomPlan is **wrong for the coverage guide.**
- **The full reconstructed mesh REMAINS the primary save artifact** — it's the actual 4D capture / the product. Everything below is an **additional, render-only LOD** derived at save; the decimation + wall-removal touch **only the proxy**, never the saved mesh. Three files persist per scan: (1) **full mesh** (artifact, unchanged), (2) **RoomPlan** (`roomplan.json`/`_raw` — walls + registration target, already written), (3) **decimated wall-subtracted non-wall proxy** (new; rescan overlay only).
- **Decision — COMPOSITE overlay (walls from RoomPlan, non-walls from the decimated proxy), rendered as one ghost.** Don't show two overlays (double-drawn walls read as two ghosts). Instead **subtract the wall faces** and render: **walls → RoomPlan planes (clean), non-walls → decimated proxy (lumpy, as they truly are)**, both in the *same* ghost wireframe style so they read as one object. The rescan loads only the proxy + RoomPlan, never the 10⁵–10⁶-face full mesh — killing the render cost **and** the `cachedGhostMeshData` memory (the large-room strain). Removing wall faces makes the proxy *smaller* than a decimated full mesh (walls are a big fraction of faces → replaced by a few rectangles).
  - **The wall/non-wall split is a shared primitive — build once, use three ways:** rendering (above); **registration** (the plane fit runs on the walls = scaffold); **change detection** (the non-wall set is the 2.2/2.3 content-change signal). This is the 4D "separate frame from content" contract, materialized. "Remove the labeled mesh" isn't cleanup — it's that segmentation.
  - **Segmentation source — CHOSEN: turn on `.meshWithClassification` (2026-07-09).** Recipe: capture per-face labels during recording; at save, **drop wall-labeled faces, decimate the remainder** → non-wall proxy; walls drawn from RoomPlan. True per-face labels beat geometric proximity (they catch walls RoomPlan *dropped*) and also yield floor/ceiling/furniture → upgrades the shared primitive to real **multi-class** (feeds 2.2/2.3 structural-vs-furniture change split, not just rendering).
    - **Cost to weigh (the one place this ADDS load):** `.meshWithClassification` runs a per-face classify pass during *every* scan's recording — the initial-scan CPU already strained on large rooms. On-device/ANE, modest, recording-phase (NOT the rescan-alignment hot path), but real → on the measure-list (`cpu-by-thread`). System net is still down (no dense ICP, no full ghost mesh in rescan memory); this line item is up.
    - **Consistency gotcha:** classifier-"wall" ≠ RoomPlan-"wall" (RoomPlan drops partial/low-texture walls). Remove-by-classifier + draw-by-RoomPlan → a RoomPlan-missed wall is removed-but-not-drawn → **gap**. Reconcile: **keep classifier-wall faces no RoomPlan wall covers in the lumpy proxy** (no gaps; honest — the walls RoomPlan couldn't clean). One save-time proximity test between the two wall sets. Geometric proximity stays the fallback if we don't want the classifier cost.
    - **✅ BENCH HARNESS BUILT (2026-07-09) — measure the classifier cost before committing to it.** Developer-Mode toggle **"Mesh Classifier (bench)"** (`AppConstants.Key.meshClassifier`, default OFF) switches `sceneReconstruction` `.mesh` ↔ `.meshWithClassification` via `ARCoverageView.meshReconstructionMode()` (both config sites: record-start + `makeConfiguration`; capability-guarded). Each run stamps `meshClassifier=on/off` into the `[MemDiag] EVENT RECORD-START` line. Varies ONLY the ARKit classifier (RoomPlan/`semanticLabeling` independent), so the A/B is clean. **Protocol:** perfDiag on; scan the **same large room** twice (toggle OFF then ON); compare the 1 Hz `[MemDiag] … cpu=…% … cpu-by-thread` + `footprint` timeline and the RECORD-START→TEARDOWN footprint delta. Decides whether the classifier's per-face pass is affordable on the already-strained large-room recording phase, or whether to use the free geometric-proximity segmentation instead. (Build green; device run pending.)
      - **✅ RESULT (2026-07-13, VR mode, semanticLabeling=on both arms, thermal=nominal): classifier is AFFORDABLE — negligible CPU, ~70–100 MB memory.** At matched face counts (≤~120k, the overlap range — the "with" run was truncated at ~123k) the two arms are within frame-to-frame noise on `unnamed` CPU (w/o 172/160/174% vs w/ 144/169/183% at ~40/85/120k faces) — no per-face CPU premium (classify pass is cheap or ANE-offloaded). Footprint is consistently ~70–100 MB higher with classification (per-face label storage) — small vs. ~4.4 GB avail. **The real CPU villain is geometry scaling, NOT the classifier:** `unnamed` (VR voxel + mesh reconstruction) climbs ~150% (<100k faces) → ~330% (400k) → ~460–500% (700k), independent of the toggle — the large-room strain that motivates keeping the rescan light (plane reg + decimated proxy, no dense ICP/2nd mesh). fps held ~60 on multi-core but 500% sustained is the thermal headroom risk on longer sessions. **Decision: use `.meshWithClassification` for the wall/non-wall segmentation — cost is not a blocker.** Caveats: measured in VR; to confirm at scale, extend the "with" arm to ~700k faces; `semanticLabeling=off` would isolate further but the delta is already clearly small.
      - **✅ AR-mode confirmation (2026-07-13, VR off, classifier ON, semanticLabeling=on, thermal=nominal, ~790k faces): the CPU-scaling villain is MODE-INDEPENDENT — it's mesh reconstruction + RoomPlan-continuous, NOT VR voxel compute.** This AR run tracks the *same* `unnamed` curve as the VR run at matched faces (~165% @85k → ~326% @400k → **~469% @700k**, 515% peak = ~5 cores). **Correction to the prior entry: the heavy `unnamed` is ARKit meshing threads + RoomPlan's continuous CoreML (both framework-internal, unnamed), present in both AR and VR — turning VR off did NOT relieve it.** Classifier still free (this ON run matches the earlier OFF curve). **Memory comfortable at scale:** peak footprint ~1.9 GB @790k faces, `avail` ≥3.2 GB, `compressed=0` throughout, thermal nominal over ~3 min — cleanest CPU-not-memory confirmation yet. **Implications:** (1) the rescan-light plan targets the right thing (the strain is intrinsic to reconstructing a large scene); (2) **RoomPlan-continuous is part of the `unnamed` load** → lightening it during recording is a second lever (but it feeds the deferred build, so it's a real tradeoff). Two transient stalls (t≈44–50 fps 21/cpu 8% with a spurious `faces=0` sample — mesh not actually lost, it returned at the same count; t≈192 fps 19) = contention hiccups under sustained ~4–5-core load, not data loss or classifier effects.
  - **Caveats:** (i) **style must unify** (color + stroke) or it reads as two ghosts — the enabling condition. (ii) RoomPlan **wall extension can overstate wall coverage** for the guide; optional fix now that wall faces are known — **clip the RoomPlan wall to where the mesh had wall coverage** (fully honest, some complexity). (iii) floor is a similar candidate (RoomPlan floor plane vs. mesh floor faces) if floor clutter matters. Decimation options for the non-wall proxy: quadric edge-collapse (best) / voxel-downsample (cheap) / sparser wireframe (cheapest).

**✅ SOLVER SCAFFOLDED + SYNTHETICALLY VALIDATED (2026-07-13) — [`PlaneRegistration.swift`](../wisescan-ios/PlaneRegistration.swift), the `LocalizationDiag.refine` sibling.** Pure math (Foundation+simd only; RoomPlan conveniences `#if canImport`-gated so it compiles standalone on macOS), no `PerfDiag` gate — intended for the production save path. What's in:
- **`register(source:target:)`** — gravity-locked 4-DOF [yaw, tx, ty, tz] weighted point-to-plane Gauss-Newton over RoomPlan rectangles (5 samples/plane: center + 4 corners — corners are what constrain yaw within a single wall pair; weight = area/5). Correspondence = nearest same-category plane under undirected-normal (25°) + perpendicular-gap (0.6 m) + lateral-reach gates, re-matched per iteration. Undirected normals throughout (RoomPlan sign isn't trusted). Floors constrain ty; **no floor matched → a tiny Tikhonov prior holds ty at ~0 instead of blowing up** (reported via `floorMatched`).
- **Observability = `weakAxisFrac`** (λ_min/λ_max of the area-weighted 2×2 horizontal normal block — the `horizMin` idea, now dimensionless) + **capped inter-axis whitening** (up-weight ∝ alignment with the weak eigen-direction, cap 4×; whitening feeds only the solve, RMS stays raw-weighted so the gate metric isn't flattered).
- **`Gate` + `exportTransform(from:)`** — the `BakeGate` sibling (converged + `weakAxisFrac≥0.1` + `finalRMS≤5cm` + trans≤0.5m + yaw≤15°; `minTransM=3cm` skip floor at call site). Save-time convention: apply source→target **directly** to exported geometry — no inverse, no `setWorldOrigin` (unlike the live bake).
- **Off-device harness ALL PASS** (scratchpad `plane_reg_harness`, 15 checks): square room w/ ±1.5 cm/±0.4° plane noise → recovers a 4°/22 cm offset to err 2.0 cm/0.04° in 3 iters (vs. mesh-ICP's 12×4-level budget); **bare corridor** → `weakAxisFrac=0.000`, gate refuses, cross-axis still recovered exactly, along-axis held at 0 (not fabricated); **cubicle row** (2 small dividers, `weakAxisFrac=0.125`) → passes gate, weak-axis err 1.6 cm; **fabricated target wall + spurious source wall** both fall out unmatched with recovery unaffected (the RoomPlan-closure robustness claim, now demonstrated); **already-aligned** → 2 cm ≈ noise floor, below the skip floor. One real bug found+fixed by the harness: yaw-Jacobian sign (`up×p` vs `p×up`) — diverged exactly like a plausible-looking-but-wrong solver would on device, caught in seconds off-device.
- **Provisional thresholds pending real-scan tuning:** `minWeakAxisFrac=0.1`, `maxFinalRMS=0.05` (BakeGate's were device-tuned; these aren't yet). **Next:** wire DECISION 1 — at save, build this rescan's room, load ghost `roomplan.json` → `planes(fromExportSurfaces:)`, `register`, apply `exportTransform` to mesh + world map (the Phase-1.3/4 co-framing detail), and validate the fit on two real scans of the same room.

**✅ REAL-DATA VALIDATION (2026-07-13, two same-room scans off-device) + viewer fix.** Ran `register` on the two scans' `roomplan.json` (rescan→original): **converged in 3 iters, finalRMS=13.6 mm, all 4 walls + floor matched, `weakAxisFrac=0.984`, GATE TRUSTED.** Measured inter-scan offset: **tx=+1.6 cm, tz=−1.8 cm, yaw=0.05° — but ty=+11.2 cm**: the live ICP bake nulled the horizontal ε; the surviving offset is almost entirely VERTICAL (and matches the mesh-ICP's post-bake 9.7 cm reading — that wasn't noise). Robustness claims demonstrated on real data: original's fabricated 0.2 m sliver wall fell out unmatched; a RoomPlan-extended wall (4.2 vs 3.6 m) matched fine (coplanar). Also resolved a red herring: the "half-meter horizontal offset" seen comparing previews in-app was **`MeshPreviewView`'s per-mesh bbox centering** — the two scans' coverage centroids differ ~1.07 m, so independently-centered previews *look* offset even with near-aligned frames. **Fixed (2026-07-13): previews of a location now share a canonical frame** (center+span = AABB of the ORIGINAL scan's RoomPlan surfaces; fixed camera angle/zoom) so cross-generation previews read TRUE misalignment — the 4D shared-frame contract, enforced in the viewer (`canonicalRoomFrame` in MeshPreviewView.swift; falls back to bbox centering when no roomplan exists).

**✅ DECISION 1 BUILT (2026-07-13) — save-time registration wired into the save pipeline ([`SaveRegistration.swift`](../wisescan-ios/SaveRegistration.swift)).** On a `.rescanSpace` save (never link-adjacent — a different physical room must not be force-matched), the freshly-built RoomBuilder room registers against the location's **ORIGINAL** scan's persisted `roomplan.json` (gen-0 authoritative; target resolved on main pre-dispatch, registration runs on the save's utility queue between the RoomBuilder build and coloring). A **trusted** fit (gate + `minTransM` floor) is baked raw→canonical into: **`mesh.obj`** (`transformOBJ` — one O(n) text pass, before coloring so all consumers see canonical geometry) and **`roomplan.json`** (clean-schema transforms premultiplied via `writeRoomPlan(_:to:applying:)` so viewer outlines stay glued to the mesh). **Stays raw:** `relocalization.worldmap` (opaque, bootstrap-only) + `roomplan_raw.json` (byte-faithful round-trip). **`registration.json` sidecar** written applied-or-not (transform + fit stats + reason — the not-applied record is the gate-tuning diagnostic), promoted to the scan dir top level by saveScan.
- **Co-framing handled:** the next rescan relocalizes into the scan's RAW map frame while its ghost mesh is canonical → the ghost loader (`loadGhostMeshData`) applies `inverseForGhost` (sidecar transform inverse) so ghost ≡ live frame — visual overlay, manual nudge, and the ICP probe keep today's behavior exactly. Cross-location stitch rendering still uses raw-frame anchors vs canonical meshes (≤~T ≈ 10 cm skew) — the remaining Phase-1.3/4 co-framing detail, deferred.
- **Offline verification ALL PASS** (scratchpad `plane_reg_verify`): `transformOBJ` round-trips (T then T⁻¹, err <1e-6 m; faces/comments verbatim); **the sign check on the two real scans** — register(rescan→original)=T, apply T to the rescan's planes, re-register → **0.0 cm residual, RMS unchanged (13.6 mm)**: the exact save-time operation, proven on device data. Build green.
- **✅ DEVICE-VALIDATED (2026-07-15, scans 3+4 of the same location).** Scan 3: `APPLIED canonical correction. trans=13.4cm yaw=0.27° finalRMS=11.1mm walls=4 weakFrac=0.949`. Scan 4: BOTH paths — `ghost mesh de-registered back to its raw capture frame` (first live run of `inverseForGhost`) then `APPLIED trans=26.8cm yaw=1.43° finalRMS=2.3mm walls=5 weakFrac=0.929`. **Offline confirmation that the baked artifacts really are canonical:** registering the exported roomplans gives scan3→scan1 = 0.0 cm, scan4→scan1 = 0.0 cm (RMS 2.3 mm over 5 walls), scan4→scan3 = 0.1 cm. Preview eyeball: scans 1–3 well aligned (slight vertical); scan 4 "a few cm off" — that residual is NOT plane-registration error (rooms coincide to mm): it's the **mesh↔room seating floor** — RoomPlan seats its planes at slightly different offsets from the true mesh surfaces per session (its room SHAPE is highly repeatable, its absolute seating less so) + within-scan mesh warp a rigid T can't fix. Disambiguation: toggle semantic outlines in previews — outlines (canonical rooms) should coincide across scans; each scan's mesh-vs-own-outlines offset is its seating/warp error. **If tighter than ~few cm is ever needed: save-time mesh polish** — after the plane alignment, a small-gate ICP pass (the demoted Phase-2 engine, run offline at save where CPU is free) against the original's mesh; not built, likely unnecessary. Export packaging fix (2026-07-15): `registration.json` now included in export staging (was app-internal only).

**✅ LIVE GHOST AUTO-ALIGN BUILT (2026-07-15) — the plan's "live feedback" gap, closed as a visual-only plane fit (user-confidence rationale: a persistently misaligned overlay erodes trust in the whole process).** During the rescan alignment phase, `planeDetection=[.horizontal,.vertical]` is now on (map-load sessions only — far cheaper than mesh reconstruction, which stays perfDiag-gated); the Coordinator collects **classified wall/floor `ARPlaneAnchor`s** (`PlaneRegistration.planes(fromPlaneAnchors:)`, classification-gated, ≥1 m²) and every ≥2 s registers the ghost room's **raw-frame planes** (`SaveRegistration.rawFramePlanes` — decoded from `roomplan_raw.json`, which is ALWAYS the raw capture frame, matching the de-registered ghost mesh + world map) onto them. A gate-trusted fit re-seats the **ghost ENTITY** (1.5 cm/0.25° hysteresis; `[PlaneReg] ghost auto-align` log). **Convergence stop (frequency contract: a few seats at most):** after 3 consecutive trusted fits land within the hysteresis (~6 s stable), fitting STOPS for the session (`ghost auto-align settled` log) — ε is stationary once settled. Fit attempts are ~µs each (10–20 rectangles vs the ICP's 6k-surfel NN loop); the only continuous cost is ARKit's own planeDetection, bounded to the alignment phase (absent from the record-start config). **Key property: visual-only — the world origin / recording frame are NEVER touched, which is exactly why it may re-run continuously as the fit improves (unlike a `setWorldOrigin` bake); save-time registration remains the authority.** Interplay: manual-nudge sliders override it entirely (user wins); it holds its last value through recording (it was never baked, so identity would snap the ghost back to misaligned); the dev ICP bake resets it to identity (the world moved instead); rescan-only (never link-adjacent — different physical room). **Validate on device:** rescan → expect `ghost auto-align reference loaded: N planes`, then `[PlaneReg] ghost auto-align: trans=…` once ≥2 classified walls are detected, and the ghost visibly seating onto reality within a few seconds of sweeping walls; manual sliders still win; scan saves unchanged (`[PlaneReg] APPLIED …` still measures the raw-frame ε at save).

**✅ DECISION 2 CORE BUILT (2026-07-15) — composite ghost proxy (walls→quads, content→mesh).** Scope per user: the RESCAN GHOST overlay (not the scan-viewer preview); `mesh.obj` stays the untouched save/export artifact. Built:
- **Per-face classification captured at save** (`RawMeshSnapshot.Anchor.classificationData` — one extra memcpy in `snapshotMeshBuffers`; nil when the classifier was off → no proxy, full-mesh fallback).
- **`buildGhostProxyOBJ`** (ARCoverageView, beside `buildMeshOBJ`): floor/ceiling faces dropped unconditionally (floor quad stands in; ceilings have no quad and dropping them un-lids the overlay — a readability win); wall/door/window faces dropped **iff a RoomPlan wall covers them** (perp ≤15 cm, rect +20 cm margin — the reconciliation rule: classifier-wall RoomPlan missed stays in the lumpy proxy, no holes); content (none/table/seat) kept; person-filter replicated; **vertices compacted** (remapped indices) so the size win is real.
- **Save wiring:** proxy built from the SAME snapshot as mesh.obj, given the SAME canonical correction (co-framed), written as `mesh_proxy.obj` (promoted to scan dir; `[GhostProxy] built: N/M faces kept` log). Requires builtRoom (quads must exist to stand in for the removed walls). **The RoomPlan wall+floor QUADS are baked INTO the artifact at save** (4 corners + 2 tris each, appended last) — one coherent, coordinate-locked OBJ: quads + mesh remainder ride the save registration and ghost de-registration together, so they can't drift apart across sessions and the renderer does no dynamic assembly (revised 2026-07-15 per user — replaces the earlier render-time quad synthesis). Rect wireframes show a triangulation diagonal per quad — acceptable v1.
- **Ghost loader: ALL rescans load the proxy when it exists** (`[GhostProxy] rescan ghost using proxy` log); de-registration (`inverseForGhost`) applies identically and proportionally cheaper (shrinks the ghost-load main-thread hitch + the GHOST-LOAD 2×-mesh memory). **A proxy ghost retires the dense mesh-ICP path for the session** (2026-07-15, per user: "we've entirely abandoned mesh for ICP"): no surfel build (`[LocDiag ICP] n/a this session — proxy ghost` log), no candidates, no record-start bake — recording stays in the raw relocalized frame and save-time registration is the sole correction, exactly DECISION 1's design. **The perfDiag alignment-phase MESH RECONSTRUCTION is also skipped for proxy ghosts** (it existed only to feed the ICP) — the "drop mesh-during-alignment" prize, a big prescan CPU cut. **The green chip (`icpAlignReady`) is now driven by the plane auto-align's trusted fit** (production-capable, no longer perfDiag-bound); the mesh-ICP producer still engages for legacy scans with no proxy artifact.
- **Still open from the D2 design:** decimating the non-wall remainder (subtraction alone is most of the win; measure first), scan-viewer (MeshPreviewView) consumption, RoomPlan wall-extension clipping for the coverage guide, multi-class change-detection upgrade (2.2/2.3).
- **Validate on device:** new scan (classifier on) → `[GhostProxy] built …` at save with a large face reduction; rescan it with perfDiag OFF → `rescan ghost using proxy` + ghost shows clean wall/floor quads + lumpy furniture, auto-align still seats it; perfDiag ON → full-mesh ghost + green chip unchanged.

**★ DECISION 3 (2026-07-15) — derived processing moves OFF the save path into an explicit POST-PROCESS stage (the Colorize button, generalized). NEW HEAD OF QUEUE.**

**Trigger/evidence (2026-07-13 device log, large scan: ~420k faces, 250 s, `thermal=serious`, single-digit fps late):** `Deferred RoomPlan: no CapturedRoomData within 3s` → `[GhostProxy] skipped: RoomBuilder produced no room at save — roomplan.json/proxy/registration all unavailable for this scan`. Diagnosis: the failure was at the **data-wait** gate (`roomPlanDataWaitSeconds=3`, [ARCoverageView.swift:3092](../wisescan-ios/ARCoverageView.swift#L3092)) — RoomBuilder (its own 20 s budget, `roomBuilderTimeoutSeconds`) **never even ran**; the save's own ~6.2 s main-thread stall (world-map export window) plausibly starved the `didEndWith` delivery, i.e. the timeout was partly *self-inflicted by the save*. On a thermally-throttled device the save path is the wrong home for anything with a timeout — and the plane pivot's one irreplaceable input (the room) is exactly what dies there. Structural fix, not budget tuning: **capture raw hot, process derived cold.**

**Architecture: save persists RAW artifacts only; everything DERIVED runs at postprocess (cool device, no budgets).** Colorization already proves the pattern — it's been deferred behind the "Color" button all along ([LocationDetailView.swift:579](../wisescan-ios/LocationDetailView.swift#L579) → `colorizeFromSavedFrames` reading saved frames/depth/poses). This decision generalizes that button into **Post-process**.
- **Save keeps:** mesh snapshot memcpy (must be co-framed with the live session), `getCurrentWorldMap`, `buildMeshOBJ` (preview needs *a* mesh), frame/depth capture (already persisted). Plus two NEW persists: **(1) `CapturedRoomData` sidecar** — verified `Codable` in the RoomPlan swiftinterface; `didEndWith` writes it into the scan dir **whenever it arrives, even after save returns** — the 3 s wait is *deleted*, save never blocks on RoomPlan again. **(2) Face-aligned per-face classification sidecar** (~1 byte/face ≈ 0.4 MB at 420k faces) — today `RawMeshSnapshot.Anchor.classificationData` dies with the process; emit the sidecar **from `buildMeshOBJ` itself** so it is index-aligned with the privacy-filtered OBJ face order by construction (per-anchor alignment would break on person-skipped faces).
- **Postprocess pipeline (ordered, chained):** RoomBuilder from the persisted `CapturedRoomData` (unbounded — the 3 s + 20 s budgets stop existing) → `roomplan.json`/`_raw` → **ghost proxy** (classification sidecar + wall/floor quads, `buildGhostProxyOBJ` relocated) → **registration** (rescan only; `SaveRegistration.run` relocated from `finishStopRecording` — same math, applied to `mesh.obj`/proxy/`roomplan.json` exactly as today, `registration.json` written here) → **colorize** (toggle below). DECISION 1 becomes *postprocess-time* correct-at-export; nothing about the solver, gates, `inverseForGhost` de-registration, or artifacts changes — only *when* they run.

**Decisions locked (2026-07-15, per user):**
- **Hard gate, prompt-driven:** rescan, save-into-location, and upload all **hard-gate on postprocess** of the prerequisite scan(s) — "You haven't post-processed this map — do it now" + a [Post-process now] action. No silent raw fallback for a rescan (its registration target + proxy must exist). Rescan entry is the primary gate (the ghost/target comes from the prior scan); save/upload gates are belt-and-braces.
- **Settings toggle (production, NOT dev-gated):** "Colorize during post-process", **default ON** (`AppConstants.Key`, SettingsView main section). Colorization becomes a step *inside* postprocess rather than its own verb; toggle off = structural-only postprocess (fast), colorize later re-offered.
- **Bulk post-process UX, mirroring bulk upload:** operates along the time dimension per roomscan and across stitched maps, reusing the existing latest-only-per-room vs. all-scans selector. **Process oldest-first per location** (canonical/original first — later generations register against it).
- **Live-phase heat is explicitly out of scope** here (accepted; separate track). This decision makes the save *robust to* a hot device; it does not cool it.

**Design details (RESOLVED 2026-07-15, per user):**
- **Postprocess state = per-artifact completeness, not a boolean.** Each derived artifact has a raw prerequisite: room ← `CapturedRoomData` (or an already-built `roomplan.json` from the save-time era); proxy ← classification sidecar; registration ← this scan's room + the ORIGINAL's room; colors ← frames. **Postprocess runs everything achievable; the gates fire only when achievable work is pending.** Three terminal states:
  1. **Complete** — all achievable artifacts exist. Gates pass.
  2. **Legacy-capped** — room exists but no classification sidecar was ever persisted (all pre-DECISION-3 scans; classification died with the process) → **proxy permanently unachievable → NOT bad**: rescan falls back to the existing full-mesh-ghost path, gates pass. This is the migration story: save-time-era scans with `roomplan.json` are complete-to-tier once retroactive registration/colorize run; nothing gate-loops. (Bonus: **registration is retroactively runnable** for legacy rescans whose roomplans both exist — old generations can be canonicalized after the fact.)
  3. **Bad — no room achievable** (no `CapturedRoomData` ever arrived AND no `roomplan.json`; e.g. the 2026-07-13 scan): the scan is degenerate for the whole plane path → **warn "this room scan is bad — redo it"** and prompt a redo; no limp-along "with warnings" tier. **Stitch caveat (accepted):** if the bad scan is a location's original/gen-0, redoing it re-baselines the frame that stitch edges pin to — breaking, but the degenerate state was unusable anyway; the 4.4 re-baseline machinery is the eventual principled home.
- **Unregistered window:** a rescan's mesh stays RAW-frame until postprocessed — the canonical-frame preview (`canonicalRoomFrame`) must tolerate/badge that; the hard gate keeps the window short.
- **Unregistered window:** a rescan's mesh stays RAW-frame until postprocessed — the canonical-frame preview (`canonicalRoomFrame`) must tolerate/badge that; the hard gate keeps the window short.

**✅ BUILT (2026-07-16, build green, device validation pending).** What landed, by file:
- **Save persists raw only.** `DeferredRoomBuild` ([ARCoverageView.swift](../wisescan-ios/ARCoverageView.swift)) rewritten as a **persistence rendezvous**: `provide` (didEndWith) + `setPersistDirectory` (stop flow) — whichever arrives second writes `captured_room_data.bin` (binary plist; `CapturedRoomData` verified Codable in the SDK swiftinterface). **No wait, no RoomBuilder at save** — the 3 s data-gate and 20 s build budget are deleted. Retry-capable (buffer released only on successful write) + the save flows **re-point** the destination at the moved raw dir post-`saveScan`, closing the didEndWith-lands-during-the-move race. `buildMeshOBJ` now also emits **`face_classes.bin`** (1 byte/face, aligned to the EMITTED face order so privacy-skips can't desync it; `MeshExportResult.faceClasses`). Both sidecars added to saveScan's promotion list. The save block in [CaptureView+Recording.swift](../wisescan-ios/CaptureView+Recording.swift) lost the whole RoomBuilder→registration→proxy chain.
- **[`ScanPostprocessor.swift`](../wisescan-ios/ScanPostprocessor.swift)** (new): per-artifact status (`pendingSteps`/`needsPostprocess`/`isBad` with a capturedAt grace guard) + the engine (`run`): ROOM (RoomBuilder from sidecar, 120 s backstop) → REGISTRATION (`SaveRegistration.run(sourcePlanes:)` overload; fresh room = capture-frame planes, legacy room = `rawFramePlanes`; applied T baked into mesh.obj both copies + `registration.json`) → roomplan write (`writeRoomPlan(applying:)` fresh / `retransformRoomPlanJSON` legacy retroactive) → PROXY (`buildGhostProxyOBJ(objData:faceClasses:roomPlanPlanes:)` — rewritten to parse persisted mesh.obj instead of the dead in-memory snapshot; frame-consistent with the current clean roomplan) → COLORIZE (per toggle). Oldest-first; **prerequisite expansion** pulls an unprocessed original into any batch that contains its rescans. Preview regen at the end.
- **Colorizer frame fix** ([VertexColorAccumulator.swift](../wisescan-ios/VertexColorAccumulator.swift)): `colorizeFromSavedFrames` now undoes an applied registration for the projection (canonical verts vs raw cameras previously misprojected by the applied T — a live bug for recoloring registered scans, e.g. scans 3/4).
- **Gates:** Rescan + Connect ([LocationDetailView.swift](../wisescan-ios/LocationDetailView.swift) `postprocessGateAllows`) — bad prerequisites (original/latest) → "Scan Needs to Be Redone" (hard block); pending work → "Post-process Required" + [Post-process Now]. Upload gated in both views' bulk paths + the per-card `uploadScan`. Post-save **bad-scan check** (`scheduleBadScanCheck`, 30 s grace → `ScanStore.badScanWarning` → alert in LocationDetailView, where the save flow lands the user).
- **UX:** "Color" → **"Process"** (bulk in both views + per-card; `wand.and.stars`), running the postprocessor; falls through to the legacy re-color prompt when nothing is pending. ScansListView's existing **Latest/All Scans scope** drives the cross-location bulk. Settings gains **"Colorize During Post-process"** (production, default ON, registered in AppDelegate). Alerts extracted to a `PostprocessGateAlerts` ViewModifier (LocationDetailView type-check budget).

**★ ROOT CAUSE OF THE MISSING ROOMS FOUND + FIXED (2026-07-16, first device run of the postprocess build).** A 245 s / ~497k-face scan reproduced the no-room failure — and the new detection surfaced it perfectly (`postprocess Scan 1: steps=colorize` → `BAD SCAN … no room data after 30s grace`), which exposed the real bug: **`stopRoomPlanSession` dropped the LAST strong reference to the `RoomCaptureSession` immediately after `stop()`** (`roomCaptureSession = nil`). `didEndWith` fires *asynchronously* after stop — RoomPlan finalizes the captured data first, seconds on a long/heavy scan — so the session deallocated mid-finalize and the callback (with the `CapturedRoomData`) was silently lost. **This explains the size/heat correlation all along**: quick scans won the race (2026-07-15 scans got rooms), big scans lost it (2026-07-13, 2026-07-16). It was never (only) thermal starvation of the 3 s wait — the data was being dropped at teardown. Fix: **`stoppingRoomCaptureSession` strong hold** until `didEndWith` delivers (released there / at next session start / at analysis start), with an **identity guard** in `didEndWith` so a superseded session's late callback can't feed a newer recording's box.

**★★ SECOND DEVICE RUN (2026-07-16 later): retain fix VERIFIED (`RP-DIDEND` now fires right after RP-STOP) — and it exposed the next layer: `CapturedRoomData` is Codable IN SIGNATURE ONLY.** Both `PropertyListEncoder` and the `JSONEncoder` fallback throw at runtime ("The data couldn't be written because it isn't in the correct format" = `EncodingError`'s generic message) — **the sidecar design is impossible**; the room MUST be built in the capture app-session. Worse, my retry path re-armed and immediately re-called the persist → **infinite encode loop → OOM crash** (own bug, now dead). **Architecture revision (built, green):** `DeferredRoomBuild` is now a **fire-and-forget POST-SAVE RoomBuilder continuation** — rendezvous of didEndWith's data + the scan directory, which is handed over **only after `saveScan` completes** (the trigger gate that keeps RoomBuilder off the pose-sensitive world-map export); it runs `ScanPostprocessor.buildRoom` (120 s backstop) on a utility queue and writes `roomplan.json`/`_raw` (scan dir + raw_data) when done, one-shot, no retries. `ScanPostprocessor` drops the `.room` step (registration/proxy consume the on-disk roomplan — the former "legacy" path is now the only path); a new **ROOM PENDING** state (`DeferredRoomBuild.isBuildInFlight`) holds the gates and makes the bad-scan check **re-schedule itself** while a build runs, so only "no room AND nothing running to produce one" warns redo. Accepted residual: app killed between save and build completion loses the room (unavoidable without persistence) → scan reads BAD next launch, redo applies. **Validate:** long scan → after save expect `Deferred RoomBuilder starting (post-save)` → `finished — roomplan.json written` (seconds later), then Process → registration+proxy; no OOM; bad alert only if the build genuinely fails.

**✅ RESCAN PRESCAN VALIDATED (2026-07-16) + two fixes from it.** The alignment phase worked end-to-end: proxy ghost loaded (`blob=5.3MB`, Δ+6MB footprint — vs. the old full-mesh 2× coexistence), auto-align reference = 5 planes, settle 4.3 s, and the trust gate earned its keep — three early fits off half-grown ARKit planes (trans ~20 cm, RMS 77–86 mm) were **correctly refused**, then plane growth gave `trans=7.3cm RMS=11.1mm weakFrac=0.94` APPLIED → 5.6 cm → settled → plane detection off. mesh-ICP correctly absent (`n/a — proxy ghost`). Two findings fixed same day (build green):
- **Double world-map load REGRESSED** (two identical `[LocDiag ε] map load` lines): `loadGhostMeshData()` runs in CaptureView.onAppear but `makeUIView` runs on the FIRST body render (before it), so the session started ghost-less with the map, then the ghost's arrival re-ran the load + `resetTracking`. Fix: **CaptureView withholds `initialWorldMapURL` until the ghost cache is loaded** (`ghostLoadAttempted` gate; no-ghost flows still degrade to ghost-less relocalization) — the one-and-only session start is ghost-complete. Supersedes the `932fb32` seeding fix's assumption that ghost data exists at makeUIView.
- **Proxy walls invisible as wireframe** (user: "I can't visually tell what the walls are") — a bare 2-triangle quad renders as outline + one diagonal = emptiness. Fix: **RoomPlan quads tessellated to a ~1 m grid** (`ghostProxyQuadCellMeters`) so walls read as a solid lattice — still massive decimation (5×3 m wall = 30 tris). Proxy artifact now carries a **version header** (`# ghostproxy v2`); ScanPostprocessor treats older proxies as not-built, so existing scans regenerate on their next Process.
- Noted, not actioned: this location's map already carries a **drift trail** (features=9243, p99=6.9 m, max=133 m — almost certainly the scan-1 mid-scan SLAM collapse/reinit) — plane registration is immune (no feature use) and relocalization settled fine; map-hygiene concern only.
- **"Wall lattice only reaches ~1 m up" RESOLVED in two layers (2026-07-16 → 2026-07-20): rendering, not geometry.** The v3 quad-dims diagnostic proved the artifact clean (`quads baked: wall 4.5×3.6, 4.2×3.6, 4.2×3.6, 4.5×3.6, floor 4.5×4.2` — full height; drop tally: 84.8k floor/ceil + 149.1k covered-wall faces replaced by ~180 lattice tris). **Layer 1 — thickness:** wireframe edges are 1 mm-thick quads — the dense lumpy mesh survives that (many edges per pixel) but the sparse 1 m lattice is sub-pixel beyond ~1.5 m. Fix (**proxy v4**): header carries `quadFaces=N` (trailing lattice face count); the ghost loader splits the wireframe build — mesh remainder 1 mm, lattice **8 mm** (`ghostProxyQuadLineThickness`). **Layer 2 — orientation (the "still clips on SOME walls" residual):** the flat-ribbon edge builder uses ONE quad with a fixed world-derived perpendicular + single winding — horizontal grid lines ride a *horizontal* ribbon (edge-on near eye height; backface-culled from one whole side, so above-vs-below-eye visibility flips with the edge's stored direction per wall), and vertical lines hit the degenerate-cross fallback whose fixed plane is edge-on for walls of particular orientations. Systematic for an axis-aligned lattice, statistically invisible on the noisy mesh. Fix: **`MeshParser.buildCrossWireframeDescriptors`** — lattice edges render as a "+" cross-section (two perpendicular ribbons, both windings = double-sided; 8 tris/edge, trivial at ~10² lattice edges); the mesh remainder keeps the cheap flat builder. Render-side only — no artifact change, no reprocess needed. **✅ Device-confirmed 2026-07-20** ("that fixed it").
- **Room-outline emphasis (2026-07-20, per user):** the structural box — wall-wall vertical corners, wall-floor seams, ceiling line — now renders as a distinct OUTLINE pass: each ghost reference plane's 4 border edges (synthesized at ghost load from `ghostReferencePlanes`, which is already de-registered into the ghost's raw frame → coordinate-consistent; children of the same container so it rides auto-align/nudge), drawn at **2 cm** (`ghostProxyOutlineThickness`) in the ghost hue **lightened 55% toward white** — same-ghost family (the D2 style-unify rule) but architecture pops from content, and the derived color can't collide with the user-configurable live-scan green. Interior lattice stays 8 mm ghost-color; coincident borders (wall bottom ≈ floor edge) overlap harmlessly.
- **`minTransM=3cm` floor question (user):** keep. The plane FIT is mm-precise (RMS 11 mm; offline 2–14 mm), but the floor guards against RoomPlan's per-session **seating** noise (~few cm — the 2026-07-15 scan-4 finding: shape repeatable, absolute seating less so). Today's consecutive trusted fits scattered 1.7 cm at one standpoint — that band is the effective sensor floor for this path; "correcting" inside it would move a well-aligned mesh by reference noise. Revisit only if canonical previews show systematic sub-3 cm offsets that matter to 4D diffing.

**✅ BASELINE DEVICE-VALIDATED (2026-07-16, cold device, thermal nominal throughout).** Full new pipeline end-to-end on a fresh scan: RP-DIDEND landed immediately after RP-STOP (retain fix working; the race window only matters hot/long); **deferred RoomBuilder took ~2 s** → "room pending" gate window is a non-issue at this size; roomplan.json written to both destinations (scan dir + raw_data mirror — the double `✓ wrote` log is one build, two writes, and prints have no Xcode timestamps because they're stdout not os_log); **8 surfaces / 5 objects** (healthy wall observability for the rescan); Process ran `steps=proxy+colorize` (registration correctly skipped — baseline IS the canonical frame); **proxy: 139,234 faces / 5.4 MB** (~⅔ face cut vs. the full mesh — the DECISION-2 ghost-load payoff, first measured). Next: the RESCAN pass (proxy ghost load, auto-align, gate check on unprocessed scan 2, postprocess-time `[PlaneReg] APPLIED`).

**Validate on device:** (a) fresh scan → save is fast, `captured_room_data.bin` + `face_classes.bin` land (check a late `CapturedRoomData sidecar persisted` log after a hot scan); (b) Process on it → roomplan/proxy appear, preview gains outlines; (c) rescan gate fires before postprocess, passes after; (d) rescan → save → Process → `[PlaneReg] APPLIED` + registration.json + canonical preview alignment matches the old save-time behavior; (e) upload gate on an unprocessed scan; (f) toggle OFF → structural-only Process, colorize offered on re-run; (g) legacy location (save-time-era artifacts) → gates pass without new work (proxy legacy-capped is fine); (h) reproduce the 2026-07-13 hot-scan scenario → sidecar lands late but lands, scan not bad, Process succeeds; (i) a genuinely RoomPlan-dead scan → bad-scan alert ~30 s post-save.

**Dependencies / open:**
- **Ghost planes:** persisted RoomPlan surfaces (`roomplan_raw.json` — already written every save, carries `category:"wall"` + transform + dimensions) + geometric large-plane fallback from the OBJ for any wall RoomPlan dropped (RoomPlan sometimes omits low-texture/partial walls).
- **Characterize RoomPlan's closure/extension** on partial / large / hallway spaces — bounds both the coverage-guide exclusion and (mildly) the fit: a *fabricated* wall with no live counterpart just falls out of the plane fit, so it's low-risk to registration but real for the guide.
- **Co-framing + world-map re-basing at save** (the T-application detail; shared with Phase 1.3/4).

**Go/no-go — measure the ICP CPU slice to *quantify* the win (`icp=N%`, `[MemDiag] EVENT ICP-REFINE`, commit `7aadca1`), but the pivot is decided on the brittleness + scaling + large-room-CPU grounds regardless.** The save-time mechanism means the live `icp` cost goes to ~0 by construction; the measurement now mainly sizes how much prescan/record CPU the whole plane path frees vs. today.

#### ★ FOLLOW-UP DIRECTION (2026-07-16) — registration-mode ladder for nonstandard rooms
> **Framing:** the journey so far — started with full mesh-to-mesh ICP (Phase 2, device-validated, then demoted), pivoted to plane-to-plane via RoomPlan + plane detection (the PRIMARY above). The plane path stays primary, but it implicitly assumes a **traditional 3–4-wall room**. Real deployments include rooms that break that assumption: a large room with maybe **one reliable wall** (a single focal area mid-wall), or **no walls at all** (scanning the center of a big space, hoping for mesh features). Rather than one method stretched across all of them, expose the ladder we already built as **selectable registration modes**.
>
> **The three room regimes → three modes** (semantics to be workshopped — needs a user-interpretable classification, maybe a slider; "traditional / less traditional / very unusual" is the placeholder):
> 1. **Traditional room (3–4 walls)** — the default/common case → the full current PRIMARY path: RoomPlan rooms both sides, save/postprocess-time plane-to-plane registration. Nothing changes.
> 2. **Less traditional room (~1 wall-ish)** — RoomPlan adds little here (it closes/extends what it can't see, and one wall barely constrains its room model) → **plane-detection-API end-to-end, no RoomPlan**. Register persisted `ARPlaneAnchor` sets against live ones. Groundwork already in: the live ghost auto-align *already* does exactly this live side (`PlaneRegistration.planes(fromPlaneAnchors:)`, classification-gated) — mode 2 is mostly (a) persisting the plane-anchor set as the scan's registration artifact (the `roomplan.json` analog) and (b) skipping RoomPlan entirely. The `PlaneRegistration` solver is source-agnostic (takes rectangles, doesn't care who made them), so it carries over unchanged — as do the observability gate + whitening (a 1-wall room will often sit at `weakAxisFrac≈0`; the gate honestly reports which DOF are constrained, and partial registration beats fabricated).
>    - **Concrete wins beyond registration:** dropping RoomPlan removes its continuous CoreML from the recording phase — measured as part of the heavy `unnamed` CPU load (2026-07-13 AR-mode confirmation) — and saves **~100 MB memory + real model CPU on live scan**. Plane anchors can still stand in for the proxy's wall/floor quads and aid the **mesh decimation/simplification** effort (the DECISION-2 wall-subtraction doesn't need RoomPlan specifically, just plane rectangles).
> 3. **Very unusual room (no walls)** — nothing for a plane fit to hold onto → **mesh-only ICP**, reviving the demoted Phase-2 engine (`LocalizationDiag.refine` — device-validated: sign correct, gates work, `horizMin` eigen-split, best-pick). Crucially, run it where the plane path already runs: **at postprocess, offline, where CPU is free** — the "save-time mesh polish" idea from the DECISION-1 validation note, promoted from polish to sole registrar for this mode. None of the live-bake hot-path baggage (mesh-during-alignment, green chip, record-await) comes back; the brittleness/scaling objections that demoted mesh-ICP were about the *live* path and about it being the *default* — as an opt-in offline mode for wall-less spaces it's the right tool.
>
> **Planes serve TWO decoupled goals — alignment AND mesh simplification — apply each as practical, independently (refined 2026-07-16).** The ladder above governs *registration* only. Simplification (quads standing in for wall/floor faces in the ghost proxy) pays off wherever *any* plane is detected: one wall that's insufficient to constrain alignment still deletes a big slab of polygons from live rendering. So plane detection stays on in every mode and the proxy builder consumes whatever planes exist; the mode decides only what the *registration* trusts. (This means even mode 3 can carry a partial-quad proxy if a stray plane shows up — the "no quads in mode 3" assumption below is the degenerate case, not the rule.)
>
> **Hybrid registration (the modes-2/3 middle ground) — a composite plane+mesh fit MUST weight by represented AREA, not sample count.** The current focus mode registers *solely* on planes. But a hybrid ghost — say 2 simplified walls + simplified floor + a handful of lumpy content meshes — implies a composite solve over both primitive types, and the raw counts there are pathological: each lumpy chair carries **orders of magnitude more faces/points than a quad's samples** (`PlaneRegistration` samples 5 per plane), so a count-uniform ICP would let the content out-vote the scaffold — exactly backwards. The planes are the clean, *stationary* scaffold; the content is the 4D change signal that **may have legitimately moved** since capture. Two-part fix:
> - **Unified currency = surface area.** `PlaneRegistration` already weights plane samples as `area/5` — extend that currency to the mesh side (each surfel/point weighted by the area it represents, ≈ its voxel-cell footprint). A 12 m² wall quad then outweighs a 0.5 m²-of-surface chair *regardless* of the chair's face count. This is a weighting-schema unification, not a new solver.
> - **Content terms additionally capped/robust.** Even area-fair, content deserves a lower trust ceiling (robust kernel or weight cap — the mirror of 2.1a's floor-de-weighting): a moved chair is a valid 4D observation and must not drag the pose. The principled composition: **scaffold-first, content only where scaffold underconstrains** — the `weakAxisFrac`/whitening machinery already names the weak eigen-direction, so content weight can be conditioned to contribute mainly along it. That's precisely the mode-2 payoff: one wall + floor constrain cross-wall + yaw + ty and leave the along-wall axis floating — the lumpy content is exactly what can pin that remaining axis.
>
> **Interactions to design when this gets picked up:**
> - **Classification source:** user-selected at location creation (the slider) vs. auto-detected (the data already exists — RoomPlan wall count/area, `weakAxisFrac`, classifier wall-face fraction) vs. hybrid (auto-suggest, user confirms). The observability math could *demote* a scan's mode automatically when the geometry doesn't support the selected one.
> - **Postprocess state machine (DECISION 3):** "no room achievable = bad scan" is only true in mode 1 — in modes 2/3 a missing RoomPlan room is *expected*, so the per-artifact prerequisites become mode-dependent (mode 2: plane-anchor sidecar replaces `CapturedRoomData`; mode 3: mesh is the only prerequisite).
> - **Ghost proxy:** mode 2 builds quads from persisted plane anchors instead of RoomPlan; mode 3 typically has no quads → decimated-full-mesh ghost (the decimation still-open item gains urgency there) — but per the decoupling above, any plane that *is* detected still becomes a quad in any mode.
> - **Per-mode gates:** mode 2 inherits Gate as-is (or the hybrid's composite gate); mode 3 revives `BakeGate`-style trust (converged + corr count + `horizMin` + finalRMS) applied at postprocess. A hybrid fit needs the gate to report *which terms* constrained which DOF (planes vs. content) — a pose pinned along the weak axis only by movable content is lower-trust on that axis by construction.
> - **Mode is per-location, not per-scan** — all generations of a location must register with the same method against the same canonical artifact, or cross-generation ε becomes method-dependent.

### Next (testing-driven, 2026-06-23) — build order for the next session
1. **2.1 polish — bake the *best* recent refine, not the *latest*. ✅ BUILT (2026-06-23) · ⚠️ device test (2026-06-25): best-pick NEVER ENGAGED (rooms gave ≤1 trusted candidate); the real problem is UNDER-CORRECTION — see course-correction block above.** In feature-desert rooms refine quality swings wildly within seconds (47%→99% inliers, `horizMin` 0.04→1.38); the bake currently grabs whichever trusted refine is latest at record-tap (once grabbed a 47%-inlier fit that WARNed while 99% fits were moments away). Keep a short rolling buffer of recent trusted refines and bake the best by (inliers · `horizMin` · 1/finalRMS). Cheap; helps exactly the common rough rooms.
   - **Built:** `LocalizationDiag.refineQuality(_:)` = `inlierFrac · max(horizMin,ε) · 1/max(finalRMS,ε)` (the three things `BakeGate` already trusts, now used to *rank*). The coordinator keeps a 6-deep rolling buffer (`icpRefineCandidates`) of trusted refines (already past `BakeGate` + the `minTransM` floor — sub-floor "already aligned" readings don't enter the buffer); on each pre-record refine completion it appends and re-selects `pendingICPBake`/`icpAlignReady` as the **max-quality** candidate, not the latest. Logs `[LocDiag BAKE PICK] best of N …` when >1 candidate. Buffer cleared at map load + once a bake is consumed at record-start. **Why best-by-quality is safe over best-by-recency:** the relocalization offset is *stationary* once settled (a property of map↔reality registration, not of where the user stands), so a higher-quality fit from a few seconds ago measures the same offset better than a fresh noisy one. **Validate:** in a feature-desert room confirm the `BAKE PICK` line shows it discarding low-inlier/`horizMin` fits in favour of a better recent one, and that `BAKE OK/WARN` improves vs. the latest-grab behaviour.
2. **Tracking-stability detector / "scan may be unreliable" warning. ✅ BUILT (2026-06-23) · ⛔️ SHELVED (2026-06-25): detector works but its PREMISE is invalid — the saved mesh re-pins, so snaps don't split it (see course-correction block above). Keep the code (perfDiag-gated, cheap); do NOT graduate the warning. Prong-2 rejected.** Rough tracking corrupts the *saved mesh* (geometry split across mid-scan snaps) regardless of the bake, and it's the common condition. Promote the existing snap probe (`SnapTracker`, the >1 m `[LocDiag snap]`) from perfDiag log-only into a real signal: detect a genuine mid-scan persistent snap (vs. degraded-frame junk) and flag/warn the scan as unreliable. Distinguishing real snaps from junk is the hard part (see SnapTracker's existing caveats).
   - **Built:** new `TrackingStabilityMonitor` ([TrackingStability.swift](../wisescan-ios/TrackingStability.swift)) — **pure logic, no `PerfDiag` gate** (a production signal). **The discriminator that beats SnapTracker's caveat:** a single-frame camera jump implying **non-physical velocity** (>6 m/s, ≈22 km/h handheld; floor 8 cm) — or angular >350°/s (floor 18°) — **between two consecutive `.normal` frames** is the world frame being re-pinned, not motion. Gating on `.normal`→`.normal` (a) excludes degraded-tracking junk poses (the VIO starvation guard already owns the degraded *loss* path) and (b) catches exactly the continuous-normal **loop-closure** snap SnapTracker said it *couldn't* distinguish from a brisk pan via raw delta. A frame-delivery gap (`dt > 50 ms`) resets the monitor (the delta across a gap is accumulated motion, not a discontinuity) — kills the dropped-frame false positive. Runs only during recording (when geometry is committed); fires `ScanStore.trackingUnreliable` (snapCount + max Δpos/Δrot). CaptureView shows a red "⚠️ Tracking jumped X cm mid-scan — this scan may be misaligned" banner, **perfDiag-gated for now** (validate the detector before alarming users; graduates with the bake in item 4). `[TrackStab]` line logged (dev-only) on each fire. **Validate:** provoke a loop-closure snap (walk a loop back to a seen area mid-record, or scan a feature-poor room) and confirm a `[TrackStab]` fire + banner; confirm brisk pans / fast repositioning under good tracking do **not** fire (tune the 6 m/s / 350°/s floors if they do). **NOT YET DONE (follow-ups):** persisting the unreliable flag onto the saved `CapturedScan` record (so the scan list / detail can mark it) and a save-time discard/keep confirmation (mirroring `handleVIOCompromised`'s alert) — current cut is a live in-scan warning only.
3. **Relocalization-timeout failure UX (the hallway hang). ✅ BUILT 2026-07-01 · DEVICE-VALIDATED 2026-07-02 (not perfDiag-gated — a production UX).** On-device: the orange panel appears on a stuck relocalization, Try Again / Go Back both work, and the panel **auto-dismisses when tracking is found while it's up**; 30 s timeout felt right. The rescan prompt ([CaptureView.swift:237](../wisescan-ios/CaptureView.swift#L237)) hangs forever on "move camera to relocalize" with no timeout/escape. Surface graceful failure + guidance after a timeout (the 8 s record-await already exists for the bake; this is the *relocalization* equivalent). The 7 m hallway jumps are false *loop-closure*, not VIO drift — self-similar geometry is a fundamental ARKit limit; real fix for feature-poor spaces is physical fiducials.
   - **Built:** a `relocTimeoutTimer` watchdog in `CaptureView`. It arms once tracking enters `.limited(.relocalizing)` with a ghost loaded (and not recording), fires after `AppConstants.relocalizationTimeoutSeconds` (**30 s** — generous: clean rooms settle 3–9 s but rough/feature-poor rooms legitimately take up to ~43 s), and flips `relocTimedOut`. The watchdog stands down (and clears the flag) the instant relocalization reaches `.normal`, the ghost is cleared, or recording starts — so it's **non-blocking / self-clearing**: if a slow-but-working settle finishes after the panel appears, the panel just disappears. Driven by `.onChange` on `trackingStatus` / `cachedGhostMeshData` / `isRecording`; cleaned up in `onDisappear`.
   - **UX:** on timeout the indefinite blue "🔄 Move camera to relocalize" prompt is **replaced in place** by an orange guidance panel — "Having trouble recognizing this spot" + why (space too similar / feature-poor) + what to do (aim at corners/furniture/textured surfaces, ensure good lighting) + two escapes: **Try Again** (reloads the world map via the proven `showRelocDialog` re-relocalize toggle → restarts relocalization, re-arms the watchdog) and **Go Back** (`selectedTab = 2` → returns to the Scans tab; `onDisappear` tears down the capture). Deliberately **no "scan without aligning"** in v1 — merely clearing the ghost does NOT let the session escape `.relocalizing` (it keeps running the loaded world map; see [ARCoverageView.swift:220-255](../wisescan-ios/ARCoverageView.swift#L220-L255)); a true unaligned-scan escape needs a new no-map `session.run` path — deferred as a follow-up if users want it.
   - **Validate on device:** in a feature-poor / self-similar space (the hallway) confirm the panel appears after ~30 s of a stuck relocalization and both buttons work; in a normal room confirm a fast settle **never** shows it, and that a deliberately slow (but eventually successful) settle either doesn't show it or auto-dismisses it on lock.
4. **Graduation:** once the item-1 stability/agreement gate is in + validated, split the bake + record-gate + chip out of `perfDiagnostics` into the production path with the user-facing affordance, then ship. **(Item 2 no longer graduates — shelved 2026-06-25.)**
5. **SpaceAnalyzer × rescan-alignment conflict (found in the 2026-07-07 rebase review) — RESOLVE AT THE END, alongside graduation (item 4).** The upstream Analyze button is gated only on `!isRecording` ([CaptureView.swift:633](../wisescan-ios/CaptureView.swift#L633)), so it's tappable during a rescan's alignment phase. Tapping it calls `startAnalysisRoomPlanSession` → `RoomCaptureSession.run()` on the **shared** ARSession ([ARCoverageView.swift:1249](../wisescan-ios/ARCoverageView.swift#L1249)), which swaps in RoomPlan's own `ARWorldTrackingConfiguration` mid-settle — perturbing relocalization, dropping the alignment-phase mesh config the ICP feed depends on, and leaving stale `icpRefineCandidates`/`pendingICPBake` measured against a pre-analysis session state. `stopAnalysisRoomPlanSession` restores frame *semantics* but not the alignment-phase session shape.
   - **Resolution (decided 2026-07-07): disable — don't sequence.** Hide or disable Analyze while `cachedGhostMeshData != nil && !isRecording` (the rescan/link-adjacent alignment window), with a short disabled-state hint ("finish aligning first"). Rationale: analysis is a *pre-scan staging* tool for fresh spaces — during a rescan the space is already staged, so the feature has near-zero value exactly where it's dangerous; a one-condition UI gate beats any attempt to make `RoomCaptureSession.run()` non-perturbing (it always reconfigures the shared session) or to save/restore the alignment session around it.
   - **Belt-and-braces (optional):** guard `startAnalysisRoomPlanSession` itself (bail + log if a ghost/world-map alignment is in flight) so a future caller can't reintroduce the hazard through a different UI path.
   - **Why end-of-queue, not now:** touching the alignment-phase UI/session mid-validation would perturb the decisive experiment (#1) and the bake plumbing it measures; until then the mitigation is protocol ("don't tap Analyze during rescan alignment"). Slot it with the graduation surgery (item 4) — same files, same seam, one device-validation pass covers both.
   - **Validate:** during a rescan alignment, Analyze is disabled/hidden with the hint; after record-start-less exit (Go Back) and on a fresh new-location capture, Analyze reappears and works; a full Analyze run on a fresh space still produces the report.

**Production bug fixes found during testing (both now COMMITTED, independent of the 2.1 work):**
- `savePendingScan` leaked `isProcessingMesh`/`isWaitingToSave` true after a rescan-into-existing-location save, leaving the record button permanently disabled; fixed (+ the disabled button now shows a spinner/"Processing previous scan…" instead of a silent dead tap). **Committed `1204e35`.**
- **Double world-map load on rescan (fixed + committed `932fb32`, 2026-06-30).** `makeUIView` loaded the map+ghost but never seeded `lastGhostMeshDataCount`, so the first `updateUIView` saw `nil→count` as a ghost change and **re-ran the whole load** — a second `makeConfiguration` (re-deserializing the same world map, the expensive bring-up step — see [[ar-session-bringup-main-thread-freeze]]) plus a second `session.run(.resetTracking)` that **restarts the relocalization makeUIView just began**. Surfaced as two identical `[LocDiag ε] map load` lines + slow/snappy settles (the clean-room run: 43 s settle, 412 cm snap). Fix: seed `lastGhostMeshDataCount = initialGhostMeshData?.count` in `makeUIView` so updateUIView is a no-op for the unchanged ghost (still fires for a genuinely different ghost/map). **Not perfDiag-gated — a real robustness win for every rescan**, and a candidate cause of the rough relocalization that's been bounding the bake. **Re-test:** confirm a rescan now logs a *single* `[LocDiag ε] map load` line and settles faster.

- [ ] **2.1** Promote ICP to **refine**: apply the ICP correction to the relocalized pose before `isRecording` flips on — slots into the alignment phase, [ARCoverageView.swift:1130-1180](../wisescan-ios/ARCoverageView.swift#L1130-L1180). Restrict/up-weight correspondences to structural classes (RoomPlan surfaces / `.meshWithClassification`; large-plane fallback) so moved furniture can't bias the pose.
- [ ] **2.1a** **Wall-weight the ICP correspondences** (the concrete, geometry-only form of 2.1's "up-weight structural" — no class labels needed). Weight each point-to-plane term by `w = max(0.1, 1 − |n·up|)` (target normal; ARKit world Y up) so vertical surfaces drive the fit and floor/ceiling are de-weighted — small, **not zero**, so the vertical `ty` offset stays observable in aggregate. Apply `w` to the ATA/ATb accumulation **and** the (now weighted) RMS in `LocalizationDiag.refine`; normals are already in the loop (~3 lines). **Why it matters even though floor contributes *exactly zero* to yaw/tx/tz** (its normal-equation row is `[-s_z,0,s_x,0,1,0]` — zeros in those DOF, so it can't bias the horizontal solution): a floor-heavy scan still posts low `finalRMS` / high inliers that reflect the *floor* fit, so a horizontally-weak alignment can sail past `BakeGate.maxFinalRMS`/inlier checks. Weighting makes the gate metrics + convergence reflect the *wall* fit, closing that hole. It does **not** manufacture horizontal constraint the geometry lacks — the lever for that is wall *coverage*, which `horizObs` already measures (and the corridor/single-axis case still needs the 2×2 horizontal eigen-split to detect). **Sequence:** AFTER the multi-space bake validation passes — it shifts `finalRMS`/inliers, so don't perturb the in-flight sign/efficacy validation; re-confirm `BAKE OK` once added.
- [ ] **2.2** **Gate the save** on the *inlier* residual + scaffold coverage — **not** a global RMS: persist iff **(inlier RMS < threshold) AND (inlier fraction > floor)**. Above-RMS → reject + re-prompt (reuse the failure-alert / `mapLoadFailed` UX — [CaptureView+Recording.swift:335](../wisescan-ios/CaptureView+Recording.swift#L335), [CaptureView.swift:890](../wisescan-ios/CaptureView.swift#L890)). Below-fraction-floor → not enough surviving structure to trust the lock → re-prompt to capture more scaffold, and if a *clean* scan still falls below the floor, surface the **re-baseline** path (4.4). Never persist a scan that failed validation.
- [ ] **2.3** Set both thresholds from Phase 0 data: inlier RMS (accept few-cm; reject gross) **and** the inlier-fraction floor (distinguish "lots changed but enough scaffold survived" from "too little structure to register"). Log the **outlier fraction, split by semantic class**, as the 4D change metric (content vs. structural).
- [ ] **2.4** **Gate the rescan Record button on alignment readiness** (replaces the current two-tier heuristics — tracking-`.normal`/`.alignedReady` + the manual ghost-nudge — with one *measured* registration gate). Reuse the **trusted-refine signal** that drives the dev `ScanStore.icpAlignReady` chip today (NOT the chip UI itself), promoted off `perfDiagnostics` once 2.1 graduates. "Bake" stays **invisible** to the user — it just fires at record-start; the user sees alignment status, never the correction. **Fallback ladder so a user is never hard-locked out** (esp. a legitimately changed room): no trust yet → "keep sweeping walls to align" coaching (the no-chip state); persistent no-trust → manual-nudge fallback / "record anyway" override; *clean* sweep still below trust → surface the **re-baseline** path (4.4). Demotes the manual ghost-nudge sliders from primary to fallback. Depends on: 2.1 off `perfDiagnostics` + the pre-record refine being reliably ready (the 2000-vert + ~2 s re-run fix — confirm across fresh shallow spaces first).
- **Decision:** on-device ICP (real-time ghost-alignment feedback) vs. offloaded. Lean on-device for bootstrap/gate given LiDAR.

### Phase 3 — Rendering: layers ride the live frame (parallel to 1/2)
- [ ] **3a** *(cheapest high-value win)* Current-session layers: bind wireframe to live `ARMeshAnchor`s with **local** verts (anchor-tracked) instead of baked world verts under `world:.zero` — [ARCoverageView.swift:705-737](../wisescan-ios/ARCoverageView.swift#L705-L737), [:865](../wisescan-ios/ARCoverageView.swift#L865). Same idea for voxels.
- [ ] **3b** Cross-map layers: render connectors/boundary/ghost from real `ARAnchor`s stored in the world map (via `didAdd`/`didUpdate`), not `AnchorEntity(world:)` from a stored transform — [ARCoverageView.swift:1486-1527](../wisescan-ios/ARCoverageView.swift#L1486-L1527).
- **Exit:** mesh/voxels/connectors stay glued to the camera through a relocalization snap (verify via 0.3).

### Phase 4 — 3D stitch durability (pose graph over location frames) — *largest, server-involved*
Depends on Phase 1 (canonical frames must exist).
- [ ] **4.1** Define stitch links between **location canonical frames**, not scan instances (extend `PendingStitchLink` / `StitchLinkStore`). **The raw↔canonical seam fix — `T`-composed, *derived* edges (prerequisite P1 in the adjacent-stitch design below) — is the concrete first piece, and a standalone correctness fix.**
- [ ] **4.2** Multi-constraint boundaries: several pin pairs (or overlap-ICP) per link, not one. **→ designed 2026-07-23 as plane-to-plane + compass refinement of the pivot (a three-source per-DOF covariance blend): see "★ ADJACENT-STITCH plane alignment" below.**
- [ ] **4.3** Server pose-graph optimization treating each link as a noisy measurement; 4D updates never touch the graph.
- [ ] **4.4** Versioned re-baselining op — **timeline-preserving**, **data-triggerable**, and **never moves the original baseline** (connector/stitch transforms are pinned to it). Fires manually or when Phase 2.2 reports a *clean* scan below the inlier-fraction floor / sustained structural-class change (regime 3).

  **Data model — canonical generations.** A location's canonical frame is an ordered list of generations, not a single map:
  - **Gen 0 = the original baseline, authoritative forever.** Connector transforms and stitch edges are stored in **gen-0 coordinates** and never move — so a re-baseline can't break the 3D graph or invalidate history.
  - Each later generation stores `transformFromBaseline` (gen 0's is identity), its bootstrap world map, and its scaffold mesh + registration quality (inlier RMS/fraction, # connector fiducials).
  - A 4D scan stores `(generationId, transformIntoGeneration)`; its position in the baseline = `gen.transformFromBaseline · transformIntoGeneration`. Everything resolves back to gen 0.

  **Solving `transformFromBaseline` (the old→new transform) — connectors are the bridge.** Use whatever of these survive, as a least-squares rigid fit:
  - **Surviving structural overlap** — ICP the new scaffold against the persisting subset of the baseline scaffold (floor, shell, unchanged walls). Same inlier-ICP, small-but-sufficient inlier set.
  - **Re-observed connector pins** — a connector is a *physical* point (a doorway); it almost always survives the change. Re-observe it and use connector positions as high-weight point correspondences. The thing we must preserve is also what measures the transform.
  - **Deliberate bridging scan** — a walk-through seeing both surviving-old and new geometry, captured when knowingly re-baselining.

  **Folds into the pose graph (4.3).** A generation is a **node**, `transformFromBaseline` is an **edge** with covariance from registration quality; connectors stay edges to neighbor locations in baseline coords. No separate machinery — the optimizer absorbs the noise.

  **Keep distinct from connector re-survey.** Frame re-baseline assumes connectors are *physically stable* (default). If a structural change actually *relocated* a link (doorway bricked up, new opening cut), that's a deliberate **re-survey of that one stitch edge** in baseline coords — a different op. Degenerate extreme: no surviving overlap *and* connectors gone = effectively a new location (re-survey connectors from scratch); flag it as the boundary where "re-baseline" stops being meaningful.

### ★ ADJACENT-STITCH plane alignment (design 2026-07-23) — extend plane registration to the 3D stitch (Phase 4.2 / 4.1)

> **Framing.** The 4D PRIMARY engine (`PlaneRegistration`) aligns a rescan to its location's canonical frame *across time*. This extends the **same solver** to the **3D stitch** *across adjacent rooms* — refining today's single pose-pair pivot with planes + compass. It realizes **Phase 4.2** ("several constraints per link, not one") as plane-to-plane, and it forces the **Phase-1.3/4 co-framing** detail the merge deferred (the raw↔canonical seam) to be settled. The solver, the observability math (`weakAxisFrac` / `eig2x2`), and the degradation ladder (rung 3, partial-apply) are **reused** — almost nothing here is new math.

**Today's stitch (the baseline we refine).** A link stores two camera poses — Pin A in map A's frame, Pin B in map B's frame — captured at one physical point with the tablet held still (compass headings are stored but **never enter the transform**). `StitchGraphBuilder.placeScans` derives the edge `r = mSrc·inv(mTgt)` and BFS-propagates it. It's a **single-point, single-orientation** correspondence: exact *at the pin*, but any yaw error rotates far geometry by `yaw·distance` (3° × 5 m ≈ 26 cm) — no lever arm, no redundancy.

**The three-source, per-DOF covariance model (the core).** Neither the pivot nor the compass is precise enough to *own* yaw, and the planes aren't *always* observable — so you cannot hard-assign a DOF to a source. Each enters the (gravity-locked 4-DOF) solve as a measurement with its own covariance, and the solve does the per-DOF handoff:

| Source | Tight on | Silent / weak on | Failure mode | Precision |
|---|---|---|---|---|
| **Pose pivot** (`mSrc·inv(mTgt)`) | translation | yaw (no lever arm; routes through relocalization) | **catastrophic 90°** via a flipped relocalization | cm–dm at the pin |
| **Compass** (per-scan north) | yaw | translation (silent) | magnetic disturbance — **small & random, never 90°** | coarse (~few°, ≈ dm-at-range) |
| **Planes** (canonical roomplans) | yaw + translation, on **observable eigen-directions only** | unobservable eigen-directions | degenerate fold on parallel-only normals | **mm** (11 mm 4D RMS) |

- **Planes own the precision** — for *both* yaw and translation, wherever walls constrain the DOF.
- **The coarse priors keep the precise fit honest.** The compass's bounded yaw covariance is exactly what stops a degenerate plane fit (or a flipped relocalization) from flipping 90°: a flip is many-σ against it. Pose backstops translation the same way. This is why per-DOF *blending* (not selection) is required, and why it is **never-worse-than-the-pivot, per DOF**.
- The output edge + its covariance (from `weakAxisFrac` / RMS + the priors) is exactly the noisy measurement **4.3's pose graph** consumes — build the blend once, it serves 4.2 and 4.3.

**Two distinct plane mechanisms (both were requested; they are not one thing):**
1. **Shared wall → translation snap.** A genuinely shared physical surface pins **one** horizontal DOF: the offset *along that wall's normal* (an *oblique* shared wall is worth more per-wall — it constrains a direction the building axes don't). Gives nothing along its own length, only weak yaw alone. **Needs a real correspondence.**
2. **Orientation → yaw refinement — NOT a Manhattan grid-snap.** The solver never assumes orthogonality (`eig2x2` *measures* conditioning; two walls at 30° are a valid, weaker basis). The banned shortcut is snapping room B's dominant grid onto A's mod-90° — it fabricates a grid that partition walls / oblique / *creative* architecture don't have. Instead: **refine yaw only within the compass window, only on a corroborated correspondence**; else keep the coarse compass yaw. The compass is the veto — an orientation match proposing a yaw *outside* the window means the walls aren't the shared grid you assumed (or the match is wrong) → drop it.

**Correspondence is the real delta from 4D.** 4D is the *same room* → blind nearest-plane matching works. Adjacent rooms share only a boundary → most walls are unique; blind matching fabricates garbage. Three guards:
- **The prior is the initial guess.** Feed the (`T`-composed, see below) pivot+compass transform as `register(initial:)`; a truly shared wall then lands within cm/° of its partner while non-shared walls land meters off or unmatched — a tight post-transform gap gate separates them. `register()` already re-matches per iteration under normal/gap/lateral gates.
- **Spatial gate to the boundary** ("within a certain distance" — a far wall of A *cannot* be shared with B). Applies to the **shared-wall** search only; the **yaw** side wants all walls.
- **Conservatism, because a wrong same-wall match is a *systematic* offset, not noise** — and partition walls are the worst case (thin, movable, often doubled a few cm apart → wrong-face lock). Prefer the prior over a shaky match. Corollary priority: the pivot is *good* at translation and far-field error is yaw-dominated, so **yaw-first** — the shared-wall translation snap is a small uniform correction *and* the riskiest correspondence; consider skipping it in exactly the creative spaces where it can't be trusted anyway.

**The raw↔canonical complication (from PR #28) — must be settled first.** PR #28 moved *geometry* (mesh / roomplan) into each location's canonical frame but left the *world map and every anchor pose* in the raw capture frame (the map is opaque, can't be re-based). So the pivot `mSrc·inv(mTgt)` is still a correct **raw↔raw** transform — it just no longer points where the geometry now lives. **The seam is exactly the endpoint registrations** (`T`, measured 11–27 cm, sub-2° yaw) — this *is* the "≤~T ≈ 10 cm skew, deferred" note under DECISION 1. A legacy scan that got retroactive registration had its geometry shift ~11 cm under an unmoved anchor → the seam appeared *retroactively*, with nobody touching the stitch.
- **Correct canonical edge:** `R_canonical = T_A · (mSrc·inv(mTgt)) · T_B⁻¹`, where `T_X` = scan X's raw→canonical (`registration.json` applied transform; identity for gen-0 / unprocessed).
- **Fix = make the edge *derived*, not authored** — recompute at graph-build time from (anchor poses + each endpoint's *current* `T`). Self-heals across processing / re-registration / legacy: an unprocessed endpoint reads `T = identity` (raw anchor + raw geometry, consistent), a re-registration recomputes, legacy links pick up their retro-`T` on processing. No migration pass. Localized to the `placeScans` `r =` derivation + a `T` lookup.
- **Data-model prerequisite:** the edge must compose the `T` of the scan whose *frame the anchor lives in* (Flow B: the scan you *relocalized into*, not the one being created). Verify `sourceScan` / `targetScan` identify the frame-scan — composing the wrong `T` turns an 11 cm fix into an 11 cm error.
- **The plane fit sidesteps `T` for what it observes:** both roomplans are already canonical → the fit yields `R_canonical` directly. `T`-composition is needed only for the **prior** (raw pose+compass → lift to canonical) and the **fallback DOFs**; compass north also gains `T`'s (tiny) yaw.
- **gen-0 endpoints (`T = identity`) are seam-free** → soft-prefer anchoring pins to gen-0 frames, and propagate endpoint registration covariance into the edge (a poorly-registered endpoint = a less-certain stitch).

**Compass specifics.** Per-scan magnetic north is already recorded across the time dimension — usable **iff** it is *pose-anchored* (heading + the pose at that instant → a frame-to-north), and it is raw-frame so it takes the same `T`-composition. The time dimension gives redundant readings of one canonical frame → averages out *temporal* noise, but **not** the site's static magnetic bias (fixed steel), which is why the compass stays coarse — and since two locations carry *different* site biases, it doesn't cancel in the inter-location yaw either. Do **not** switch the app to `.gravityAndHeading` (bakes the disturbed absolute heading into every frame, big blast radius, no common-mode cancellation). `|C_A − C_B| ≈ 0` at the pin is a free "the tablet didn't move" certificate on the whole pivot premise.

**Gate adaptation.** The 4D `exportTransform` gates bound the **absolute** transform (`trans ≤ 0.5 m`, `yaw ≤ 15°`) — right for a rescan that should land *near* canonical, wrong for a stitch edge of arbitrary magnitude. Gate the **correction relative to the prior**, not the absolute pose.

**Solver reuse — two tiers.** `register()` already seeds from `initial`, and its Tikhonov damps the *increment* toward zero, so a **truly** unobserved DOF (zero column) already inherits `initial`. Therefore:
- **Tier A (nearly free):** pass the `T`-composed prior as `initial`, relax the all-or-nothing `weakAxisFrac` gate to per-DOF. Correct for *truly* unobserved DOFs; **soft on *weakly* observed ones** (the tiny fixed λ won't hold a small-but-nonzero eigen-direction against a noisy observation).
- **Tier B (the right form):** replace the fixed λ with an **eigenvalue-adaptive prior toward the pivot / compass** — the covariance blend above. Handles weak DOFs correctly and *is* the 4.3 edge. **Recommended: skip Tier A, build Tier B** (Tier A's per-DOF gate is throwaway once B exists, and B serves 4.3).

**Build ladder (mirrors the degradation ladder, across rooms):**
1. **Shared corner** (≥2 independent shared normals) + floor → full rigid from geometry; the prior is a fold-resolver / sanity check.
2. **One shared wall + compass yaw** → perpendicular offset + yaw (+ vertical from the shared floor); along-wall translation stays on the pivot. *The common case → partial-apply.*
3. **No shared wall, structure present** → yaw refined within the compass window; all translation on the pivot. Still kills the distance-amplified far-field error.
4. **Creative / no usable structure** → coarse priors only (compass yaw + pivot translation). dm-at-range, robust, never-worse-than-today.

**Prerequisites to solidify first (before any refinement):**
- **P1 — Settle the raw↔canonical seam (also a standalone correctness fix). Do first.** Verify the seam is live as described (`placeScans` uses raw poses; the DECISION-1 note confirms it), confirm `sourceScan` / `targetScan` frame-scan semantics, then make the edge derived + `T`-composed. This is the foundation the plane prior and every fallback DOF sit on, *and* it removes today's decimeter seam on any stitch touching a rescanned / legacy location.
- **P2 — Confirm per-scan north is pose-anchored + retrievable** as a frame-to-north (heading + pose), not a bare scalar; add the small persistence if not.
- **P3 — Confirm `registration.json` `T` is loadable at graph-build time** for stitch endpoints (rescan / legacy: applied `T`; gen-0: identity).

**First step (after P1 lands): measure the residual.** With the canonical baseline correct, measure the *residual* stitch error on real stitched-**and**-rescanned data (offline plane-fit of the two canonical roomplans vs. the `T`-composed pivot). That number sizes the compass-window covariance and decides whether the shared-wall translation snap is worth building at all — the empirical input the whole covariance blend needs. Only then build Tier B, yaw-first.

**✅ P1 VERIFIED + FIXED (2026-07-23, simulator build green).** The raw↔canonical seam is confirmed live and the derived-`T` edge is built.
- **Verify — all four questions resolved.** *(Q1)* Seam is live: `StitchGraphModel.placeScans` built `r = mSrc·inv(mTgt)` from raw anchors while `CombinedMeshView` draws the canonical `mesh.obj`. *(Q2 — the load-bearing one)* In BOTH flows the link's endpoint scan **is** the anchor's frame-scan: Flow A stores `savedScan` (the scan just recorded, anchor from its session, [CaptureView+Extend.swift:136-138](../wisescan-ios/CaptureView+Extend.swift#L136-L138)); Flow B stores `activeScanToExtend`, which `LocationDetailView` sets to `latestScan.id` in the *same* place it loads `latestScan.worldMapURL` for relocalization ([LocationDetailView.swift:134-135](../wisescan-ios/LocationDetailView.swift#L134-L135)) → you relocalize into exactly the scan the link records. So `T` per endpoint = the registration of `link.sourceScan`/`targetScan`, unambiguous. *(Q3)* The forward `T` is `Sidecar.transformMatrix`, decoded by `loadSidecar`, per-scan via `scanDirectory`; added `SaveRegistration.appliedTransform(scanDirectory:)` (the forward sibling of `inverseForGhost`). *(Q4)* The stitch compass is already pose-anchored — `anchorACompassHeading`/`anchorBCompassHeading` captured with the pin pose in the same frame (a per-scan `compass_heading` in metadata is a bonus source).
- **Fix — derived, `T`-composed edge.** `placeScans` now lifts each anchor by its endpoint's `T`: `r = (T_src·mSrc)·inv(T_tgt·mTgt)`, `T` from the sidecar (identity when a scan saved raw / isn't processed), memoized per scan id. Frame algebra checked through the BFS: `world[loc]` maps loc-canonical → root-canonical, the root's identity *defines* the shared frame as its own canonical frame, the `inverse(r)` backward branch inherits the fix, and unprocessed endpoints (`T = identity`, raw anchor + raw mesh) stay consistent — self-healing across processing/legacy, no migration. Two files: `SaveRegistration.swift` (accessor) + `StitchGraphModel.swift` (`placeScans`).
- **Remaining:** (1) **measure the residual** the fix removes — needs a stitched-**and**-rescanned capture; (2) one-line confirm `StitchGraphView`'s `CombinedMeshItem.meshURL` is the canonical `mesh.obj` (expected — reuses `MeshPreviewView` geometry). Then Tier-B, yaw-first.

**✅ Op-1 (live Pin-A plane-composition) BUILT + device-validated (2026-07-23).** The adjacent *connect* now runs the same proxy-ghost plane path as a rescan — `.linkAdjacent` broadened alongside `.rescanSpace` at the ghost-load gates (proxy + reference planes load, dense ICP retired), and `confirmAlignment` composes the auto-align fit into Pin A (`pinA_mapA = fit⁻¹ · pinA_live`) so the stitch anchor lands in map A's raw frame that `placeScans` then lifts by `T`. Device run (link `0C3D15AC → ABE7BD40`): connect loaded the 5.4 MB proxy, auto-aligned, placed Pin B, created the link, saved map B with no freeze.
- **Finding — the boundary fit wanders, and Op-1 has no post-save safety net.** The connect auto-align never settled: trans wandered `22 → 7.5 → 12.6 → … → 21.5 cm`, `weakFrac` fell `0.98 → 0.51` when a 3rd (likely non-shared boundary / room-B) wall joined, RMS `9.9 → 34.8 mm`; the live frame was still snapping (a 23 cm recovery snap). A rescan tolerates this because it **re-bakes at save** — the live fit is only a visual seat. Op-1 bakes the fit into the stitch link **at Confirm with no later correction**, so the *latest* fit at Confirm (here the worst one) must not be what's used.
- **Decision — the anchor fit is BEST-not-latest.** Publish `ghostAutoAlignFit` only when a **tighter-RMS** trusted seat appears (rank by `report.finalRMS` = how well the walls actually line up); on the log this keeps the `9.9 mm / weakFrac 0.98 / 12.6 cm` seat instead of the `34.8 mm / 0.51 / 21.5 cm` latest. The **visual** seat (`ghostAutoAlign`) + green chip (`icpAlignReady`) still track the *latest* fit — only the baked anchor fit is best-not-latest. Coordinator-local `ghostAutoAlignBestRMS` (delegate-queue, reset in `resetGhostAutoAlign`); one file (`ARCoverageView`). No trusted seat ⇒ still identity ⇒ raw Pin A (never-worse-than-pivot holds).
- **Open (metric refinement).** RMS-primary is safe — the weak-axis correction is already capped, so a tight single-wall seat corrects only its observable direction and leaves the rest on the raw prior — and it matched the log. But a *looser-but-better-conditioned* seat vs. a *tighter-but-underdetermined* one is a real tie only `weakAxisFrac` can break; fold it into the ranking if device data shows the need. This is Op-1 hardening; the **cross-room** refinement (room-B walls → refined EDGE on the link + covariance, at postprocess) is still Op-2 / Tier-B, unbuilt.

**✅ Residual measurement BUILT (2026-07-23, sim build green) — the "measure the residual" first step, now instrumented on-device.** A pure-measurement diagnostic (no mutation), the direct skeleton of Op-2's solver. Two additions: `SaveRegistration.canonicalFramePlanes` (the `rawFramePlanes` sibling *without* the raw-frame undo) + `StitchGraphBuilder.logResidual(link:)`, called per link from `build(from:)` gated on `PerfDiag`.
- **Why on-device, not a Mac-side script:** the pivot edge needs the **anchor poses (SwiftData)** and each endpoint's **`T`** — neither is in the export zip. So "offline" = *outside the live loop*, not off-device. It reuses the exact solver (`PlaneRegistration.register`) on the exact data.
- **The math:** load both endpoints' CANONICAL roomplans; lift the target's into the source frame by the pivot `r = (T_src·mSrc)·inv(T_tgt·mTgt)` (identical to `placeScans`); `register(source, target)` with `initial = identity`. Then `initialRMS` = residual under the pivot ALONE (headline), `transM`/`yawDeg` = the correction planes would add (sizes the compass window + the shared-wall-snap go/no-go), `finalRMS` = the achievable floor, `sharedWalls`/`weakAxisFrac` = correspondence observability, `|C_A−C_B|` = the compass certificate.
- **To read it:** enable `PerfDiag`, open the stitch graph / combined view (triggers `build`) → one `[StitchResidual] <src>→<tgt> …` line per link. `register`→nil ⇒ **<2 shared walls** under the pivot (logged distinctly — that link is pivot-only, no plane signal, which is itself a Tier-B input).
- **Next:** run it on `0C3D15AC→ABE7BD40`, read the numbers → decide Tier-B scope (yaw-first). Op-2 proper (store the refined edge + covariance on the link, wire into 4.3) remains unbuilt; this measurement is its front half.

**★ Device result + the "what IS a shared wall across a boundary" refinement (2026-07-27).** Ran the diagnostic on `0C3D15AC→ABE7BD40` (anchor placed just inside room-A's doorway): **RUNG-1 n/a — <2 walls correspond** (srcWalls=4 tgtWalls=6). Not a bug — a doorway stitch shares at most the one doorway wall, and `register` needs ≥2. This IS rung-2/3, as the plan predicted; the pivot is fine.
- **The design crux (from the field observation).** The doorway wall shows up in both scans as OPPOSING faces — anti-parallel normals, offset by ~wall thickness (a few cm). A human aligning the two rooms labels them "the same wall," and for **correspondence** they are — but the sharing decomposes, and only ORIENTATION survives:
  - **Orientation is shared** (face-independent) → a clean, thickness-free **yaw** constraint. Undirected matching (`|n_s·n_t|`, already in `register`) correctly pairs the opposing faces.
  - **Position is NOT shared** — two faces, ~thickness apart. Point-to-plane would drive them to COINCIDE (perp→0), over-closing the layout by the thickness → a *systematic* translation bias. So "same wall" is true for angle, false for position.
  - **The normal SIGN is the discriminator.** `n_s·n_t < 0` (opposite faces — the cross-boundary case) ⇒ orientation-only: refine yaw, hold translation on the pivot. `n_s·n_t > 0` (same face — the *rescan* case) ⇒ position valid too. This is precisely why the rescan's point-to-plane translation is clean and the stitch's is not — same solver, opposite face sign.
- **Consequence for Op-2 / the build ladder.** For the typical adjacent (doorway / opposite-face) stitch: **yaw-only** — refine yaw from wall orientations within the compass window; ALL translation stays on the pivot (a direct point measurement at the doorway; it needs no wall correction). This largely **answers the plan's open "is the shared-wall translation snap worth building?" → no, for opposite-face walls** (thickness-biased); rung-2's "perpendicular offset" collapses into rung-3 (yaw) unless a shared wall is genuinely *same-face* (rare in a stitch). The human analogy is exact: **the wall gets the angle right; the doorway point you stand at gets the position right.**
- **Diagnostic extended to the single-wall case** (built, sim green): reports the best near-∥ wall pair's `faces=opposite|same`, `perp` (thickness + pivot error), `lateral`, `normalΔ`, thickness-free `yawΔ`, near-∥/tight counts, and `|C_A−C_B|`. Re-running gives the doorway wall's `yawΔ` — the yaw the plane wants — to weigh against the compass window (does yaw refinement even have signal here, and does it fit inside the compass guardrail?).
- **Future — the doorway OPENING as a richer shared feature.** The opening/door itself (a *localized* gap seen from both sides, and exactly where the pin sits) could pin position — along-wall AND perpendicular — far better than the thickness-ambiguous wall faces, *if* RoomPlan's opening/door detection is reliable enough. Noted for later.

**★ RESOLVED (2026-07-27) — full-landscape re-run: the pivot is EXCELLENT; plane refinement is a guardrail, not a corrector.** Logging all 12 near-∥ pairs for `0C3D15AC→ABE7BD40` decided it. Grouped by source wall: the pin-*nearest* wall (`pinPerp=84cm`) has **no** coplanar partner (best `perp=129cm`) — it's a *side wall* room B doesn't share, and its 1.3 m gap is room geometry, not error. The one genuinely shared wall is one further in (`pinPerp=128cm`): **`perp=1.6cm`, opposite faces, `lateral=929cm`** — the doorway wall, with the big lateral being the offset-centroid signature of intentional small overlap. So the pivot aligns the real shared wall to **1.6 cm ⊥ / 0.4° yaw** (corroborated by `|C_A−C_B|=0`). Conclusions:
- **A well-executed stitch (still tablet, good pin) needs no plane refinement** — the pivot+compass already land at cm / sub-degree. The plane fit *confirms*; it doesn't improve. So **Op-2's value is a GUARDRAIL / DETECTOR for BADLY-executed stitches** (flipped relocalization, tablet moved, bad pin), not a corrector for good ones. Gate it on the cheap health signals: `|C_A−C_B|` disagreement + the doorway-wall residual (`perp`/`yawΔ`).
- **Shared-wall correspondence selector (learned):** the shared wall is **coplanar (small `perp`) + opposite-face (`n·n<0`) + near the boundary** — *not* pin-nearest (that's a side wall) and *not* centroid-close (offset by design). `coplanar∥` count cleanly isolates it (=1 here).
- **What the shared wall constrains:** **yaw** (clean) + **through-doorway perpendicular** offset (thickness-caveated). It does **NOT** constrain along-doorway translation — no shared wall runs perpendicular to the boundary (each room owns its side walls, e.g. S1) — so along-doorway stays on the pivot/pin. Vertical stays on the floor.
- **Next experiment to justify building Op-2 at all:** capture a *deliberately bad* stitch (move the tablet between pins, or force a poor relocalization) and re-run the diagnostic — confirm it (a) *detects* the badness (large doorway-wall `perp`/`yawΔ` or `|C_A−C_B|`) and (b) quantify how much the yaw + ⊥ snap would recover. Only then is the refine-vs-flag tradeoff grounded.

**★ BAD-STITCH validated + Op-2 scoped as a yaw+⊥ REFINER (2026-07-27).** Botched stitch `DF5FAC4A→86032CDA` (strode forward during the fast session flip): **`|C_A−C_B|=6.8°` and plane `yawΔ=+5.5°` on every pair — both instruments caught the yaw error and AGREE** (compass = coarse guardrail brackets the precise plane fix). Visual preview shows **~0.5 m translation + rotation** across the overlap. So a bad stitch is cleanly detectable, and the yaw fix is validated (three-source model working).
- **Error model (from good vs bad):** the **pin owns translation well** but is not immune — a fast flip mostly injects **yaw** (rotation in place), and a stride injects **through-doorway translation** too. The per-wall `perp` is yaw-contaminated (`perp/yaw ≈ 4 cm/°` = lever arm), so yaw and translation must be solved **separately, yaw-first.**
- **The refiner math (correspondence-light):** (1) **yaw** = median near-∥ `yawΔ` — *correspondence-free* (the whole roomplan rotates uniformly), accepted only within the compass window (else keep pivot, resolves the Manhattan 90° ambiguity); (2) **de-yaw about the pin**, then the smallest coplanar **opposite-face** gap = the pure **through-doorway ⊥ translation** (gated — only snap when ≫ wall thickness, else it's just thickness); (3) **along-doorway + vertical** stay on the pin/floor (no wall constrains them; the doorway *opening* is the only future lever). `StitchGraphBuilder.logResidual` now logs the PROPOSED `yaw` + de-yawed `⊥trans` (+ a `postNΔ` de-yaw sanity) — Step 1, measure/verify before mutating the edge.
- **Scope (decided):** **auto-correct** (Op-2 proper) — compose the guardrailed yaw + gated ⊥ correction into the stitch edge — **plus a named indicator + an in-app MANUAL ADJUSTER** as the follow-up remedy. **NOT "rescan"** (user, 2026-07-27): rescanning is the wrong action for a *placement* error; the fix is a transform update — a simple rotate+translate gizmo per scan piece (the pattern countless apps have), persisting a delta on the link edge, for the residual after auto-correct and for gated-off cases. Precedent exists (`manualNudgeActive` on the live ghost). **Refer to stitches by LOCATION NAME, never hex** — satisfied (`Stitchbadtl ↔ Adjacent to Stitchbadtl`).
- **Build ladder:** Step 2a yaw + Step 2b ⊥ translation — **BOTH DONE & VALIDATED** (`stitchCorrection`, composed `r'=(Tperp·Cyaw)·r` about the pin in `placeScans`, fires on the combined render, logs by name). Bad stitch `Stitchbadtl ↔ Adjacent to Stitchbadtl`: `auto-corrected yaw +5.5°, translation 49cm` — **visually dead-on** (rotation + overlap both resolved). Yaw = correspondence-free median (compass-guardrailed); ⊥ = min de-yawed coplanar-opposite gap (NOT the pin-nearest 5.4 m side wall), room B placed pin-opposite at nominal thickness. → **Step 2c DONE** (below).
- **★ Why it works with ZERO co-observation — the core justification for planes over ICP (confirmed in the extreme, 2026-07-27).** The inside scan saw *nothing* of the outside room (not the boundary wall's far face, not any outside wall) and vice-versa. The ONLY shared thing is the **boundary plane**, realized *independently from each side* — A's near face, B's near face, `faces=opposite`, **never co-observed**. Plane-to-plane alignment needs only that common plane; it needs **no overlapping surface**, which is exactly what ICP *requires* and what an intentionally-minimal-overlap adjacent stitch *cannot provide*. This is the empirical vindication of the PRIMARY pivot (plane registration, not mesh ICP). Degrades gracefully: if a room's boundary-wall view is too poor for RoomPlan to emit the plane, no match → gated off → raw pivot (never-worse).

**✅ STEP 2c BUILT (2026-07-27, sim build green) — the combined render is now the alignment cockpit.** Three surfaces, all on the **full combined render** (`CombinedMeshScreen`), none on the all-scans graph list:
- **Map legend panel** (`StitchLegendPanel`, top-right, toggleable): one row per map — **tint swatch + location NAME** (so you can finally tell which piece is which) + status: *Aligned* (green), *Auto-corrected · yaw/shift* (orange, by magnitude), *Manually adjusted* (cyan), or *Base map (reference)* for the root. Footer names the orange convention + "tap to fine-tune, or rescan if still off." Hex never shown (satisfies the user's "refer to them by name").
- **Stitch-point markers**: an always-on-top red pin sphere at each link's shared boundary point (source-side pin mapped into the shared frame), so the join is findable. Repositions live as pieces move.
- **Manual join adjuster** (`ManualAdjustPanel`, bottom): tap a map → gravity-locked **yaw + 3-axis translate** sliders (mirrors the ghost-nudger idiom). Live: every drag recomposes placement transforms **in-memory** and re-seats the SCNNodes (no reload, no disk). Save persists a `ManualNudge` (4 scalars) on the `StitchLink`; Cancel restores; Reset zeroes. This is the user's remedy **in place of "rescan"** for a placement error.
- **Architecture:** `computeAutoCorrections` (the only disk read — canonical roomplans) runs ONCE at render-present; `placeScans` is now pure matrix math taking the cached autos + manual overrides, composes `r' = manual · C_auto · r` about the (post-auto) pin, and returns `ComponentPlacement {scans (+parentLinkId), stitchPoints}`. Manual rides the edge like the auto correction, so the child's whole subtree moves with it. The Op-2 residual/`[StitchCorrect]` logs moved here too, still `PerfDiag`-gated (per the user: diag belongs to the render, not the every-rebuild graph list). Corrections themselves apply always (unchanged).
- **Non-issue (confirmed, user 2026-07-27):** the manual slider "handedness" can read inverted on a *backward* BFS edge, but it's entirely viewing-angle-relative (with live feedback you just push the slider the way that looks right) — not worth fixing. The nudge is stored link-intrinsically (root-independent), so the *relative* alignment is always correct regardless of BFS direction.

**✅ STEP 2c REFINED (2026-07-27, sim build green) — auto-correct became opt-in + the correction became authoritative.** Four changes from a UX/architecture pass:
- **Auto-correct is now OPT-IN, not silent** (user: "easiest way to visualize over time how good it is at various scenarios"). The render shows the **raw pivot by default**; the user applies the solver's fix per-join (an **"Auto" chip** in the legend row), in bulk (**"Autocorrect all"** / **"Clear"**), or globally (**"Always autocorrect"** checkbox → `StitchPrefs.alwaysAutocorrect`, UserDefaults, implicitly all joins). Data model: `StitchLink.autoCorrectOverride: Bool?` (nil = follow the global default; true/false = explicit per-join override that wins). `placeScans` gates the auto compose on the effective flag. So you can flip a join on/off and watch the before/after — the empirical read on solver quality.
- **Floating name labels at each map's centroid** (user: color-coding can't identify maps — colorize/RoomPlan modes are already multi-colored). A billboarded, depth-independent SCNText with a dark backing plate hovers over each piece's top-center and rides live nudges. This, not tint, is now the "which map is which" answer; the legend swatch is secondary.
- **Adjuster slider ranges + response** (user): rotation cap ±15°→±30°, translation ±0.5 m→**±2 m**, and all sliders made **nonlinear** (signed-power, `curve=2`: value = sign(p)·|p|²·max over a linear thumb position p∈[-1,1]). The center of the travel is fine (~0–0.5 m) and the extremes reach the full range — fine-tune normally, still dial out a big drift. Round-trips (the inverse positions the thumb for a saved nudge).

**✅ AUTO + MANUAL MERGED into ONE correction (2026-07-27, sim build green) — no more layering.** The user caught that auto was a *separate transform under* the manual nudge (`r' = manual · C_auto · r`), so the sliders showed only the manual delta and toggling auto yanked its contribution out from under a hand-tweak. Fixed by **transferring** auto into the manual values instead of layering:
- The auto correction is **losslessly a `ManualNudge`** (`StitchCorrection.asNudge`: the signed yaw + the translation vector actually applied) — applying it about the **raw pin** reproduces the old `C_auto` exactly. So "Autocorrect" now *seeds the sliders* with the solver's `+X° / Y cm`; there is a **single** correction (the nudge), pivoted about the raw pin, used by both render (`placeScans`) and export.
- Controls: the per-join **"Auto" chip** toggles the nudge between the solver value and zero (flip a join corrected/raw to judge it); **"Autocorrect all"** seeds every offered join; **"Clear"** zeros; **"Always autocorrect"** *transfers* the fix into every untouched join (persisted, so it survives toggling always off). `StitchLink.autoCorrectOverride` + the render-time auto layer were **removed**; `StitchGraphBuilder.effectiveNudge` is the single source (stored nudge, else the auto fix when always-on for an as-yet-unopened render / export).
- Consequences (told to user): re-clicking Autocorrect re-seeds from the solver (overwrites hand-tweaks on that join); with always **on**, a Cleared join re-seeds on next open (turn always off to keep one raw). Both consistent with a "sync the values" model.

**✅ SANITY CHECK + room-level data sourcing (2026-07-27).** Checked the merged changeset against edge cases; folded in the cheap room-level fix, deferred the model change:
- **No roomplan → manual still works.** ✓ Auto chip hidden (`autoAvailable` false), but the adjust button + nudge are roomplan-independent; the pin needs only the anchors (+ identity sidecar). `effectiveNudge` returns zero so "always" is a safe no-op there.
- **Node loss.** Deleting a whole *location* cleanly splits the graph — each survivor re-roots at min-UUID (the base is recomputed per render, never persisted → nothing stale) and surviving edge corrections persist (stored per-link, root-independent). ✓ **But** deleting a single *scan* that anchors a link **cascade-deletes the link** (`deleteRule: .cascade`; links never re-point) — so a room can lose its stitch by deleting one generation, and deleting scans is a supported rollback action. **Deferred** to a separate link-integrity fix.
- **(b) Room-to-room data sourcing — DONE.** The frame was already room-to-room (everything lifted to gen-0 canonical via `appliedT`), but the *data* was read from the stitch-time generation. Now `stitchCorrection` (walls) and `placeScans`' `scanForLocation` (rendered mesh) read each location's **canonical owner (gen-0)** via `StitchGraphBuilder.canonicalScan` — the registration reference, zero-registration-error, in the shared canonical frame — falling back to the link's scan if gen-0 has no roomplan (never lose availability). The **pin stays link-sourced** (the measured stitch point, canonical-lifted). Per pair the autocorrect is now reference-walls ↔ reference-walls, rescan-stable. Rescan *drift* itself is rebaked separately by `SaveRegistration` (per-scan raw→canonical), so corrections layer on drift-corrected frames.
**✅ LINK INTEGRITY across generation churn — DONE (re-point, 2026-07-27).** A scan deletion no longer destroys a still-connected room's stitch. `StitchLinkStore.repointIncidentLinks(beforeDeleting:in:)` runs before every **scan** deletion (`ScanFileManager.deleteScan`, `LocationDetailView.bulkDelete` partial, ScansListView `.latest`) — **not** room deletion — and re-points a link off the doomed scan onto a surviving generation of the same location (canonical owner preferred), re-expressing the pin **exactly**: `canonicalPin = appliedT(old)·anchor`, `newAnchor = inv(appliedT(new))·canonicalPin` (so `appliedT(new)·newAnchor == canonicalPin` — the rendered/exported geometry doesn't move). It saves before the caller's delete so the re-point beats the cascade.
- A room whose **last** scan is deleted has no survivor → the link cascades → the graph **bisects** (that's a room deletion, per design — user's note). Deleting gen-0 (canonical owner) re-points to the new oldest; the canonical frame lives on via the immutable registration sidecars, so it stays consistent until a Phase-4.4 re-baseline.
- The bulk-delete "deleting removes connections" warning now fires only when the map is fully emptied (a partial delete preserves its stitches).
- Chose re-point over room-scoping the link model: no SwiftData migration, and the pin is location-stable so the re-expression is lossless. Room-scoping the relationships is a possible deeper cleanup but isn't needed for correctness.

**✅ Adjuster UX polish (2026-07-27).** The map **legend now stays visible while adjusting** (edited row highlighted; any row's adjust button switches the target) — no reason to hide it since the legend is top-right and the adjuster is bottom (no overlap). The **"Auto" toggle is also in the adjuster header** (by Reset), so you can flip a join corrected/raw without leaving the sliders. And the adjuster is now **live-persist** — slider release, Auto, Reset, and Done all write through immediately (Save/Cancel removed) — so switching maps or dismissing never strands an uncommitted edit, matching the "what you see is applied and exported" model. Finally, the floating name labels now hold a **constant on-screen size** — scaled by camera distance each frame in the `SCNSceneRendererDelegate` render loop — instead of growing/shrinking with zoom (they're 3D geometry). Anchored to the default-framing size; the base size is a one-line tune if needed.
- **Bake at export (non-destructive) — the correction is authoritative downstream, no longer render-only.** `asDTO()` bakes the *effective* correction (auto-if-effective ∘ manual) into the exported SOURCE anchor via `StitchGraphBuilder.bakedSourceAnchor`: `A' = inv(T)·C·T·A` (the correction is source-canonical; `T` conjugates it back to the raw frame the wire/world-map convention uses). The model's stored anchors stay raw/as-measured — only the exported copy moves — so `stitching.json` / `graph.json` / the server now receive the corrected edge (`bakedSource · inv(target)` reproduces the combined-render placement). Answered the user's "does this propagate to the stitch anchor data?" → it didn't (render-only), now it does at the export boundary without destroying the measurement. Identity/no-op when nothing is applied.

**✅ REVIEW ROUND — self-review (7 finder angles) + Copilot round-2 (2026-07-27, `c32cc49`, sim build + placement tests green).** Fixed: (1) `labelBaseHeight` kept only the **last** label's plate height yet scaled all labels by it (plate height varies per name's glyphs) — now per-label `labelBaseHeights` keyed by scanId, with a floor guarding a zero/lazy `SCNText.boundingBox`; (2) `willRenderScene` (SceneKit **render thread**) reassigned-while-iterating `labelNodes` and read `UIView.bounds` off-main — now snapshots the dicts into locals and reads the viewport from a main-thread cache set in `updateUIView`; (3) `placeScans`' BFS walked `edges` in nondeterministic order → placements + each map's `parentLinkId` could vary run-to-run — now `build(from:)` sorts edges by link id (transforms unchanged, tie-break only); (4) diagnostics print the **signed** applied yaw (`yawSigned`), not the unsigned measured median; (5) corrected the "raw pin" comments — the nudge pivot is the **T-composed canonical** pin (`mSrc.columns.3 = tSrc·anchor`), the same pivot render/`effectiveCorrection`/`bakedSourceAnchor` all use (stale wording invited a pivot desync). Verified-and-**not**-fixed (false positives): re-import double-correction is **unreachable** (the bake only lands in the ephemeral export bundle, never the raw local `stitching.json` migration reads; reinstall wipes Documents); single-scan-delete orphan is **refuted** (`deleteScan` saves before the caller's `isEmpty` check); the auto⇄manual three-path matrices were **proven** to agree. Deferred (real, noted): export bakes on the main actor with per-link disk reads when `alwaysAutocorrect` is on + join never opened (thread the precomputed autos in); Op-2 solver hardening (yaw-sign tie → arbitrary `-yawFix`; 25° vs 10° near-∥ gate mismatch; single-wall ⊥ gate; sub-1° yaw floor) — **left untouched pending on-device tuning**.

**★ COMPASS-NULL disables auto (decision 2026-07-27): keep fail-closed; fix the root cause, not a fallback.** `stitchCorrection`'s guardrail is `trustworthy = compassDeg.map{ |‖yawFix‖ − ‖C_A−C_B‖| ≤ 15 } ?? false`, so if **either** endpoint's `anchorCompassHeading` is nil the whole auto-fix (yaw **and** ⊥) is skipped even when the walls agree perfectly. Root cause: `bestHeading` (`locationManager`, an optional) is captured verbatim at pin time in `CaptureView+Alignment/Extend/Recording` with no gate/retry — nil when Core Location has no heading fix (denied / not-yet-available / indoor magnetometer trouble, common right at the inter-room doorways where stitches are made), and it sticks forever. User's call: a yaw-without-compass **fallback is the wrong trade** (would apply un-certified fits); **no-autofix is the sensible floor**. Real fix is capture-time — ensure/await a heading at Pin A/B — and, short of that, **surface the problematic state to the user** (legend/badge: "no compass certificate → auto unavailable") instead of a silent no-op. Both deferred to a follow-up.

---

## Open decisions
- **Compass-null capture + feedback** (see ★ above): where/how to guarantee a heading fix at pin time (await `bestHeading`? request auth earlier? indoor fallback?), and how to surface "no compass certificate → auto unavailable" in the combined-render legend instead of silently withholding the fix.
- **Room-classification selector for the registration-mode ladder** (see "★ FOLLOW-UP DIRECTION" in the PRIMARY section): user-interpretable semantics (slider? "traditional / less traditional / very unusual" is placeholder), manual vs. auto-detect vs. auto-suggest+confirm, and where it lives in the flow (location creation).
- On-device vs. offloaded ICP (latency for live ghost-alignment feedback vs. compute).
- Canonical-scan selection policy: lean toward **best structural coverage** (it's the registration reference forever) — open question is how to score it (RoomPlan surface completeness? floor+wall area? explicit "reference scan" at setup?).
- ICP **inlier** residual threshold **and** the inlier-fraction floor (both set empirically from Phase 0). The floor is what separates "redecorated but registerable" from "re-baseline needed."
- Source of the stable scaffold for registration: RoomPlan surfaces vs. `.meshWithClassification` vs. geometric large-plane extraction (cost / availability tradeoff).
- Re-baseline policy: auto-suggest threshold + how much history re-registration to do eagerly vs. lazily.
- Keep `ARWorldMap` purely as bootstrap with mesh+pose-graph as source of truth, vs. a deeper mesh-first persistence rework.

## Related
- `docs/` design notes; memory: `ar-session-bringup-main-thread-freeze` (the `makeUIView` world-map deserialize freeze — Phase 1.3 shrinks what's loaded but the background-load fix is still wanted), `rescan-relocalization-chain-compounds-drift`.
